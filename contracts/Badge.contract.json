{
  "$schema": "https://tds.internal/schemas/component.v1.json",
  "name": "Badge",
  "version": "1.0.0",
  "level": "atom",
  "status": "beta",
  "description": "카운트 뱃지 — 제목/라벨 옆에 개수를 표시하는 원형 pill. 출처: apps/admin/src/pages/dashboard/components/Card.tsx 의 CountBadge (count<=0 이면 렌더하지 않음). 비대화형 표시 전용이며 클릭 이벤트를 갖지 않는다.",
  "owner": { "code": "component-eng", "design": "ui-design", "figma": "figma-eng" },

  "props": {
    "count": {
      "type": "number",
      "required": true,
      "description": "표시할 개수. hideWhenZero=true 이고 count<=0 이면 아무것도 렌더하지 않는다 (구현: `if (count <= 0) return null`)"
    },
    "tone": {
      "type": "enum",
      "values": ["neutral", "danger", "success"],
      "default": "neutral",
      "figmaProperty": "Tone",
      "required": false,
      "description": "시각 의미. neutral = 텍스트색 배경 위 서피스색 숫자(현행 구현), danger/success = feedback 토큰 페어"
    },
    "hideWhenZero": {
      "type": "boolean",
      "default": true,
      "figmaProperty": "HideWhenZero",
      "description": "count<=0 일 때 렌더 생략 여부. false 면 0 도 표시한다"
    }
  },

  "states": ["default"],

  "tokens": {
    "background": "color.text.default",
    "text": "color.surface.default",
    "backgroundDanger": "color.feedback.danger.surface",
    "textDanger": "color.feedback.danger.text",
    "backgroundSuccess": "color.feedback.success.surface",
    "textSuccess": "color.feedback.success.text",
    "radius": "radius.full",
    "size": "space.5",
    "paddingX": "space.1"
  },

  "a11y": {
    "role": "status",
    "keyboard": ["none — 비대화형. 포커스 순서에 들어가지 않는다"],
    "focusVisible": false,
    "aria": {
      "aria-live": "카운트가 런타임에 갱신되는 문맥에서는 polite. 정적 렌더 시 불필요",
      "labelling": "숫자만으로는 의미가 없다 — 인접 제목(CardTitle 등)이 접근 가능한 문맥을 제공해야 한다"
    },
    "contrastMin": 4.5
  },

  "responsive": {
    "breakpoints": ["sm", "md", "lg"],
    "behavior": "size-fixed"
  },

  "compat": {
    "breakingSince": null,
    "deprecatedProps": []
  },

  "dependencies": []
}
