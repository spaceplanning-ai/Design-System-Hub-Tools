{
  "$schema": "https://tds.internal/schemas/component.v1.json",
  "name": "Button",
  "version": "1.1.0",
  "level": "atom",
  "status": "beta",
  "description": "기본 액션 버튼. 이 계약에서 React Props 타입, Storybook argTypes, Figma Component Properties, Docs가 자동 생성된다 (tools/codegen). G3 승인 시 Frozen — 변경은 변경 요청 → G3 재진입 + SemVer 재판정.\n\n[네이티브 속성 패스스루 — 계약 prop 이 아니다] 계약 props 외의 표준 HTML/ARIA 속성(aria-label · aria-describedby · aria-busy · title …)은 구현이 <button> 으로 그대로 전달한다 (Card 선례: CardProps & Omit<HTMLAttributes,'style'|'children'|'className'>). 이 속성들은 시각 변형이 아니고 Figma Component Property 대응도 없으므로 계약 prop 으로 열거하지 않는다. 실사용: RolePanel(aria-describedby·title) · PointsCard(aria-label) · ConfirmDialog(aria-busy). className/style 은 토큰 규칙 보호를 위해 차단한다.",
  "owner": { "code": "component-eng", "design": "ui-design", "figma": "figma-eng" },

  "props": {
    "variant": {
      "type": "enum",
      "values": ["primary", "secondary", "ghost", "danger"],
      "default": "primary",
      "figmaProperty": "Variant",
      "required": false,
      "description": "시각 위계. 디자인 스펙(DS)의 variant 목록과 완전 일치해야 함. 기본값 primary 는 유지한다 — apps/admin 의 <Button> 호출부는 전수 감사 결과 100% variant 를 명시하고 있어(무지정 호출부 0건) 기본값 차이로 시각이 뒤집히는 호출부가 없다"
    },
    "type": {
      "type": "string",
      "values": ["button", "submit", "reset"],
      "default": "button",
      "required": false,
      "description": "네이티브 button type. 허용 값은 button · submit · reset 이며 그 외 값은 구현이 button 으로 좁힌다. **기본값 button 은 HTML 기본값(submit) 을 의도적으로 뒤집은 DS 결정이다** — 폼 안의 보조 버튼이 실수로 제출하지 않게 한다. submit 을 주면 폼을 제출한다 (실사용: LoginForm · RoleFormModal · CreateGroupModal · PasswordChangeModal · PointsCard 의 폼 5개). [enum 이 아닌 이유] type 은 시각 변형이 아니라 HTML 시맨틱이라 Figma Component Property 대응이 없다. enum 으로 선언하면 스키마가 figmaProperty 를 강제해(G3) 시각차 0인 3값 Figma Variant 축이 생기고, contract-test 의 조합 커버리지 요구가 3배로 뛴다. 허용 값은 values 로 기술하되 Figma/Variant 축은 만들지 않는다 (스키마 확장은 후속 변경 요청으로 제안)"
    },
    "size": {
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "figmaProperty": "Size",
      "required": false
    },
    "loading": {
      "type": "boolean",
      "default": false,
      "figmaProperty": "Loading",
      "description": "로딩 중 스피너 표시 + onClick 차단 + aria-busy"
    },
    "disabled": {
      "type": "boolean",
      "default": false,
      "figmaProperty": "Disabled",
      "description": "비활성. onClick 차단 + aria-disabled"
    },
    "isFullWidth": {
      "type": "boolean",
      "default": false,
      "figmaProperty": "FullWidth",
      "description": "컨테이너 100% 폭. 기본은 내용 폭(inline-flex). 실사용: 로그인 제출 CTA (LoginForm — 오너 확정 화면의 시각이다). [이름] boolean prop 은 is/has/can 접두 또는 상태 형용사 화이트리스트만 허용된다 (naming-guard boolean-prop 규칙 · ADR-0005) — CR 원안의 fullWidth 는 규칙 위반이라 isFullWidth 로 좁혔다. Figma 쪽 이름은 FullWidth 로 유지한다"
    },
    "iconLeft": {
      "type": "slot",
      "accepts": ["Icon"],
      "default": null,
      "hiddenWhen": ["loading"],
      "description": "좌측 아이콘 슬롯. loading 중에는 스피너로 대체되어 숨김"
    },
    "children": {
      "type": "node",
      "required": true,
      "description": "버튼 레이블"
    }
  },

  "events": {
    "onClick": {
      "payload": "MouseEvent",
      "blockedWhen": ["disabled", "loading"],
      "description": "disabled/loading 상태에서는 발화 금지 — Storybook Play Function이 전수 검증"
    }
  },

  "states": ["default", "hover", "active", "focus-visible", "disabled", "loading"],

  "tokens": {
    "background": "component.button.background",
    "backgroundHover": "component.button.background-hover",
    "backgroundActive": "component.button.background-active",
    "backgroundDisabled": "component.button.background-disabled",
    "text": "component.button.text",
    "focusRing": "component.button.focus-ring",
    "radius": "component.button.radius",
    "paddingX": "component.button.padding-x",
    "paddingY": "component.button.padding-y",
    "gap": "component.button.gap",
    "typography": "component.button.typography",
    "transitionDuration": "component.button.transition-duration",
    "transitionEasing": "component.button.transition-easing"
  },

  "a11y": {
    "role": "button",
    "keyboard": ["Enter", "Space"],
    "focusVisible": true,
    "ariaDisabled": "when disabled",
    "ariaBusy": "when loading",
    "aria": {
      "native-passthrough": "계약 props 외의 표준 HTML/ARIA 속성(aria-label · aria-describedby · aria-busy · title …)은 <button> 으로 그대로 전달한다 — 새 prop 을 만들지 않는다 (Card 선례). 호출부가 명시한 aria-busy 는 loading 파생값보다 우선한다: ConfirmDialog 는 스피너 없이 aria-busy 만 필요로 한다",
      "type-submit": "type=\"submit\" 은 폼 제출 시맨틱이다 — 이 버튼이 <form> 안에 있으면 Enter 키 제출도 함께 살아난다. type 을 하드코딩하면 폼이 조용히 죽는다"
    },
    "contrastMin": 4.5
  },

  "responsive": {
    "breakpoints": ["sm", "md", "lg"],
    "behavior": "size-fixed"
  },

  "compat": {
    "breakingSince": null,
    "deprecatedProps": []
  },

  "dependencies": []
}
