{
  "$schema": "https://tds.internal/schemas/component.v1.json",
  "name": "FileDropzone",
  "version": "1.0.0",
  "level": "molecule",
  "category": "File",
  "taxonomyItem": "drag-and-drop-zone",
  "status": "beta",
  "description": "가로형 파일 드롭존 — 클릭하면 탐색기가 열리고, 끌어다 놓아도 된다. 출처: apps/admin/src/shared/ui/FilePicker.tsx (사이트 설정의 파비콘·대표 이미지·비공개 이미지가 공유).\n\n[왜 ImageUploadField 가 아닌가] 그 필드는 미리보기를 자기가 소유하는 큰 정사각 드롭존이다. 이쪽은 FileChip 과 가로로 나란히 서는 **한 줄짜리 조각**이고, 미리보기는 완전히 다른 자리(브라우저 탭 목업·OG 카드)에 그려진다.\n\n[진짜 <input type=\"file\"> 은 감춘다] 파일 입력의 기본 UI 는 스타일링이 되지 않는다. 그래서 <button> 이 AT 에 보이는 컨트롤이고 숨은 입력은 탐색기를 여는 트리거일 뿐이다(aria-hidden + tabIndex -1) — ImageUploadField 와 같은 판정이다.\n\n[검증하지 않는다] 파일을 고르는 일까지만 한다. 확장자·용량·해상도 검증과 업로드는 호출부가 소유한다 — 화면마다 규칙이 다르고(파비콘은 ICO 16x16, 비공개 이미지는 PNG/JPG/GIF) 해상도 검사는 비동기라 여기서 삼키면 호출부가 실패를 알 수 없다. accept 는 탐색기의 1차 필터일 뿐 **보증이 아니다**.\n\n[같은 파일 재선택] change 후 입력 값을 비운다 — 그러지 않으면 검증 실패 뒤 같은 파일을 다시 고르는 경로가 조용히 막힌다.",
  "owner": { "code": "component-eng", "design": "ui-design", "figma": "figma-eng" },

  "props": {
    "label": {
      "type": "string",
      "required": true,
      "description": "접근 가능한 이름의 뿌리 — 문구만 있는 버튼이라 무엇을 고르는 자리인지 여기서 밝힌다. 실제 aria-label 은 '<label> — 클릭하거나 파일을 끌어다 놓으세요'"
    },
    "title": {
      "type": "string",
      "required": true,
      "description": "1차 안내 문구 — '파일 선택 또는 끌어다 놓기'. 한국어 어절이 갈리지 않게 word-break: keep-all 로 그린다"
    },
    "meta": {
      "type": "string",
      "default": "",
      "description": "형식·크기 안내 — '최소 16x16 / ICO'. 비우면 둘째 줄이 없다"
    },
    "accept": {
      "type": "string",
      "default": "",
      "description": "<input type=\"file\"> 의 accept — 탐색기의 1차 필터일 뿐 보증이 아니다(호출부가 다시 검증한다)"
    },
    "describedBy": {
      "type": "string",
      "default": "",
      "description": "오류/힌트 문단의 id — 문단 자체는 호출부가 소유한다. 비우면 aria-describedby 를 렌더하지 않는다"
    },
    "disabled": {
      "type": "boolean",
      "default": false,
      "figmaProperty": "Disabled",
      "description": "비활성 — 클릭·드롭을 함께 막고 흐리게 표시한다. onSelect 발화 없음"
    },
    "isInvalid": {
      "type": "boolean",
      "default": false,
      "figmaProperty": "Invalid",
      "description": "오류 상태 — 테두리를 danger 색으로 바꾼다. **문구는 그리지 않는다**(호출부가 describedBy 로 잇는 문단이 소유한다)"
    }
  },

  "events": {
    "onSelect": {
      "payload": "File",
      "blockedWhen": ["disabled"],
      "description": "고른 파일 1건을 발화한다(탐색기 선택·드롭 모두 같은 경로). 검증은 호출부가 한다. disabled 에서는 발화 금지 — <button disabled> 와 드롭 핸들러의 조기 반환이 함께 막는다 (Storybook Play Function 이 전수 검증)"
    }
  },

  "states": ["default", "hover", "focus-visible", "disabled", "error"],

  "tokens": {
    "gap": "space.1",
    "padding": "space.4",
    "borderDefault": "color.border.default",
    "borderActive": "color.action.primary.default",
    "borderInvalid": "color.feedback.danger.border",
    "borderWidth": "border-width.thin",
    "borderWidthActive": "border-width.medium",
    "radius": "radius.md",
    "surface": "color.surface.default",
    "surfaceActive": "color.surface.raised",
    "titleText": "color.text.default",
    "titleTypography": "typography.label.md",
    "metaText": "color.text.muted",
    "metaTypography": "typography.caption.md",
    "textDisabled": "color.text.disabled",
    "focusRing": "color.border.focus",
    "focusRingWidth": "border-width.medium",
    "transitionDuration": "motion.duration.fast"
  },

  "a11y": {
    "role": "button",
    "keyboard": ["Tab", "Enter", "Space"],
    "focusVisible": true,
    "aria": {
      "label": "'<label> — 클릭하거나 파일을 끌어다 놓으세요' — 두 조작 경로를 이름 안에서 함께 알린다",
      "aria-describedby": "describedBy 가 비어 있지 않으면 호출부의 오류/진행 문단을 잇는다",
      "hidden-input": "진짜 <input type=file> 은 aria-hidden + tabIndex -1 이다 — AT 에는 버튼 하나만 보인다(중복 탭 정지점 없음)",
      "locking": "disabled 이면 <button disabled> 로 잠그고 drop 핸들러도 조기 반환한다 — 드래그 경로가 잠금을 우회하지 못한다"
    },
    "contrastMin": 4.5
  },

  "responsive": {
    "breakpoints": ["sm", "md", "lg"],
    "behavior": "fluid"
  },

  "compat": {
    "breakingSince": null,
    "deprecatedProps": []
  },

  "dependencies": [],

  "anatomy": {
    "kind": "frame",
    "name": "FileDropzone",
    "layout": "VERTICAL",
    "align": "CENTER",
    "justify": "CENTER",
    "styles": {
      "fill": "surface",
      "stroke": "borderDefault",
      "strokeWidth": "borderWidth",
      "radius": "radius",
      "pad": "padding",
      "gap": "gap"
    },
    "children": [
      {
        "kind": "text",
        "name": "Title",
        "text": "파일 선택 또는 끌어다 놓기",
        "textProp": "title",
        "styles": { "color": "titleText", "typography": "titleTypography" }
      },
      {
        "kind": "text",
        "name": "Meta",
        "text": "최소 16x16 / ICO",
        "textProp": "meta",
        "styles": { "color": "metaText", "typography": "metaTypography" }
      }
    ]
  }
}
