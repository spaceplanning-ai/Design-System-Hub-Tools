{
  "$schema": "https://tds.internal/schemas/component.v1.json",
  "name": "RowSelectCell",
  "version": "1.0.0",
  "level": "molecule",
  "status": "beta",
  "description": "표 행의 선택 칸 — 보이지 않는 라벨 + 단일 체크박스를 담은 <td>. 목록 표(회원·운영자·공지·FAQ·팝업·배너·버전 이력)의 행 선택 마크업을 한 벌로 올렸다. 도메인을 모른다 — 행 식별자(id)·보이지 않는 라벨 문구(label)·checked·토글 콜백만 받는다. 출처: apps/admin/src/shared/ui/TableSelection.tsx 의 RowSelectCell (TableSelection 묶음의 행-셀 대표). 보이는 라벨이 없으므로 시각적으로 숨긴 문구를 두고 aria-labelledby(id 파생) 로 잇는다.",
  "owner": { "code": "react-refactor", "design": "ui-design", "figma": "figma-eng" },

  "props": {
    "id": {
      "type": "string",
      "required": true,
      "description": "행 식별자 — 보이지 않는 라벨의 id('select-{id}')를 파생한다(한 문서에 여러 표가 있어도 안 겹친다)"
    },
    "label": {
      "type": "string",
      "required": true,
      "description": "보이지 않는 라벨 문구 — 예: '{공지 제목} 선택'. aria-labelledby 로 체크박스에 이어진다"
    },
    "checked": {
      "type": "boolean",
      "required": true,
      "figmaProperty": "Checked",
      "description": "선택 여부. 제어 값 — onToggle 로만 바뀐다"
    }
  },

  "events": {
    "onToggle": {
      "payload": "boolean",
      "description": "다음 선택 상태(boolean)를 인자로 발화한다"
    }
  },

  "states": ["default", "focus-visible", "checked"],

  "tokens": {
    "control": "color.action.primary.default",
    "cellText": "color.text.default",
    "cellBorder": "color.border.default",
    "cellPaddingX": "space.2",
    "cellPaddingY": "space.3",
    "controlSize": "space.4",
    "cellWidth": "space.6",
    "focusRing": "color.border.focus",
    "borderWidth": "border-width.thin"
  },

  "a11y": {
    "role": "checkbox",
    "keyboard": ["Space", "Tab"],
    "focusVisible": true,
    "aria": {
      "aria-labelledby": "시각적으로 숨긴 라벨(span#select-{id})을 가리킨다 — 스크린리더가 '무엇을 고르는지' 읽는다"
    },
    "contrastMin": 4.5
  },

  "responsive": {
    "breakpoints": ["sm", "md", "lg"],
    "behavior": "size-fixed"
  },

  "compat": {
    "breakingSince": null,
    "deprecatedProps": []
  },

  "dependencies": []
}
