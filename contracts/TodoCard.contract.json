{
  "$schema": "https://tds.internal/schemas/component.v1.json",
  "name": "TodoCard",
  "version": "2.0.0",
  "level": "organism",
  "status": "beta",
  "description": "'오늘의 할일' 카드. Card + Badge 를 조립해 처리 대기 건수를 한 줄로 보여주는 업무 위젯 (ADR-0003 organism). items 는 데이터 prop 이라 Figma 대응이 없다 — 탭(상품·문의·영업)에 따라 항목 집합이 통째로 바뀌므로 항목을 계약에 고정하지 않고 호출부(Pages)가 주입한다.\n\n[2.0.0 — SPA 내비게이션 탈출구] ListCard 와 동일 결함·동일 처방이다 (같은 판정으로 함께 올린다). onItemClick 이 MouseEvent 를 함께 넘겨 호출부가 preventDefault() 후 navigate() 할 수 있다.",
  "owner": { "code": "component-eng", "design": "ui-design", "figma": "figma-eng" },

  "props": {
    "title": {
      "type": "string",
      "default": "오늘의 할일",
      "description": "카드 제목"
    },
    "items": {
      "type": "array",
      "itemShape": "{ key: string; label: string; count: number; href?: string }",
      "required": true,
      "description": "할일 항목 목록. count > 0 인 항목은 강조색(color.feedback.danger.text), 0 인 항목은 흐리게(color.text.disabled) 렌더한다. href 가 있으면 링크, 없으면 정적 텍스트. 데이터 prop — Figma Component Property 대응 없음 (ADR-0003)"
    },
    "loading": {
      "type": "boolean",
      "default": false,
      "figmaProperty": "Loading",
      "description": "로딩 중 스켈레톤 표시 + aria-busy + onItemClick 차단"
    },
    "showTotal": {
      "type": "boolean",
      "default": true,
      "figmaProperty": "ShowTotal",
      "description": "제목 옆에 items 의 count 합계를 Badge 로 표시"
    }
  },

  "events": {
    "onItemClick": {
      "payload": "{ key: string; event: MouseEvent }",
      "blockedWhen": ["loading"],
      "description": "항목 클릭 시 해당 item.key 와 **원본 MouseEvent** 를 함께 전달한다. 호출부가 event.preventDefault() 로 기본 내비게이션을 가로채 SPA 라우팅으로 바꿀 수 있다. loading 상태에서는 발화 금지 (구현은 이미 event.preventDefault() 로 <a> 기본 동작도 막는다 — TodoCard.tsx:25-28) — Storybook Play Function이 전수 검증. **2.0.0 파괴적 변경**: 1.x 의 payload 는 string 이었다"
    }
  },

  "states": ["default", "loading"],

  "tokens": {
    "surface": "color.surface.default",
    "border": "color.border.default",
    "radius": "radius.md",
    "padding": "space.5",
    "gap": "space.4",
    "itemGap": "space.2",
    "title": "color.text.default",
    "titleTypography": "typography.label.md",
    "typography": "typography.body.md",
    "countPending": "color.feedback.danger.text",
    "countEmpty": "color.text.disabled",
    "focusRing": "color.border.focus"
  },

  "a11y": {
    "role": "region",
    "keyboard": ["Tab", "Shift+Tab", "Enter"],
    "focusVisible": true,
    "ariaBusy": "when loading",
    "aria": {
      "aria-labelledby": "카드 제목 요소를 가리켜 region 에 접근성 이름을 부여한다"
    },
    "contrastMin": 4.5
  },

  "responsive": {
    "breakpoints": ["sm", "md", "lg"],
    "behavior": "fluid"
  },

  "compat": {
    "breakingSince": "2.0.0",
    "deprecatedProps": []
  },

  "dependencies": ["Card", "Badge"]
}
