// The generated bundle is inlined by esbuild's json loader at build time.
// Typed as `any` here (then cast to Bundle in code.ts) so tsc does not expand the
// ~400KB JSON into a giant literal type on every typecheck.
declare module '*.json' {
  const value: unknown;
  export default value;
}
