{/* DateRangeField — Docs 페이지. 서술의 근거는 contracts/DateRangeField.contract.json 이다. */}

import { Controls, Meta, Story } from '@storybook/blocks';

import * as DateRangeFieldStories from './DateRangeField.stories';

<Meta of={DateRangeFieldStories} />

# DateRangeField

<code>molecule</code> · <code>Inputs</code> · <code>taxonomyItem: date-picker</code> · <code>v1.1.0</code> · <code>status: beta</code>

노출 기간(시작~종료) 입력 — 날짜 입력 두 칸을 '~' 로 잇고 아래에 인라인 오류/힌트를 그린다.
출처: `apps/admin/src/shared/ui/DateRangeField.tsx`
(소비 9곳: 팝업·배너·이벤트·프로모션·쿠폰·계약·프로젝트·견적).

두 화면이 각자 날짜 두 칸 + 종료≥시작 오류 자리를 복사하면 **배치·오류 문구가 어긋난다** — 한 벌만 둔다.

**도메인을 모른다** — 무엇의 기간인지 알지 못한다.

## Props

<Controls />

| prop | 타입 | 기본값 | 의미 |
|---|---|---|---|
| `label` | `string` | — (필수) | 그룹 라벨 텍스트 — 두 날짜 칸 위에 한 번 그린다. **각 칸의 숨김 라벨('label 시작일'·'label 종료일')도 여기서 파생한다.** |
| `startValue` | `string` | — (필수) | 시작일 제어값 (`YYYY-MM-DD`). |
| `endValue` | `string` | — (필수) | 종료일 제어값 (`YYYY-MM-DD`). |
| `required` | `boolean` | `false` | 필수 필드 — 그룹 라벨 옆에 `aria-hidden` 마커(\*)를 붙이고, **두 날짜 입력 각각에** native `required` + `aria-required` 를 낸다 (범위는 시작·종료가 함께 있어야 성립한다). |
| `disabled` | `boolean` | `false` | 비활성 — 두 날짜 입력을 native `disabled` 로 함께 막는다. |
| `error` | `string` | `''` | 인라인 오류 메시지(종료≥시작 위반 등). 값이 있으면 `role=alert` 로 오류 `<p>` 를 그리고 **두 입력에** `aria-invalid=true` 를 준다. |
| `hint` | `string` | `''` | 보조 안내 — **오류가 없을 때만** 그린다. |

### 검증은 스키마가 소유한다

계약 `description`: 종료≥시작 같은 규칙은 **이 컴포넌트가 판정하지 않는다** — 호출부의 zod 스키마가 판정해
`error` 로 내려준다(검증 정본은 스키마). 이 컴포넌트는 `error` 문자열을 `role=alert` 로 주입해 그릴 뿐이다.

### 라벨 구조

그룹 라벨(`<span>`)은 두 칸 위에 한 번 그리고, 각 날짜 입력에는 **visually-hidden `<label>`**
('… 시작일'·'… 종료일')을 `htmlFor` 로 붙여 스크린리더가 두 칸을 구분하게 한다.
`id` 는 `useId` 로 각각 생성한다.

### 계약이 명시한 예외 — `exactOptionalPropertyTypes`

옵셔널 문자열 prop(`error`·`hint`)은 호출부가 `string | undefined` 를 그대로 넘긴다 —
구현이 경계에서 `undefined` 를 허용해 받고 정규화한다 (**9곳 호출부 무변경**).

## States

계약 `states`: `default` · `focus-visible` · `disabled` · `error`.

<Story of={DateRangeFieldStories.Default} />
<Story of={DateRangeFieldStories.FocusVisible} />
<Story of={DateRangeFieldStories.Disabled} />
<Story of={DateRangeFieldStories.Error} />

## 필수

<Story of={DateRangeFieldStories.RequiredField} />
<Story of={DateRangeFieldStories.RequiredDisabled} />

## 이벤트

| 이벤트 | payload | 의미 |
|---|---|---|
| `onStartChange` | `string` | 시작일 변경 — 새 문자열(`event.target.value`)을 넘긴다. |
| `onEndChange` | `string` | 종료일 변경 — 새 문자열을 넘긴다. |

둘 다 `blockedWhen` 이 **없다** — 잠금은 native `disabled` 가 담당한다.

## 접근성 계약

- **role**: `group`
- **keyboard**: `Tab` · `Shift+Tab`
- **focusVisible**: `true`
- **contrastMin**: 4.5

계약 `a11y.aria`:

- `group-label`: 그룹 라벨(`<span>`)은 두 칸 위에 한 번. 각 날짜 입력엔 visually-hidden `<label>`(`htmlFor`)로
  '시작일'·'종료일'을 구분해 준다.
- `role-alert`: 오류 `<p>` 는 `role=alert` 로 즉시 알린다. **색만으로 전달하지 않고 메시지 텍스트를 함께 그린다.**
- `aria-invalid`: `error` 가 빈 문자열이 아닐 때 두 입력 모두 `aria-invalid=true` 이며,
  **항상** `aria-describedby` 로 오류 `<p>` id 를 함께 가리킨다 (describedby 없는 `aria-invalid` 금지 — A11Y-11).
  유효할 때는 두 속성 모두 부여하지 않는다.
- `native-disabled`: `disabled` 는 두 입력에 native 속성으로 반영된다.
- `aria-required`: `required=true` 이면 **두 입력 모두** native `required` + `aria-required="true"` 를 받는다 —
  범위는 시작·종료가 함께 있어야 성립하므로 각 칸에 참이다. 그룹 라벨의 마커(\*)는 `aria-hidden` 장식이라
  시각 경로일 뿐이다 (A11Y-11).

## 토큰

`color.surface.default` · `color.text.default` · `color.border.default` ·
`color.feedback.danger.border`(borderInvalid) · `color.border.focus`(focusRing) ·
`color.text.default`(labelText) · `typography.label.md` · `color.text.muted`(tilde·hintText) ·
`color.feedback.danger.text`(errorText) · `radius.md` · `space.3`(paddingX) ·
`space.2`(paddingY·rowGap).

`responsive.behavior` 는 `fluid`. 의존 없음.

<Story of={DateRangeFieldStories.DarkTheme} />
<Story of={DateRangeFieldStories.RightToLeft} />
