{/* ImageGalleryField — Docs 페이지. 서술의 근거는 contracts/ImageGalleryField.contract.json 이다. */}

import { Controls, Meta, Story } from '@storybook/blocks';

import * as ImageGalleryFieldStories from './ImageGalleryField.stories';

<Meta of={ImageGalleryFieldStories} />

# ImageGalleryField

<code>molecule</code> · <code>Inputs</code> · <code>v1.1.0</code> · <code>status: beta</code>

다중 이미지 업로드(갤러리) 필드 — ImageUploadField 와 **같은 메커니즘**(드래그드롭·클릭 선택·object URL
프리뷰·클라이언트 검증)을 여러 장으로 확장한다. 값(`values`)은 **이미지 URL 배열**이다.

**도메인을 모른다** — `label`/`values`/`onChange` 와 힌트만 받는다 (ADR-0003).

> 이 컴포넌트의 계약에는 `taxonomyItem` 이 없다.

## Props

<Controls />

| prop | 타입 | 기본값 | 의미 |
|---|---|---|---|
| `label` | `string` | — (필수) | 필드 라벨 — 드롭존의 접근 가능한 이름과 **각 타일 alt 의 기준.** |
| `values` | `array` | — (필수) | 현재 이미지 URL 배열. 빈 배열이면 드롭존만, 있으면 그리드 프리뷰 + 개별 제거. 데이터 prop — Figma 대응 없음. |
| `required` | `boolean` | `false` | 필수 표식(\*)을 라벨에 붙인다 (**장식 — `aria-hidden`**). |
| `disabled` | `boolean` | `false` | 드롭존/추가/제거를 비활성한다. |
| `error` | `string` | `''` | 스키마가 내려주는 오류 — **로컬 검증 오류(타입·용량·개수)보다 우선.** 비어 있지 않으면 danger 테두리 + `role=alert` 인라인 오류. |
| `hint` | `string` | `''` | 도움말 문구 — **오류가 없을 때만** 표시된다. |
| `maxFiles` | `number` | `10` | 등록 가능한 최대 장수. **초과분은 인라인 오류로 막고 상한까지만 담는다.** |
| `maxSizeMB` | `number` | `5` | 파일당 용량 상한(MB). 초과하면 인라인 오류로 막는다. |

### 계약이 명시한 설계 결정

- **손코딩 유지**: react-dropzone **기각**(오너 검토) — 드롭존 버튼을 직접 만든다.
- **프리뷰**: 그리드 타일, 각 타일에 개별 제거 버튼. **순서는 추가 순서 유지.**
  타일 URL 이 비거나 로드 실패면 이미지 아이콘 placeholder.
- **검증**: `image/*` · 파일당 용량 상한(`maxSizeMB`) · 개수 상한(`maxFiles`).
  위반은 **인라인 오류(토스트 아님)**. `imageFileError` 순수 함수를 ImageUploadField 와 공유.
- **누수 방지**: **우리가 만든 object URL 만** 추적해 제거/언마운트 시 `revoke` 한다.

### 계약이 명시한 예외 — 경계

`onChange` 는 새 URL 배열을 넘기는 **값 콜백**이다 (네이티브 이벤트가 아니다).
`error`/`hint` 는 `exactOptionalPropertyTypes` 경계에서 `undefined` 허용으로 넓힌다 (B2 선례).

## States

계약 `states`: `default` · `error` · `disabled`.

<Story of={ImageGalleryFieldStories.Empty} />
<Story of={ImageGalleryFieldStories.WithImages} />
<Story of={ImageGalleryFieldStories.Full} />
<Story of={ImageGalleryFieldStories.WithError} />
<Story of={ImageGalleryFieldStories.WithHint} />
<Story of={ImageGalleryFieldStories.Disabled} />

## 필수

<Story of={ImageGalleryFieldStories.Required} />
<Story of={ImageGalleryFieldStories.RequiredDisabled} />

## 접근성 계약

- **role**: `button` (드롭존 기준)
- **keyboard**: `Enter` · `Space` · `Tab`
- **focusVisible**: `true`
- **contrastMin**: 4.5

계약 `a11y.aria`:

- `dropzone`: 드롭존/추가 칸은 `<button>` — 접근 가능한 이름을 `aria-label` 로 준다. Enter/Space 로 파일 선택을 연다.
- `file-input`: 실제 `<input type=file multiple>` 은 **시각적으로 숨긴 트리거** — `tabIndex=-1`, `aria-hidden`.
- `remove`: 각 타일 제거 버튼은 focusable + `aria-label`(`'N번째 이미지 제거'`).
- `error`: `error` 가 비어 있지 않으면 `role=alert` 로 즉시 통지하고 드롭존에 `aria-describedby` 로 연결한다.
- `tile`: 타일 이미지는 label+순번 기준 `alt`. placeholder 아이콘은 `role=img` + `aria-label`.

## 토큰

`color.border.default`(dropzoneBorder) · `color.border.focus`(dropzoneBorderActive) ·
`color.feedback.danger.border`(dropzoneBorderInvalid) · `color.surface.raised`/`.default` ·
`color.border.default`(tileBorder) · `color.surface.raised`(tileSurface) ·
`color.surface.default`(removeSurface) · `color.feedback.danger.text`(removeColor) ·
`color.action.primary.default`(iconColor) · `color.text.default`/`.muted` ·
`color.feedback.danger.text`(errorText) · `color.feedback.success.text`(successText) ·
`radius.md` · `space.3`(gap) · `color.border.focus`(focusRing) · `typography.label.md` ·
`space.8`(iconButtonSize).

`responsive.behavior` 는 `fluid`. 의존 없음.

<Story of={ImageGalleryFieldStories.DarkTheme} />
