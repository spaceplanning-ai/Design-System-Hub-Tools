{/* Tabs — Docs 페이지. 서술의 근거는 contracts/Tabs.contract.json 이다. */}

import { Controls, Meta, Story } from '@storybook/blocks';

import * as TabsStories from './Tabs.stories';

<Meta of={TabsStories} />

# Tabs

<code>molecule</code> · <code>Navigation</code> · <code>taxonomyItem: tabs</code> · <code>v1.0.0</code> · <code>status: beta</code>

하단 밑줄형 탭 목록.

계약 `description`: 선택된 탭만 액션 컬러 밑줄을 갖고, **비선택 탭도 동일 두께의 투명 밑줄을 둬
전환 시 라벨이 밀리지 않는다.**

**탭 패널은 이 컴포넌트가 렌더하지 않는다** — `aria-controls` 로 바깥 패널을 가리킨다.
**도메인 중립**: 탭 항목은 `items` prop 으로 주입한다 (ADR-0003).
출처 구현: `apps/admin/src/pages/dashboard/components/TabBar.tsx`.

## Props

<Controls />

| prop | 타입 | 기본값 | 의미 |
|---|---|---|---|
| `value` | `string` | — (필수) | 선택된 탭의 id. `items[].id` 중 하나여야 한다. |
| `items` | `array` | — (필수) | 탭 목록. 데이터 prop — Figma Component Property 대응 없음 (ADR-0003). |
| `ariaLabel` | `string` | — (필수) | tablist 의 접근 가능한 이름 (예: '업무 영역'). |

### 이벤트 — `onChange`

payload `string`(선택된 탭의 id) · `blockedWhen` **없음** — Tabs 에는 `disabled` prop 이 없다.

## States

계약 `states`: `default` · `hover` · `focus-visible` · `selected`. **disabled 상태가 없다.**

<Story of={TabsStories.Default} />
<Story of={TabsStories.Hover} />
<Story of={TabsStories.FocusVisible} />
<Story of={TabsStories.Selected} />

## 항목 개수

<Story of={TabsStories.MinimalItems} />
<Story of={TabsStories.LongItems} />

## 키보드 이동

<Story of={TabsStories.ArrowKeyNavigation} />

## 접근성 계약

- **role**: `tablist`
- **keyboard**: `Tab` · `ArrowLeft` · `ArrowRight` · `Home` · `End`
- **focusVisible**: `true`
- **contrastMin**: 4.5

계약 `a11y.aria`:

- `aria-label`: `ariaLabel` prop — tablist 의 이름.
- `aria-selected`: 각 탭(`role=tab`) — `value` 와 일치하면 `true`.
- `aria-controls`: 각 탭 — **대응하는 tabpanel 의 id.**
  패널을 렌더하지 않으므로 **그 id 가 실제로 존재하는지는 조립하는 쪽의 책임이다.**

## 토큰

`color.border.default`(listBorder) · `color.action.primary.default`(indicatorSelected) ·
`color.text.muted`(text) · `color.text.default`(textSelected) · `typography.label.md` ·
`space.2`(paddingTop) · `space.3`(paddingBottom) · `space.5`(gap) ·
`color.border.focus`(focusRing) · `motion.duration.fast` · `motion.easing.standard`.

`responsive.behavior` 는 `fluid`. 의존 없음.

<Story of={TabsStories.RightToLeft} />
