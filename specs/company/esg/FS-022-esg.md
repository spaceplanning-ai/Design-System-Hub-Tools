---
id: FS-022
title: "ESG 활동 관리 (목록·분류 필터·등록/수정)"
screen: SCR-022               # ⚠ 기업 관리 SCR 미작성 — §7 미결 사항 참조
route: /company/esg
owner: A62
reviewer: A64
gate: G9
status: draft
confirmedAt: 2026-07-17
version: 1.0
date: 2026-07-17
---

# FS-022. ESG 활동 관리 (목록·분류 필터·등록/수정)

## 1. 화면 개요

| 항목 | 내용 |
|---|---|
| 목적 | 고객 화면에 노출되는 ESG 활동(환경·사회·지배구조)을 분류로 좁혀 찾고, 개별 활동을 등록·수정·삭제(단건·일괄)한다 |
| 역할(주 사용자) | 관리자 (구현에 역할 분기 없음 — §4.1) |
| 진입 경로 | 좌측 GNB > 기업 관리 > ESG (`/company/esg`) |
| 포함 화면 | 목록 `/company/esg` · 등록 `/company/esg/new` · 수정 `/company/esg/:id/edit`. **상세 조회 화면은 없다** — 행을 클릭하면 곧바로 수정 폼으로 간다 |
| 구현 경로 | `apps/admin/src/pages/company/esg/**` (`EsgListPage.tsx` · `EsgFormPage.tsx` · `EsgCategoryFilter.tsx` · `data-source.ts` · `types.ts` · `validation.ts` · `esg.test.ts`) |
| 대응 SCR | SCR-022 (미작성 — §7) |
| 공통 컴포넌트 | `shared/crud/{CrudListShell,CrudTable,useCrudList,FormPageShell,useCrudForm,createCrudAdapter,requiredText}` · `shared/ui/{filterPanelStyle,filterNavStyle,filterListStyle,filterHeadingStyle,filterItemStyle,badgeStyle,StatusBadge,Button,PlusCircleIcon,SelectionBar,RowSelectCell,SelectAllHeaderCell,SeqCell,RowActions,Empty,ConfirmDialog,FormField,SelectField,TextareaField,ImageGalleryField,useRowSelection,useUnsavedChangesDialog}` · `shared/format.formatNumber` · `shared/useRowNavigation` · `shared/bulk.settleAll` |
| 검증 정본 | `validation.ts` 의 `esgSchema` (zod/mini) |
| 순수 규칙 정본 | `types.ts` 의 `sortEsg`(일자 내림차순) · `filterEsg`(분류 필터) · `countEsgByCategory`(배지 건수) — **테스트가 직접 부른다** |

> **분류는 서버 목록이 아니라 코드 상수다.** `ESG_CATEGORY_OPTIONS`(`types.ts:33-37`)의 3종(환경·사회·지배구조)이 고정이며 카테고리 조회·생성·삭제 API 가 없다(FAQ 의 카테고리 관리 모달과 다른 점 — BE-022 §7.3).

## 2. 영역 구성

| 영역번호 | 이름 | 설명 |
|---|---|---|
| FS-022-SEC-01 | 목록 좌측 분류 필터 | '전체' + 환경·사회·지배구조 4항목 세로 나열 + 건수 배지 |
| FS-022-SEC-02 | 목록 툴바 | 우측 정렬 'ESG 활동 등록' 버튼 하나 (**검색 입력 없음**) |
| FS-022-SEC-03 | 목록 요약 · 일괄 작업 바 | 조회 요약 문구 + 선택 일괄 삭제 바(비표시 기본) |
| FS-022-SEC-04 | 목록 표 | 선택·순번·분류·제목·내용·일자·행 액션. 스켈레톤·빈 상태 포함 |
| FS-022-SEC-05 | 목록 조회 실패 배너(비표시 기본) | 표 전체를 대체 |
| FS-022-SEC-06 | 삭제 확인 다이얼로그(비표시 기본) | 단건 / 일괄 |
| FS-022-SEC-07 | 등록·수정 폼 | 하나의 폼이 등록/수정을 겸한다(`:id` 유무) |
| FS-022-SEC-08 | 폼 실패·충돌 표면(비표시 기본) | 상세 로드 실패 대체 화면 · 저장 실패 배너 · 충돌 다이얼로그 · 미저장 이탈 가드 |

## 3. 요소 명세

| 요소번호 | 영역 | 이름 | 유형 | 동작 | [서버] | 비고 |
|---|---|---|---|---|---|---|
| FS-022-EL-001 | FS-022-SEC-01 | 분류 필터 패널 | 카드 | `<nav aria-label="ESG 분류 필터">` 안에 제목 '분류' + 4항목 목록. 공유 `filterPanelStyle`/`filterNavStyle` 사용 | — | 좌측 고정 열 |
| FS-022-EL-001.1 | FS-022-SEC-01 | 분류 필터 항목 | 버튼 | '전체'·'환경'·'사회'·'지배구조' 4개. 선택 상태를 **`aria-pressed={active}`** 로 노출한다(앱 전체 단일 규약 — `aria-current` 를 쓰지 않는다). 클릭 시 그 분류로 좁히고 **행 선택을 전부 해제**한다 | — | 화면 로컬 상태 (URL 미반영 — §7 #1) |
| FS-022-EL-001.2 | FS-022-SEC-01 | 분류 건수 배지 | 텍스트 | 항목마다 건수(`formatNumber`, 천 단위 구분). '전체'=총 건수, 나머지=그 분류의 건수. **조회된 전체 목록 기준**이며 현재 선택된 분류에 영향받지 않는다 | — | `countEsgByCategory` |
| FS-022-EL-002 | FS-022-SEC-02 | 'ESG 활동 등록' 버튼 | 버튼 | `<PlusCircleIcon/>` + 'ESG 활동 등록'(primary). 우상단. 클릭 시 `/company/esg/new` 로 이동 | — | 권한과 무관하게 상시 렌더(§7 #2) |
| FS-022-EL-003 | FS-022-SEC-03 | 조회 상태 알림(비가시) | 텍스트 | 항상 마운트된 `aria-live="polite"` 영역. 최초 로드 중에는 침묵, 완료 후 'ESG 활동 N건을 찾았습니다.' / 0건이면 '조건에 맞는 ESG 활동 결과가 없습니다.' / 실패면 'ESG 활동 목록을 불러오지 못했습니다.' | — | 시각적 비표시 |
| FS-022-EL-004 | FS-022-SEC-03 | 조회 요약 텍스트 | 텍스트 | 최초 로드 중 '불러오는 중…', 완료 후 `전체 N건`(**현재 분류로 좁힌 건수**). 재조회 중이면 ' · 새로고침 중…' 덧붙임(`aria-busy`). 선택 1건 이상이면 ' · N건 선택됨' 덧붙임 | — | — |
| FS-022-EL-005 | FS-022-SEC-03 | 선택 일괄 작업 바 | 배너 | 선택 1건 이상일 때만 렌더(`SelectionBar`). 'N건 선택됨' + '선택 해제' + 일괄 삭제 버튼 | — | 비표시 기본 |
| FS-022-EL-005.1 | FS-022-SEC-03 | 일괄 '삭제' 버튼 | 버튼 | 라벨 `선택 N건 삭제`(위험). 클릭 시 FS-022-EL-016 을 연다. 단건 삭제 진행 중이면 비활성 | O | 실제 호출은 FS-022-EL-016 |
| FS-022-EL-006 | FS-022-SEC-04 | ESG 목록 표 | 표 | `aria-busy`. 컬럼: 선택 · 순번 · 분류 · 제목 · 내용 · 일자 · 행 액션. **페이지네이션 없이 조회된 전량을 렌더한다**(§7 #3). caption 이 행 클릭 규칙을 설명 | O | 정렬 = 일자 내림차순 고정 |
| FS-022-EL-006.1 | FS-022-SEC-04 | 전체선택 체크박스(헤더) | 입력 | 접근 이름 '이 페이지의 ESG 활동 전체 선택'. **현재 보이는 행 전부**(분류 필터 적용 후) 선택/해제. 일부 선택은 indeterminate | — | 화면 로컬 |
| FS-022-EL-006.2 | FS-022-SEC-04 | 행 체크박스 | 입력 | 접근 이름 `<제목> 선택` | — | — |
| FS-022-EL-006.3 | FS-022-SEC-04 | 순번 셀 | 텍스트 | `행index + 1`. 페이지네이션이 없어 offset 보정이 없다 | — | — |
| FS-022-EL-006.4 | FS-022-SEC-04 | 분류 셀 | 배지 | `StatusBadge`. 환경=success '환경' · 사회=info '사회' · 지배구조=neutral '지배구조'. 줄바꿈 방지 | — | `esgCategoryTone`·`esgCategoryLabel` |
| FS-022-EL-006.5 | FS-022-SEC-04 | 제목 셀 | 텍스트 | `item.title` 원문. **truncate 없음** — 길면 열이 넓어진다(§7 #4) | — | — |
| FS-022-EL-006.6 | FS-022-SEC-04 | 내용 셀 | 텍스트 | `item.summary` 를 최대폭에서 1줄 ellipsis 로 자른다. **hover/expand 로 전문을 보는 수단이 없다**(§7 #4) | — | — |
| FS-022-EL-006.7 | FS-022-SEC-04 | 일자 셀 | 텍스트 | `item.date` 원문('YYYY-MM-DD'). 줄바꿈 방지. **`shared/format` 을 경유하지 않는다**(§7 #5) | — | — |
| FS-022-EL-006.8 | FS-022-SEC-04 | 행 액션(수정·삭제) | 버튼 | `RowActions`, 접근 이름 `<제목>`. 수정 → `/company/esg/<id>/edit`. 삭제 → FS-022-EL-015 를 연다. 그 행 삭제 진행 중 비활성 | O | 키보드 진입 경로를 겸한다 |
| FS-022-EL-006.9 | FS-022-SEC-04 | 행 전체 클릭 이동 | 텍스트 | 행 빈 영역 클릭 시 `/company/esg/<id>/edit` 로 이동(**수정 폼** — 상세 화면이 없다). 체크박스·버튼·링크·입력은 자기 동작만 수행. 텍스트를 드래그 선택 중이면 이동하지 않는다. **마우스 전용 보조 수단** — 키보드 경로는 FS-022-EL-006.8 | — | 비표시 규칙 |
| FS-022-EL-007 | FS-022-SEC-04 | 목록 로딩 스켈레톤 | 스켈레톤 | **최초 로드에서만**(`data === undefined`) 표 본문을 5행 스켈레톤으로 대체. 재조회 중에는 이전 행을 유지한다 | — | 비표시. 행 수 5 고정(§7 #6) |
| FS-022-EL-008 | FS-022-SEC-04 | 빈 상태 | 빈상태 | 조회 완료·0행. 분류가 '전체'가 아니면 '필터에 맞는 ESG 활동이 없습니다' + **'필터 초기화'** 버튼(분류를 '전체'로 되돌린다). '전체'인데 0건이면 '등록된 ESG 활동이 없습니다'. 조사(이/가)는 `Empty` 가 자동 선택 | — | 비표시. 검색 축이 없어 '검색 지우기' 는 나타나지 않는다 |
| FS-022-EL-009 | FS-022-SEC-05 | 목록 조회 실패 배너 | 배너 | 조회 실패 시 **요약·일괄바·표 대신** 위험 톤 Alert 'ESG 활동 목록을 불러오지 못했습니다.' + '다시 시도'(`refetch`). 자동 소멸 안 함 | O | 툴바(등록 버튼)는 남는다 |
| FS-022-EL-010 | FS-022-SEC-07 | 폼 '목록으로' 백링크 | 버튼 | `<ChevronLeftIcon/>` + '목록으로'. 좌상단. 클릭 시 `/company/esg` 로 이동. **미저장 변경이 있어도 가로채지 않는다**(§7 #7) | — | — |
| FS-022-EL-011 | FS-022-SEC-07 | 폼 페이지 제목 | 텍스트 | `<h1>` 등록 'ESG 활동 등록' / 수정 'ESG 활동 수정' + 설명 '별표(*) 항목은 필수입니다. 분류(환경/사회/지배구조)와 활동 내용을 입력하세요.' | — | ⚠ AppHeader 도 동시에 `<h1>` 을 그린다(§7 #8) |
| FS-022-EL-012 | FS-022-SEC-07 | 폼 카드 제목 | 텍스트 | `CardTitle` 'ESG 활동 정보' | — | 정적 |
| FS-022-EL-012.1 | FS-022-SEC-07 | 분류 select | 입력 | `SelectField` '분류'(필수). `ESG_CATEGORY_OPTIONS` 3개(환경·사회·지배구조). **'선택하세요' 빈 옵션이 없다** — 기본값 '환경' | — | 저장 중·로딩 중 비활성 |
| FS-022-EL-012.2 | FS-022-SEC-07 | 일자 입력 | 입력 | `<input type="date">` '일자'(필수). 브라우저 기본 date picker | — | 분류와 2열 그리드(좁으면 세로) |
| FS-022-EL-012.3 | FS-022-SEC-07 | 제목 입력 | 입력 | `FormField` '제목'(필수). `maxLength=120`, placeholder '예: 사옥 전력 재생에너지 전환' | — | — |
| FS-022-EL-012.4 | FS-022-SEC-07 | 내용 textarea | 입력 | `TextareaField` '내용'(필수). `rows=6`, `maxLength=1000`, placeholder '활동 내용을 입력하세요.'. RHF `setValue(shouldValidate: false, shouldDirty: true)` — **입력 중 재검증하지 않는다** | — | `aria-invalid`·`aria-describedby` 를 DS 가 자동 배선 |
| FS-022-EL-012.5 | FS-022-SEC-07 | 본문 이미지 갤러리 | 입력 | `ImageGalleryField` '본문 이미지'(선택). hint '활동을 보여줄 이미지를 여러 장 올릴 수 있습니다. 최대 10장.'. 클릭·드래그드롭으로 추가, 타일 ×로 제거. 카운터 'N/10' | — | ⚠ **업로드 심이 없다** — FS-022-EL-012.6 |
| FS-022-EL-012.6 | FS-022-SEC-07 | 이미지 업로드 규칙 | 텍스트 | 선택한 파일을 **서버로 보내지 않는다**. `URL.createObjectURL(file)` 로 만든 **`blob:` URL** 을 값으로 넣고 그것이 그대로 저장된다. 갤러리 언마운트 시 그 URL 들이 `revokeObjectURL` 되므로 **저장한 이미지는 폼을 떠나는 순간 죽는다**(§7 #9) | — | 비표시 규칙. **결함** |
| FS-022-EL-013 | FS-022-SEC-07 | 폼 푸터 | 레이아웃 | 우측 정렬. secondary '취소'(→ `/company/esg`, 저장 중 비활성) + primary 제출 버튼 | — | 카드 안 |
| FS-022-EL-013.1 | FS-022-SEC-07 | 제출 버튼 | 버튼 | 라벨 '저장 중…' / '저장'(수정) / '등록'. 저장 중·상세 로딩 중 비활성. 성공 시 토스트 'ESG 활동을 등록했습니다.' / '…저장했습니다.' + `/company/esg` 로 **replace** 이동 | O | **동기 제출 잠금 + 제출 시도 단위 멱등키**로 중복 제출을 막는다 |
| FS-022-EL-014 | FS-022-SEC-08 | 상세 로드 실패 대체 화면 | 배너 | 수정 진입 시 상세 조회 실패면 **폼 대신** 위험 톤 Alert. **404 와 그 외를 가른다**: 404 → 'ESG 활동을(/를) 찾을 수 없습니다. 이미 삭제되었을 수 있습니다.' + '목록으로'만(재시도 미제공) / 그 외 → 'ESG 활동을(/를) 불러오지 못했습니다.' + '다시 시도' + '목록으로' | O | 비표시 |
| FS-022-EL-014.1 | FS-022-SEC-08 | 폼 로딩 스켈레톤 | 스켈레톤 | 수정 진입 시 상세 도착 전 입력 대신 `aria-busy` + 막대 4개. 등록 모드에서는 나타나지 않는다 | — | 비표시 |
| FS-022-EL-014.2 | FS-022-SEC-08 | 저장 실패 배너 | 배너 | 저장 실패 시 카드 상단 위험 톤 Alert '저장하지 못했습니다. 잠시 후 다시 시도해 주세요.' + **오류 참조 코드**(있으면) '오류 코드 TDS-…'(복사 가능) | O | 비표시. 422 는 여기 오지 않는다 |
| FS-022-EL-014.3 | FS-022-SEC-08 | 서버 필드 거절(422) 처리 | 텍스트 | 서버가 필드를 지목하면 폼 배너가 아니라 **그 입력의 인라인 에러**로 꽂고 첫 위반 필드로 포커스를 옮긴다 | O | 비표시 규칙 |
| FS-022-EL-014.4 | FS-022-SEC-08 | 충돌 다이얼로그(409/412) | 모달 | 저장이 409/412 면 **덮어쓰지 않는다**. 제목 '다른 사용자가 먼저 변경했습니다', 본문에 서버 문구 + '최신 내용을 불러오면 지금 입력한 내용은 사라집니다…'. '최신 내용 불러오기'(위험 — 상세 재조회로 폼을 덮는다) / '이어서 편집'(다이얼로그만 닫고 입력 유지, 취소 토스트 없음). **성공 토스트·목록 이동 없음** | O | 비표시. 초기 포커스는 '이어서 편집' |
| FS-022-EL-014.5 | FS-022-SEC-08 | 미저장 이탈 가드 | 모달 | 변경 있고 저장 중 아닐 때 이탈 3경로(탭 닫기·앱 내 링크·뒤로가기)를 가로챈다. 문구 'ESG 활동에 저장하지 않은 변경 사항이 있습니다. 이 화면을 벗어나면 입력한 내용이 사라집니다.'. 저장 성공 즉시 해제 | — | 비표시. **'취소'·'목록으로' 버튼은 가로채지 못한다**(§7 #7) |
| FS-022-EL-014.6 | FS-022-SEC-08 | 언마운트 abort | 텍스트 | 화면을 떠나면 진행 중이던 저장 요청을 abort 한다. abort 는 실패로 표시하지 않는다 | O | 비표시 규칙 |
| FS-022-EL-015 | FS-022-SEC-06 | 단건 삭제 확인 다이얼로그 | 모달 | 제목 'ESG 활동 삭제', 본문 `'<제목>'을(/를) 삭제합니다. 이 작업은 되돌릴 수 없습니다.`, 확인 '삭제'. 진행 중 확인 버튼 잠금. 성공 시 닫고 토스트 `'<제목>'을(/를) 삭제했습니다.`. 실패 시 **다이얼로그를 열어 둔 채** 안 배너 '삭제하지 못했습니다. 잠시 후 다시 시도해 주세요.'(재클릭이 retry). 취소·Esc·딤 클릭 시 요청 abort + 상태 리셋 | O | 초기 포커스 = 취소 |
| FS-022-EL-016 | FS-022-SEC-06 | 일괄 삭제 확인 다이얼로그 | 모달 | 제목 'ESG 활동 일괄 삭제', 본문 `선택한 ESG 활동 N건을 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.`, 확인 `N건 삭제`. 전원 성공: 닫고 선택 해제 + 토스트 'ESG 활동 N건을 삭제했습니다.'. **부분 실패: 닫지 않고** 안 배너 `N건 중 M건을 삭제하지 못했습니다. 잠시 후 다시 시도해 주세요.`, 선택 유지 | O | `settleAll` — 항목마다 독립 요청 |
| FS-022-EL-017 | FS-022-SEC-04 | 필터 변경 시 선택 해제 | 텍스트 | 분류가 바뀌면 행 선택을 전부 해제한다 — 보이지 않는 행이 선택된 채 남지 않게 | — | 비표시 규칙 |

## 4. 예외 명세

| 요소번호 | 빈 상태 | 로딩 | 실패 | 유효성 | 권한없음 | 경합 | 대량 |
|---|---|---|---|---|---|---|---|
| FS-022-EL-001 | N/A — 분류 3종이 코드 상수라 항상 4항목 | 도착 전에도 패널은 렌더된다(배지만 0) | 조회 실패 시에도 패널은 남는다(배지 0) — 필터를 눌러도 재조회하지 않는다(화면 로컬 필터) | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | N/A — 서버 상태 아님 | 항목 4개 고정 |
| FS-022-EL-001.1 | N/A — 항상 4개 | 조회 중에도 조작 가능(로컬 필터라 즉시 반영) | 조회 실패 시 대상 데이터가 없어 눌러도 표가 나타나지 않는다 | N/A — 버튼 | §4.1 공통 규칙 적용 | 화면 로컬 상태 — 다른 탭·관리자와 무관 | N/A — 4개 |
| FS-022-EL-001.2 | 해당 분류 0건이면 '0' | 도착 전 전부 '0'(**'—' 가 아니다** — 0건과 미도착을 구분하지 못한다, §7 #10) | 조회 실패 시 전부 '0' 유지 — 같은 이유로 실패와 0건이 배지에서 같아 보인다 | N/A — 표시 전용 | §4.1 공통 규칙 적용 | 재조회 시점에만 갱신 | 천 단위 구분 |
| FS-022-EL-002 | N/A — 항상 표시 | 조회 중에도 활성 | N/A — 라우팅만, 서버 호출 없음 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — **권한이 없어도 보이고 눌린다**(§7 #2) | N/A — 저장 상태 없음 | N/A — 단일 버튼 |
| FS-022-EL-003 | 0행이면 '조건에 맞는 ESG 활동 결과가 없습니다.' | **최초 로드 중에는 침묵한다** — 아직 알릴 사실이 없다 | 실패면 'ESG 활동 목록을 불러오지 못했습니다.' | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 재조회·필터 변경 시 새 문장이 주입돼 announce 된다 | 건수만 바뀐다 |
| FS-022-EL-004 | 0건이면 '전체 0건' | 최초 로드 '불러오는 중…'. **재조회 중에는 건수를 지우지 않고** ' · 새로고침 중…' 만 덧붙인다 | 조회 실패 시 FS-022-EL-009 가 이 줄까지 대체한다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 재조회 시점에만 갱신 | 천 단위 구분 |
| FS-022-EL-005 | 선택 0건이면 미렌더 | N/A — 표시 전용 | N/A — 이 바 자체는 서버를 호출하지 않는다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 분류 변경 시 해제(FS-022-EL-017) | 선택 상한 없음 — 전량 선택 가능(§7 #11) |
| FS-022-EL-005.1 | 바가 없으면 이 버튼도 없다 | 단건 삭제 진행 중 비활성 | N/A — 다이얼로그만 연다. 실패 처리는 FS-022-EL-016 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 진행 중 재클릭 차단 | 선택 전원 대상 — **건수 임계 경고·진행률·취소 없음**(§7 #11) |
| FS-022-EL-006 | 0행이면 FS-022-EL-008 로 본문 대체 | FS-022-EL-007 스켈레톤 + `aria-busy`. **재조회 중에는 이전 행 유지** | FS-022-EL-009 로 표 전체 대체 | N/A — 표 자체 입력 없음 | §4.1 공통 규칙 적용 | 다른 관리자가 지운 행을 열면 FS-022-EL-014 의 404 화면 | **페이지네이션 없음** — 조회된 전량을 DOM 에 그린다(§7 #3) |
| FS-022-EL-006.1 | 0행이면 미체크·미indeterminate | 조회 중 본문이 스켈레톤이라 선택 대상 없음 | 조회 실패 시 표 대체로 미표시 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 분류 변경 시 해제 | **현재 보이는 행 전부**를 한 번에 선택한다 — 100건이면 100건(§7 #11) |
| FS-022-EL-006.2 | 행 없으면 없음 | 조회 중 스켈레톤 | 조회 실패 시 미표시 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 화면 로컬 상태 | **Shift-click 범위 선택 없음** — 한 건씩 눌러야 한다(§7 #11) |
| FS-022-EL-006.3 | 행 없으면 없음 | 조회 중 스켈레톤 | 조회 실패 시 미표시 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 삭제·필터로 다시 매겨진다 | 페이지 offset 보정이 없다(페이지네이션 부재) |
| FS-022-EL-006.4 | 행 없으면 없음 | 조회 중 스켈레톤 | 조회 실패 시 미표시 | 세 값 밖의 분류는 타입상 불가능. 알 수 없는 값이 오면 라벨을 원문 그대로 표시하고 tone 은 neutral | §4.1 공통 규칙 적용 | 조회 시점 값 | nowrap |
| FS-022-EL-006.5 | 행 없으면 없음 | 조회 중 스켈레톤 | 조회 실패 시 미표시 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 조회 시점 값 | **truncate 없음** — 120자 제목이 열을 밀어 표 폭을 바꾼다(§7 #4) |
| FS-022-EL-006.6 | 행 없으면 없음 | 조회 중 스켈레톤 | 조회 실패 시 미표시 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 조회 시점 값 | 최대폭 초과 시 1줄 ellipsis. **전문 확인 수단이 행 클릭(수정 폼 진입)뿐**(§7 #4) |
| FS-022-EL-006.7 | 행 없으면 없음 | 조회 중 스켈레톤 | 조회 실패 시 미표시 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 조회 시점 값 | 'YYYY-MM-DD' 원문 — TZ·로케일 변환 없음(§7 #5) |
| FS-022-EL-006.8 | 행 없으면 없음 | 그 행 삭제 진행 중 비활성 | N/A — 다이얼로그/라우팅만 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — **권한이 없어도 보이고 눌린다**(§7 #2) | 진행 중 그 행 잠금 | 행 단위 |
| FS-022-EL-006.9 | 행 없으면 없음 | 조회 중 스켈레톤 행이라 이동 규칙이 걸리지 않는다 | N/A — 라우터 내부 이동 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 이미 삭제된 항목이면 수정 폼에서 404 화면(FS-022-EL-014) | N/A — 행 단위 |
| FS-022-EL-007 | N/A — 도착 전 상태 | 이것이 로딩 표현. 5행 × (열수+3) 막대 | 조회 실패 시 FS-022-EL-009 로 바뀐다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | **재조회 때는 뜨지 않는다** — 이전 행이 남는다 | 행 수 5 고정 — 실제 행 수와 무관(§7 #6) |
| FS-022-EL-008 | 이것이 빈 상태 표현. 필터 유무로 문구·복구 액션이 갈린다 | 재조회 시 이전 행이 남으므로 빈 상태가 번쩍이지 않는다 | 조회 실패 시 FS-022-EL-009 로(빈 상태로 폴백하지 않는다) | N/A — 입력 없음 | §4.1 공통 규칙 적용 | N/A — 표시 전용 | N/A — 1행 |
| FS-022-EL-009 | N/A — 실패 상태 | 재시도 시 배너가 사라지고 스켈레톤으로 | 이것이 실패 표현. 문구 1종. 복구 '다시 시도'. **403/404/500 을 구분하지 않는다**(§7 #12) | N/A — 입력 없음 | §4.1 공통 규칙 적용 — 권한 부족도 이 배너 | 재시도는 목록 재조회 | N/A — 표시 전용 |
| FS-022-EL-010 | N/A — 항상 표시 | N/A — 라우팅만 | N/A — 서버 호출 없음 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | **미저장 변경이 있어도 확인 없이 이동한다**(§7 #7) | N/A — 단일 버튼 |
| FS-022-EL-011 | N/A — 정적 헤더 | N/A — 조회 안 함 | N/A — 서버 호출 없음 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | N/A — 저장 상태 없음 | 고정 문구 |
| FS-022-EL-012 | N/A — 정적 제목 | 로딩 중에도 제목은 남는다(스켈레톤은 본문만) | 로드 실패 시 카드째 FS-022-EL-014 로 대체 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | N/A — 저장 상태 없음 | 고정 문구 |
| FS-022-EL-012.1 | 등록 모드 기본값 '환경'. **빈 옵션이 없어 '미선택' 상태가 존재하지 않는다** | 저장 중·로딩 중 비활성 | 저장 실패는 FS-022-EL-014.2 | 세 값이 아니면 '분류를 선택하세요.'. **폼으로는 위반이 불가능**(select 옵션이 3개뿐) — 서버 422 나 손상된 데이터에서만 발화한다 | §4.1 공통 규칙 적용 | 조회 시점 값 | 선택지 3개 고정 |
| FS-022-EL-012.2 | 등록 모드 빈 값 | 저장 중·로딩 중 비활성 | 저장 실패는 FS-022-EL-014.2 | 빈값 '일자를 입력하세요.' / `YYYY-MM-DD` 형식이 아니면 '일자 형식이 올바르지 않습니다.'. **미래 일자·범위 제약 없음**(§7 #13) | §4.1 공통 규칙 적용 | 조회 시점 값 | `type=date` 라 브라우저가 형식을 강제 |
| FS-022-EL-012.3 | 등록 모드 빈 문자열 + placeholder | 저장 중·로딩 중 비활성 | 저장 실패는 FS-022-EL-014.2 | 제출 시 검증. 빈값 '제목을 입력하세요.' / 120자 초과 '제목은 120자를 넘을 수 없습니다.' | §4.1 공통 규칙 적용 | 다른 관리자의 동일 제목 등록과 충돌 검사 없음 | 120자 상한(`maxLength`). **카운터 없음**(§7 #14) |
| FS-022-EL-012.4 | 등록 모드 빈 문자열 + placeholder | 저장 중·로딩 중 비활성 | 저장 실패는 FS-022-EL-014.2 | 제출 시에만 검증(`shouldValidate: false`). 공백만 '내용을 입력하세요.' / 1000자 초과 '내용은 1000자를 넘을 수 없습니다.' | §4.1 공통 규칙 적용 | 충돌 감지 없음 | 1000자 상한. **카운터 없음**(§7 #14) |
| FS-022-EL-012.5 | 등록 모드 빈 배열 → 드롭존 1개 | 저장 중·로딩 중 비활성(타일 ×도 잠긴다) | **업로드 실패 개념이 없다** — 서버로 보내지 않기 때문(FS-022-EL-012.6) | 파일마다 type(`image/*`)·크기(5MB) 검증 후 위반 파일만 거절하고 사유를 인라인 표시. 10장 초과 시 '이미지는 최대 10장까지 등록할 수 있습니다.'. 폼 제출 시 `esgSchema` 가 배열 길이를 재확인 | §4.1 공통 규칙 적용 | 다른 관리자의 이미지 변경과 충돌 검사 없음 | 10장 상한. **진행률·취소 경로 없음**(업로드가 없으므로) |
| FS-022-EL-012.6 | N/A — 규칙 | N/A — 서버 호출 없음 | **결함**: 저장된 `blob:` URL 이 폼 언마운트와 함께 죽는다 — 목록으로 돌아가면 이미 무효다 | N/A — 규칙 | §4.1 공통 규칙 적용 | N/A | 장수와 무관하게 전부 무효화된다(§7 #9) |
| FS-022-EL-013 | N/A — 항상 표시 | 저장 중 '취소' 비활성 | N/A — 라우팅만 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | '취소' 는 미저장 변경을 확인 없이 버린다(§7 #7) | N/A |
| FS-022-EL-013.1 | N/A — 항상 표시 | 요청 중 '저장 중…' + 비활성. 상세 로딩 중에도 비활성 | 실패 시 FS-022-EL-014.2 배너, 버튼 재활성, 이동 없음, 입력 유지 | 제출 시 `esgSchema` 검증. 위반이면 서버 미호출 + 첫 위반 필드로 포커스 이동 | §4.1 공통 규칙 적용 — 권한 부족은 저장 실패로만 나타난다(§7 #2) | **동기 잠금**이 두 번째 제출을 즉시 멈춘다. **멱등키**를 제출 시도 단위로 잡아 재클릭이 같은 키를 재사용한다(성공하면 폐기). 다른 관리자가 먼저 지웠으면 409 → FS-022-EL-014.4 | 단건 저장 |
| FS-022-EL-014 | N/A — 수정 모드에서만 | 재시도 시 FS-022-EL-014.1 로 | 이것이 로드 실패 표현. **404 는 '다시 시도' 를 주지 않는다**(재시도해도 영원히 없다) | N/A — 표시 규칙 | §4.1 공통 규칙 적용 — 서버가 404 로 은닉하면 '찾을 수 없습니다' 로 보인다 | 다른 관리자가 지운 항목을 열면 여기 도달한다 | 단건 로드 |
| FS-022-EL-014.1 | N/A — 도착 전 상태 | 이것이 로딩 표현. 막대 4개 | 로드 실패 시 FS-022-EL-014 로 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 도착하면 폼을 그 값으로 `reset` — 편집 중 원격 변경은 반영하지 않는다 | 막대 4 고정 |
| FS-022-EL-014.2 | N/A — 오류 없으면 미렌더 | 재제출 시 기존 문구·참조 코드를 먼저 지운다 | 이것이 generic 저장 실패 표현. 참조 코드가 있으면 함께. **raw 응답·스택·status 를 문장으로 쓰지 않는다** | 필드 단위 거절은 여기 오지 않는다 — FS-022-EL-014.3 | §4.1 공통 규칙 적용 — 403 도 이 배너(§7 #12) | 409 는 여기 오지 않는다 — FS-022-EL-014.4 | 1건만 표시 |
| FS-022-EL-014.3 | N/A — 서버가 필드를 지목해야 성립 | N/A — 표시 규칙 | 이 경로에서는 폼 배너를 띄우지 않는다 | 서버 문구를 그 필드의 인라인 에러 자리(클라이언트 zod 에러와 같은 자리)에 꽂는다 | §4.1 공통 규칙 적용 | N/A | 위반 수만큼 필드에 꽂고 **첫 필드로만** 포커스 |
| FS-022-EL-014.4 | N/A — 충돌이 있어야 열린다 | busy 없음(즉시 판단) | 이것이 충돌 표현. 재시도 개념 없음 — 두 선택지로 갈린다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 이것이 경합 표현. '이어서 편집' 후 재제출하면 다시 409 일 수 있다(그때 다시 열린다) | 단건 |
| FS-022-EL-014.5 | N/A — 변경이 있어야 성립 | N/A — 서버 호출 없음 | N/A — 서버 호출 없음 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 저장 중에는 비활성(`isDirty && !saving`) | N/A — 표시 전용 |
| FS-022-EL-014.6 | N/A — 진행 요청이 있어야 성립 | N/A — 취소 규칙 | abort 는 실패가 아니다 — 배너·토스트 없음 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 성공 콜백도 `signal.aborted` 면 토스트·이동을 하지 않는다 | N/A — 단건 |
| FS-022-EL-015 | N/A — 대상이 있어야 열린다 | 요청 중 확인 버튼 잠금(+`aria-busy`). 취소 시 abort + `mutation.reset` | 실패 시 **닫지 않고** 안 배너. 복구 = 재클릭 또는 취소 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — 권한 부족도 이 배너 | **이미 삭제된 항목이면 409('이미 삭제된 항목입니다.')** → 실패 배너로 보인다(멱등 처리하지 않는다 — §7 #15) | 단건 |
| FS-022-EL-016 | N/A — 선택이 있어야 열린다 | 요청 중 확인 버튼 잠금. 취소 시 abort | **부분 실패 시 닫지 않고** 'N건 중 M건을…' 배너, 선택 유지. 전원 성공일 때만 목록 무효화 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — 권한 부족은 실패 건수에 포함 | 항목마다 독립 요청 — 한 건 실패가 나머지를 중단시키지 않는다. **abort 는 실패로 세지 않는다** | 선택 전원 병렬. **재시도는 전량 재실행** — 성공분을 다시 요청한다(§7 #16) |
| FS-022-EL-017 | N/A — 선택이 있어야 성립 | N/A — 서버 호출 없음 | N/A — 서버 호출 없음 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 화면 로컬 상태만 비운다 | 선택 전량 해제 |

### 4.1 공통 예외 규칙

| 규칙 | 내용 |
|---|---|
| 네트워크 단절 | 목록 조회 실패는 FS-022-EL-009 배너가 표를 대체한다. 상세 로드 실패는 FS-022-EL-014, 저장 실패는 FS-022-EL-014.2, 삭제 실패는 다이얼로그 안 배너. **오프라인 감지·전용 문구는 없다** |
| 세션 만료 | 조회·쓰기가 401 이면 앱 전역 인터셉터가 세션을 폐기하고 `/login?returnUrl=<현재경로>&reason=session_expired` 로 보낸다. **편집 중인 폼 입력은 보존되지 않는다** |
| 요청 타임아웃 | 프론트 타임아웃 상한 없음. abort 는 화면 이탈·다이얼로그 닫기에서만 발생한다 |
| 중복 제출 | 폼 제출은 **동기 잠금 + `disabled`** 두 겹으로 막는다(검증이 비동기라 `disabled` 만으로는 틈이 남기 때문). 검증 실패 시 잠금이 풀린다. 삭제·일괄 삭제는 진행 중 확인 버튼 비활성 |
| 실패 통지의 자리 | ① 조회 실패는 표를 대체하는 인라인 배너 ② 저장 실패는 폼 카드 안 배너(+참조 코드) ③ 필드 거절(422)은 그 입력의 인라인 에러 ④ 충돌(409/412)은 전용 다이얼로그 ⑤ 삭제 실패는 **그 다이얼로그 안** 배너(모달 뒤에 숨는 토스트를 쓰지 않는다) ⑥ 삭제·저장 **성공**은 토스트 ⑦ abort 는 아무 통지도 하지 않는다 |
| 낙관적 업데이트 | 없다. 등록·수정·삭제 전부 비관적 — 확인 + busy 잠금 후 서버 응답으로만 반영한다(비가역 액션에 optimism 을 쓰지 않는다) |
| 동시 조회 | 목록·상세 조회는 각각 동시에 1건만 유지된다(react-query, `staleTime` 30초 · 자동 재시도 없음 · 창 포커스 재조회 없음). 목록은 `placeholderData: (previous) => previous` 라 재조회 중 이전 데이터를 유지한다 |
| 권한 없음 | 역할 기반 분기 없음. 이 라우트들의 **읽기** 권한이 없으면 앱 전역 가드가 403 화면을 그린다. **쓰기 권한은 화면에서 게이팅하지 않는다** — 등록·수정·삭제 버튼이 그대로 보이고 눌리며 서버 403 이 각 요소의 실패로 떨어진다(§7 #2) |
| 검증 시점 | 제출 시에만 검증한다. 내용(textarea)은 `shouldValidate: false` 로 명시적으로 입력 중 재검증을 끈다 |
| 필터의 수명 | 분류 필터는 **화면 로컬 상태**다. 폼으로 갔다 돌아오거나 새로고침하면 '전체'로 초기화된다 — URL 에 없다(§7 #1) |
| 필터 적용 지점 | 필터·정렬이 **클라이언트에서** 일어난다: 어댑터가 전량을 돌려주고(`fetchAll`), `sortEsg` 로 정렬된 목록을 `filterEsg` 가 좁힌다. 필터를 바꿔도 재조회하지 않는다 |
| 저장 후 이동 | 등록·수정 성공은 토스트 + `/company/esg` 로 **replace** 이동한다(뒤로가기로 폼에 되돌아오지 않는다). 상세 화면이 없어 수정 후에도 목록으로 간다 |

## 5. 서버 연동 지점

| 요소번호 | 이름 | 읽기/쓰기 | 필요 데이터 | 프론트 어댑터 (data-source.ts) | 비고 |
|---|---|---|---|---|---|
| FS-022-EL-001.2 / EL-003 / EL-004 / EL-006 / EL-007 / EL-008 / EL-009 | ESG 목록 조회 | R | ESG 활동 전량(id·분류·제목·내용·일자·이미지 URL 배열) | `esgAdapter.fetchAll(signal)` (`useCrudList` → `useCrudListQuery`) | 캐시 키 `['esg','list']`. **필터·정렬·페이징 파라미터가 없다** — 전량을 받아 클라이언트가 좁힌다 |
| FS-022-EL-014 / EL-014.1 | ESG 상세 조회 | R | 그 활동 1건 전문 | `esgAdapter.fetchOne(id, signal)` (`useCrudItem`) | 캐시 키 `['esg','detail',id]`. 없으면 404 로 던진다 |
| FS-022-EL-013.1 (등록) | ESG 등록 | W | 분류·제목·내용·일자·이미지 URL 배열 | `esgAdapter.create(input, signal)` | 성공 후 목록 키 무효화 |
| FS-022-EL-013.1 (수정) | ESG 수정 | W | id + 위 입력 | `esgAdapter.update(id, input, signal)` | 성공 후 목록·상세 키 무효화. 대상이 없으면 409 |
| FS-022-EL-006.8 / EL-015 | ESG 삭제(단건) | W | id | `esgAdapter.remove(id, signal)` | 성공 후 목록 키 무효화. 이미 없으면 409 |
| FS-022-EL-005.1 / EL-016 | ESG 삭제(일괄) | W | 선택된 id 배열 | `esgAdapter.remove(id, signal)` × N (`settleAll`) | 전원 성공일 때만 목록 무효화 |
| FS-022-EL-012.5 / EL-012.6 | 이미지 업로드 | W | 이미지 파일 | **없음 — 심 없음(미정)** | `URL.createObjectURL` 로 `blob:` URL 을 만들 뿐 서버로 보내지 않는다. §7 #9 · BE-022 §4 EP-06 |

> **현재 구현 상태 (A63 참고)**: `esgAdapter` 는 `createCrudAdapter<EsgItem, EsgInput>({ scope: 'esg', seed: ESG_SEED, ... })` 로 만든 **브라우저 안 mutable 픽스처**다(`data-source.ts:49-58`). 다섯 함수 전부 400ms 지연 후 메모리 배열을 읽고 쓴다 — 실제 네트워크 호출은 없다. `data-source.ts:48` 의 `// TODO(backend): GET/POST /api/company/esg · GET/PUT/DELETE /api/company/esg/:id` 주석이 유일한 연동 지점이며, 위 표는 백엔드 연결 후 의도된 동작이다. **저장한 값은 새로고침하면 시드로 돌아간다**(영속 없음). 어댑터는 안전 기본값을 지킨다: `update`·`remove` 가 **없는 id 를 조용히 통과시키지 않고 409 로 던진다**(`crud.ts:71-74,82-85` — 유령 저장 방지). **이미지 업로드는 어떤 형태로도 존재하지 않는다.**

## 6. 자기 점검 (제출 전 확인)

- [x] 구현 소스를 전수 대조했다 — `EsgListPage.tsx` · `EsgFormPage.tsx` · `EsgCategoryFilter.tsx` · `data-source.ts` · `types.ts` · `validation.ts` · `esg.test.ts` + 소비하는 공용 모듈(`useCrudList` · `CrudListShell` · `CrudTable` · `useCrudForm` · `FormPageShell` · `FormFeedback` · `crud.ts` · `Empty` · `ImageGalleryField`)
- [x] 보이지 않는 요소(스켈레톤·빈 상태·실패 배너·live region·행 클릭 규칙·필터 변경 시 선택 해제·언마운트 abort·업로드 규칙·422 처리·409 충돌)에 번호를 줬다
- [x] §4 예외 7축 빈칸 0건. 모든 `N/A` 에 사유
- [x] `[서버]` = O 요소가 §5 에 전부 요약됐다 (조회 2 · 쓰기 3 + 심 없는 업로드 1건 명시)
- [x] 엔드포인트·HTTP·에러코드·DB 스키마를 쓰지 않았다 (BE-022 영역). §4 의 '409'·'422' 표기는 화면 분기의 근거로만 등장하며 계약 정의는 BE-022 가 한다
- [x] **없는 것을 지어내지 않았다** — 상세 조회 화면·검색 입력·페이지네이션·카테고리 관리·이미지 업로드 심은 존재하지 않으며 그렇게 적었다
- [x] 검증 문구를 `validation.ts` 원문대로 옮겼다 — **조사는 fallback 리터럴이 아니라 `objectParticle`/`topicParticle` 파생이다**(`shared/crud/validation.ts:17,21,24` → `shared/format.ts:269+`) — 실제 렌더 문구다

## 7. 미결 사항 (A11 / A01 / A63 / A40 이관)

| # | 내용 | 이관 대상 |
|---|---|---|
| 1 | **분류 필터가 URL 에 없다** — `useState` 로만 산다. '사회'로 좁혀 행을 열고 '목록으로' 를 누르면 '전체'로 되돌아간다. F5·링크 공유로 필터 view 를 재현할 수 없다. 앱에 `useListState`(URL 직렬화)가 있으나 이 화면이 소비하지 않는다 | A41 · A11 |
| 2 | **쓰기 권한 게이팅이 없다** — 등록·수정·삭제·일괄삭제 버튼이 권한과 무관하게 렌더된다. **⚠ 범위 정정(F3b 이후)**: `useRouteWritePermissions` 소비자는 이제 **7곳**이다(`products/{categories,items,returns}` · `settings/{api-keys,languages,oauth,site}`) — **`pages/company/**` 만 그 목록에 없다**(`grep -rn "useRouteWritePermissions\|useRouteCan" pages/company/` → **0건**). '앱 전역 미구현'이 아니라 **이 섹션의 미적용**이며 배선 선례가 이미 앱 안에 있다(`settings/site/SiteSettingsPage`) | A11 change_request |
| 3 | **페이지네이션이 없다** — 어댑터가 전량을 돌려주고 화면이 전량을 DOM 에 그린다. ESG 활동이 쌓이면 표가 무한히 길어지고 '전체 선택'이 그 전부를 선택한다 | A11 · A40 |
| 4 | 제목 열에 truncate 가 없어 긴 제목이 표 폭을 민다. 내용 열은 ellipsis 지만 **전문을 보는 수단이 행 클릭(수정 폼 진입)뿐**이다 — 읽기 전용 상세 화면이 없다 | A11 change_request |
| 5 | 일자를 `shared/format` 을 거치지 않고 `item.date` 원문으로 렌더한다 — 앱의 다른 화면과 날짜 표기가 갈릴 수 있다 | A41 |
| 6 | 스켈레톤 행 수가 5 고정이다 — 페이지 크기·실제 행 수와 무관해 로딩 → 로드 완료에서 표 높이가 튄다 | A40 (`CrudTable`) |
| 7 | 폼의 **'취소'·'목록으로' 버튼이 미저장 가드를 통과한다** — 두 버튼 모두 `navigate(listPath)`(프로그램적 push)라 링크 가로채기(앵커 대상)에도, popstate sentinel 에도 걸리지 않는다. 반쯤 채운 폼이 확인 없이 사라진다 | A40 (`FormPageShell`) · A11 |
| 8 | **폼 화면에 `<h1>` 이 2개다** — AppHeader 가 `findNavLabel('/company/esg/new')` → branch 폴백으로 '기업 관리' 를 그리고, `FormPageShell` 이 'ESG 활동 등록' 을 그린다. 상위 제목이 화면을 지목하지 못한다. 목록 화면에는 in-content 제목이 없어 title 소스가 화면 타입마다 다르다 | A40 (`AppHeader`/`findNavLabel`) · A11 |
| 9 | **이미지가 저장되지 않는다** — 업로드 심이 없어 `blob:` URL 이 값으로 들어가고, 폼을 떠나면 `revokeObjectURL` 로 그 URL 이 죽는다. 목록으로 돌아온 직후부터 이미 무효이며 새로고침하면 더욱 그렇다. **업로드 엔드포인트·저장 형식·최대 용량이 전부 미정** | A63 (BE-022) · A11 · A01 |
| 10 | 분류 배지가 미도착·조회 실패·진짜 0건을 전부 '0' 으로 표시한다(FAQ 필터는 '—' 로 구분한다) | A11 change_request |
| 11 | 대량 작업 안전장치가 없다 — Shift-click 범위 선택 없음, '전체 선택'의 scope(현재 보이는 행)가 라벨에 드러나지 않음, 큰 건수 삭제에 강화 confirm·진행률·취소 없음 | A11 · A40 |
| 12 | 목록 조회 실패가 403/404/500 을 구분하지 않는다 — `CrudListShell` 이 `error !== null` 하나로 뭉갠다. 저장 실패 배너도 403 과 500 이 같은 문구다 | A40 (`CrudListShell`) · A11 |
| 13 | 일자에 범위 제약이 없다 — 미래 일자·1900년 이전을 막지 않는다. ESG **활동** 일자가 미래인 것이 정당한지 미정 | A63 (BE-022) · A11 |
| 14 | 제목(120자)·내용(1000자)에 글자 수 카운터가 없다 — 상한에 닿으면 예고 없이 입력이 멈춘다. `FormField`/`TextareaField` 가 `counter` 를 지원하나 넘기지 않는다 | A41 |
| 15 | 이미 삭제된 항목의 삭제가 409('이미 삭제된 항목입니다.')로 실패한다 — 다른 관리자가 먼저 지운 경우 운영자에게는 '삭제하지 못했습니다' 로 보인다. DELETE 를 멱등으로 볼지 결정 필요 | A63 (BE-022) |
| 16 | 일괄 삭제 부분 실패 후 재시도가 **전량을 재실행**한다 — 성공분을 다시 요청한다. 실패한 id 를 돌려주지 않아 '실패분만 재시도' 가 불가능하다 | A40 (`bulk.ts`/`useCrudList`) |
| ~~17~~ | **해소됨(통합) — 이관 취소.** `requiredText` 와 `esgSchema` 가 이제 josa 헬퍼를 쓴다 — 조사 헬퍼가 `shared/format.ts:269+` 로 승격되고 `shared/crud/validation.ts:17,21,24` 가 `objectParticle`/`topicParticle` 로 조립한다 → '제목을 입력하세요.' · 'ESG 활동을 등록했습니다.' `pages/company/` 의 사용자 대상 조사 리터럴 **0건**(ERP-13) | **이관 취소** |
| 18 | 대응 SCR 문서 부재 (기업 관리 섹션 공통 사안) | A11 / A01 |
