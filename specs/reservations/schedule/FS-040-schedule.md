---
id: FS-040
title: "예약 일정 (달력 · 일/주 뷰)"
screen: SCR-040               # ⚠ 예약/신청 SCR(D2) 미작성 — §7 미결 사항 참조
route: /reservations/schedule
owner: A62
reviewer: A64
gate: G9
status: draft
confirmedAt: 2026-07-17
version: 1.0
date: 2026-07-17
---

# FS-040. 예약 일정 (달력 · 일/주 뷰)

## 1. 화면 개요

| 항목 | 내용 |
|---|---|
| 목적 | 예약을 시간 격자(행=시간 슬롯 · 열=날짜)로 훑어 언제가 비었고 언제가 찼는지 한눈에 보고, 슬롯을 골라 그 시간대의 예약을 확인·수정으로 넘어간다 |
| 역할(주 사용자) | 관리자 (구현에 역할 분기 없음 — §4.1) |
| 진입 경로 | 좌측 GNB > 예약/신청 관리 > 예약 일정 (`/reservations/schedule`) |
| 포함 화면 | 달력 `/reservations/schedule` 하나 — 하위 라우트 없음 |
| 구현 경로 | `apps/admin/src/pages/reservations/schedule/{ScheduleCalendarPage.tsx,schedule-data.ts,components/CalendarGrid.tsx}` |
| 대응 SCR | SCR-040 (미작성 — §7) |
| 공통 컴포넌트 | `shared/crud/useCrudListQuery` · `shared/ui/{Alert,Button,Card,CardTitle,ChevronLeftIcon,ChevronRightIcon,StatusBadge,hintStyle}` · `shared/format.formatNumber` |
| 섹션 공용 모듈 | `_shared/reservation-store.ts`(**예약 관리와 같은 어댑터**) · `_shared/reservation.ts`(`reservationsInSlot`) · `_shared/calendar.ts`(`addDays`·`weekDates`·`formatDayLabel`·`isToday`·`toDateString`) · `_shared/booking.ts`(상태 라벨·톤) · `_shared/resources.ts`(수용량 계산) |
| 데이터 출처 | **전용 데이터 소스가 없다.** `ScheduleCalendarPage.tsx:116-121` 이 `useCrudListQuery('reservations', reservationAdapter)` 로 **예약 관리(FS-037)와 같은 쿼리 키(`['reservations','list']`)** 를 읽는다 — 이 화면은 예약 목록의 **다른 표현**이다 |

### 1.1 슬롯 · 수용량 모델

| 항목 | 정의 | 코드 |
|---|---|---|
| 영업시간 | 09:00–20:00 | `schedule-data.ts:13-14` (`OPEN_HOUR = 9` · `CLOSE_HOUR = 20`) |
| 슬롯 | 1시간 단위 **11개**(09:00 … 19:00) | `schedule-data.ts:30-36` (`DAY_SLOTS`) |
| 열(일 뷰) | 1열 — `anchor` 하루 | `ScheduleCalendarPage.tsx:124` |
| 열(주 뷰) | 7열 — `anchor` 가 속한 주의 **월~일** | `_shared/calendar.ts:58-70` (`startOfWeek` 가 월요일 시작) |
| 셀에 세는 예약 | 그 날짜에 그 슬롯 `[startMin, endMin)` 과 **시간이 겹치는 유효 예약**. 취소·노쇼는 자리를 비우므로 제외 | `_shared/reservation.ts:135-149` (`reservationsInSlot`) |
| 수용량(capacity) | **자원의 개수**(`listResources().length` = 4) | `schedule-data.ts:39-41` (`slotCapacity`) |
| 마감(full) | `booked >= capacity` | `schedule-data.ts:56` |

> ⚠ **수용량 모델이 예약 도메인의 다른 모델과 어긋난다.** 여기서 수용량은 '**자원 개수**'(자리 4개)이고 세는 대상은 '**예약 건수**'다 — 즉 '자원 하나에 예약 하나'라는 배타 점유 모델이다. 그런데 같은 도메인의 `ReservationResource.capacity` 는 '**동시 수용 인원**'(세미나룸 A = 12명)이고 예약 폼은 그것으로 인원을 검증한다(FS-037-EL-023). **한 슬롯의 예약 4건이 전부 같은 방(room-b)에 몰려 있어도 '마감'으로 표시되고, 나머지 세 자원은 비어 있는데도 그렇다** — `schedule.test.ts:51-63` 이 그 동작을 그대로 고정한다(같은 `resourceId` 로 capacity 건을 만들고 `full === true` 를 기대). §7 #1

## 2. 영역 구성

| 영역번호 | 이름 | 설명 |
|---|---|---|
| FS-040-SEC-01 | 뷰 · 기간 컨트롤 | 일/주 토글, 이전·오늘·다음, 기간 범위 라벨 |
| FS-040-SEC-02 | 안내 힌트 | 격자 읽는 법 안내 — 조회 중에는 로딩 문구를 겸한다 |
| FS-040-SEC-03 | 달력 격자 | 행=시간 슬롯 · 열=날짜. 셀마다 예약 칩 + 예약 수/수용량 + 마감 |
| FS-040-SEC-04 | 슬롯 상세 패널(비표시 기본) | 선택한 슬롯의 예약 목록 → 예약 수정 진입 |
| FS-040-SEC-05 | 조회 실패 대체 화면(비표시 기본) | 화면 전체를 대체한다 |

## 3. 요소 명세

| 요소번호 | 영역 | 이름 | 유형 | 동작 | [서버] | 비고 |
|---|---|---|---|---|---|---|
| FS-040-EL-001 | FS-040-SEC-01 | '일' 버튼 | 버튼 | 일 뷰로 전환(열 1개 = `anchor`). 선택 중이면 primary, 아니면 secondary. 클릭 시 슬롯 선택 해제 | — | ⚠ 선택 상태를 **색상(variant)으로만** 표현한다 — `aria-pressed` 없음(§7 #2) |
| FS-040-EL-002 | FS-040-SEC-01 | '주' 버튼 | 버튼 | 주 뷰로 전환(열 7개 = `anchor` 가 속한 주 월~일). **초기 뷰가 주다**. 클릭 시 슬롯 선택 해제 | — | FS-040-EL-001 과 동일한 색상-only 문제 |
| FS-040-EL-003 | FS-040-SEC-01 | '이전' 버튼 | 버튼 | `<ChevronLeftIcon/>`, 접근 이름 '이전'. 일 뷰 −1일 / 주 뷰 −7일. 슬롯 선택 해제 | — | 아이콘만 — 접근 이름은 `aria-label` |
| FS-040-EL-004 | FS-040-SEC-01 | '오늘' 버튼 | 버튼 | `anchor` 를 오늘로 되돌리고 슬롯 선택 해제. **`anchor === today` 이면 비활성** | — | '오늘'은 브라우저 로컬 시각 기준(§7 #3) |
| FS-040-EL-005 | FS-040-SEC-01 | '다음' 버튼 | 버튼 | `<ChevronRightIcon/>`, 접근 이름 '다음'. 일 뷰 +1일 / 주 뷰 +7일. 슬롯 선택 해제 | — | 미래 상한 없음 |
| FS-040-EL-006 | FS-040-SEC-01 | 기간 범위 라벨 | 텍스트 | 일 뷰 `formatDayLabel(anchor)` → '7월 16일 (목)'. 주 뷰 `'<첫날> ~ <마지막날>'` → '2026-07-13 ~ 2026-07-19' | — | ⚠ **두 뷰의 날짜 표기 형식이 다르다** — 일 뷰는 사람이 읽는 표기, 주 뷰는 ISO 원본 문자열(§7 #4) |
| FS-040-EL-007 | FS-040-SEC-02 | 안내 힌트 (로딩 겸용) | 텍스트 | 조회 중(`isFetching`)이면 '불러오는 중…', 아니면 '셀의 숫자는 예약 수/수용량입니다. 슬롯을 클릭하면 아래에서 예약을 확인·수정할 수 있습니다.' | O | ⚠ **이 한 줄이 이 화면의 유일한 로딩 표현이다** — 격자는 로딩 중에도 그대로 렌더된다(FS-040-EL-008 참조) |
| FS-040-EL-008 | FS-040-SEC-03 | 달력 격자 | 표 | `role="grid"`, 접근 이름 '예약 일정 달력'. CSS grid — 1열(시간) + 날짜 수만큼. 행 = `DAY_SLOTS` 11개. **로딩·빈·실패 상태를 갖지 않는다** — 데이터가 없으면 모든 셀이 '0/4' 로 렌더된다 | O | ⚠ 최초 로드 중과 '예약 0건'이 **화면상 구별되지 않는다**(§7 #5) |
| FS-040-EL-008.1 | FS-040-SEC-03 | '시간' 헤더 셀 | 텍스트 | 좌상단 모서리. `role="columnheader"`, 문구 '시간' | — | — |
| FS-040-EL-008.2 | FS-040-SEC-03 | 날짜 헤더 셀 | 텍스트 | `role="columnheader"`, `formatDayLabel(day)` → '7월 16일 (목)'. **오늘이면 primary 색** | — | ⚠ '오늘'을 **색상으로만** 표시한다 — 텍스트·아이콘 등 비색상 인코딩이 없다(§7 #2) |
| FS-040-EL-008.3 | FS-040-SEC-03 | 시간 슬롯 행 헤더 | 텍스트 | `role="rowheader"`, 슬롯 라벨 'HH:00'. tabular-nums | — | ⚠ `role="row"` 안에 있지 않다(§7 #6) |
| FS-040-EL-008.4 | FS-040-SEC-03 | 슬롯 셀 | 버튼 | `<button type="button" className="tds-ui-focusable">`. 접근 이름 `'<날짜 라벨> <슬롯 라벨> — 예약 N건 / 수용 M건'`. `aria-pressed={selected}`. 클릭 시 그 슬롯을 선택해 FS-040-SEC-04 를 편다. 선택된 셀은 info 배경 | — | ⚠ `role="gridcell"` 이 아니다. 주 뷰에서 **77개(11×7)가 전부 Tab 정지점**이다(§7 #6) |
| FS-040-EL-008.5 | FS-040-SEC-03 | 예약 칩 | 배지 | 셀에 걸치는 유효 예약마다 `StatusBadge`. 라벨 `'<시작시각> <고객명>'`, 톤은 `bookingStatusTone(status)` | — | ⚠ **고객명이 격자에 노출된다**(개인정보 — BE-040 §7.3). ⚠ 상태를 **톤 색상으로만** 구분한다 — 라벨에 상태 문구가 없다(§7 #2). ⚠ **칩 클릭 핸들러가 없다** — 파일 헤더 주석은 '칩 클릭 → 해당 예약 수정으로 이동'이라 하지만 미구현이며 클릭은 셀 선택으로 흡수된다(§7 #7) |
| FS-040-EL-008.6 | FS-040-SEC-03 | 예약 수 / 수용량 | 텍스트 | 마감이 아니면 `'<booked>/<capacity>'`(muted), 마감이면 **'마감'**(danger 톤 + bold) | — | 이중 인코딩 O — 색상 + 문구가 함께 바뀐다 |
| FS-040-EL-008.7 | FS-040-SEC-03 | 가로 스크롤 컨테이너 | 텍스트 | 격자를 `overflowX: 'auto'` 컨테이너가 감싼다. 격자 최소 폭 `calc(var(--tds-space-6) * 12)`, 셀 최소 폭 `calc(var(--tds-space-6) * 3)` | — | 비표시 규칙. 좁은 폭에서 페이지가 아니라 이 컨테이너가 스크롤된다 |
| FS-040-EL-009 | FS-040-SEC-04 | 슬롯 상세 패널 | 카드 | 슬롯이 선택됐을 때만 렌더. 격자 아래에 나타난다. 뷰·기간 변경 시 닫힌다 | O | 비표시 기본. **포커스를 옮기지 않고 announce 하지도 않는다**(§7 #6) |
| FS-040-EL-009.1 | FS-040-SEC-04 | 패널 제목 | 텍스트 | `CardTitle` — `'<날짜 라벨> <슬롯 라벨>'`. 그 날짜가 오늘이면 info 톤 '오늘' 배지를 덧붙인다 | — | — |
| FS-040-EL-009.2 | FS-040-SEC-04 | 패널 빈 상태 | 빈상태 | 선택한 슬롯에 유효 예약이 0건이면 '이 시간대에 예약이 없습니다.' | — | 비표시. 복구 액션 없음(이 화면에 예약 등록 경로가 없다 — §7 #8) |
| FS-040-EL-009.3 | FS-040-SEC-04 | 예약 행 | 텍스트 | 예약마다 한 줄: 상태 배지(`StatusBadge` — 톤 + `bookingStatusLabel(status,'방문완료')`) · `'<예약번호> · <시작>~<종료>'` · `'<고객명> · <인원>명'` · `resourceName(resourceId)` | — | 격자 칩과 달리 **상태 라벨 문구가 있다** |
| FS-040-EL-009.4 | FS-040-SEC-04 | '예약 상세' 버튼 | 버튼 | 클릭 시 `/reservations/<id>/edit` 로 이동 | — | ⚠ **라벨은 '예약 상세'인데 목적지는 수정 폼이다** — 예약 도메인에 상세 라우트(`/reservations/:id`)가 존재하지 않는다(§7 #9) |
| FS-040-EL-010 | FS-040-SEC-05 | 조회 실패 대체 화면 | 배너 | 조회 실패 시 **화면 전체**를 danger Alert '예약 일정을 불러오지 못했습니다.' + '다시 시도'(`refetch`)로 대체한다 | O | ⚠ 뷰 토글·기간 이동 컨트롤까지 함께 사라진다 — 실패 상태에서는 아무것도 조작할 수 없다(§7 #10) |

## 4. 예외 명세

| 요소번호 | 빈 상태 | 로딩 | 실패 | 유효성 | 권한없음 | 경합 | 대량 |
|---|---|---|---|---|---|---|---|
| FS-040-EL-001 | N/A — 항상 표시 | 조회 중에도 조작 가능 — 뷰 전환이 재조회를 유발하지 않는다(같은 캐시를 다시 그린다) | 실패 시 FS-040-EL-010 이 화면 전체를 대체해 **이 버튼도 사라진다** | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 화면 로컬 상태 | N/A — 단일 버튼 |
| FS-040-EL-002 | N/A — 항상 표시 | 위와 동일 | 위와 동일 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 화면 로컬 상태 | 주 뷰는 열이 7개라 셀이 77개가 된다 |
| FS-040-EL-003 | N/A — 항상 표시 | 조회 중에도 조작 가능. 재조회를 유발하지 않는다 | 위와 동일 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 화면 로컬 상태 | **과거 하한 없음** — 무한히 뒤로 갈 수 있다 |
| FS-040-EL-004 | N/A — 항상 표시 | 조회 중에도 조작 가능 | 위와 동일 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | `today` 는 **첫 렌더 시점**에 고정된다(`toDateString(new Date())`) — 자정을 넘겨도 갱신되지 않는다(§7 #3) | N/A — 단일 버튼 |
| FS-040-EL-005 | N/A — 항상 표시 | 조회 중에도 조작 가능 | 위와 동일 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 화면 로컬 상태 | **미래 상한 없음** |
| FS-040-EL-006 | N/A — 항상 값이 있다 | 조회와 무관 — `anchor` 만으로 계산된다 | 위와 동일 | N/A — 표시 전용 | §4.1 공통 규칙 적용 | `anchor` 는 화면 로컬 | 두 뷰의 형식이 다르다(§7 #4) |
| FS-040-EL-007 | 예약 0건이어도 안내 문구 그대로 | **이것이 이 화면의 유일한 로딩 표현이다** — '불러오는 중…'. 재조회 때도 뜬다(`isFetching` 직결)이라 데이터가 있는데도 이 줄만 바뀐다 | 실패 시 FS-040-EL-010 이 화면 전체를 대체해 이 줄도 사라진다 | N/A — 표시 전용 | §4.1 공통 규칙 적용 | 재조회 시 문구가 잠깐 바뀐다 — 격자는 이전 데이터를 유지한다 | 고정 문구 |
| FS-040-EL-008 | **빈 상태 표현이 없다.** 예약 0건이면 모든 셀이 '0/4' 로 렌더된다 — 격자 자체는 그대로 서 있다 | **로딩 표현이 없다.** `data ?? []` 라 최초 로드 중에도 격자가 '0/4' 로 완성돼 보인다 — 도착 전과 '예약 없음'이 **화면상 같다**(§7 #5) | FS-040-EL-010 이 화면 전체를 대체 | N/A — 표 자체 입력 없음 | §4.1 공통 규칙 적용 | 재조회로 셀 숫자가 바뀐다 — 예고 없이 | 슬롯 11 × 열 7 = **셀 77개 + 헤더 8개**가 항상 렌더된다 |
| FS-040-EL-008.1 | N/A — 정적 헤더 | 조회와 무관 | 화면 전체 대체로 사라진다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | N/A — 정적 | 1개 고정 |
| FS-040-EL-008.2 | N/A — 열은 날짜가 정하므로 항상 있다 | 조회와 무관 — `days` 는 `anchor` 로만 계산된다 | 화면 전체 대체로 사라진다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | '오늘' 강조가 첫 렌더의 `isToday(day)` 로 정해진다 — 자정을 넘겨도 갱신 안 됨 | 일 뷰 1개 / 주 뷰 7개 |
| FS-040-EL-008.3 | N/A — 슬롯은 상수라 항상 있다 | 조회와 무관 | 화면 전체 대체로 사라진다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | N/A — 상수 | 11개 고정. **영업시간 밖(20시 이후·9시 이전) 예약은 격자에 아예 나타나지 않는다**(§7 #11) |
| FS-040-EL-008.4 | 예약 0건이면 '0/4' 만 담고 여전히 클릭 가능 | 최초 로드 중에도 클릭 가능 — 열면 빈 패널이 뜬다 | 화면 전체 대체로 사라진다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 선택은 화면 로컬. 재조회로 그 슬롯의 예약이 바뀌면 패널 내용이 조용히 바뀐다 | 주 뷰 77개 — 전부 Tab 정지점(§7 #6) |
| FS-040-EL-008.5 | 셀에 예약이 없으면 칩 없음 | 최초 로드 중에는 칩이 없다(데이터 부재) — '예약 없음'과 구별 불가 | 화면 전체 대체로 사라진다 | N/A — 표시 전용 | §4.1 공통 규칙 적용 — **고객명이 권한 분기 없이 노출된다** | 재조회 시점에만 갱신 | **한 셀의 칩 수에 상한이 없다** — 겹치는 예약이 많으면 칩이 셀 안에서 계속 줄바꿈되어 그 행 전체가 세로로 늘어난다(§7 #12) |
| FS-040-EL-008.6 | 예약 0건이면 '0/4' | 최초 로드 중 '0/4' — 잘못된 사실을 단정적으로 보인다(§7 #5) | 화면 전체 대체로 사라진다 | N/A — 표시 전용 | §4.1 공통 규칙 적용 | 재조회로 숫자·마감 여부가 바뀐다 | 수용량은 자원 수 고정(4). 예약이 5건이면 '마감'만 보이고 5/4 라는 사실은 사라진다 |
| FS-040-EL-008.7 | N/A — 컨테이너 | N/A — 조회와 무관 | 화면 전체 대체로 사라진다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | N/A | 열이 7개여도 페이지가 아니라 이 컨테이너가 가로 스크롤된다 |
| FS-040-EL-009 | 슬롯 미선택이면 미렌더 | 최초 로드 중 슬롯을 열면 **빈 패널**이 뜬다('이 시간대에 예약이 없습니다') — 도착하면 조용히 채워진다 | 화면 전체 대체로 사라진다(선택도 함께 사라진다) | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 뷰·기간 변경 시 닫힌다. 재조회로 내용이 바뀌어도 **알리지 않는다**(§7 #6) | 슬롯의 예약 수만큼 행 |
| FS-040-EL-009.1 | N/A — 패널이 있어야 성립 | N/A — `selected` 로만 계산 | 화면 전체 대체 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | '오늘' 배지가 `isToday(selected.date)` — 매 렌더 재평가된다 | 1줄 |
| FS-040-EL-009.2 | 이것이 빈 상태 표현. '이 시간대에 예약이 없습니다.' | 최초 로드 중에도 이 문구가 뜬다 — **거짓 빈 상태**(§7 #5) | 화면 전체 대체 | N/A — 입력 없음 | §4.1 공통 규칙 적용 | 재조회로 예약이 도착하면 조용히 행으로 바뀐다 | N/A — 1줄 |
| FS-040-EL-009.3 | 예약 0건이면 FS-040-EL-009.2 | 최초 로드 중 미표시 | 화면 전체 대체 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — 고객명·인원이 그대로 노출된다 | 조회 시점 값 | 예약 수만큼. 상한·페이지 없음 |
| FS-040-EL-009.4 | 예약이 있어야 존재 | 최초 로드 중 미표시 | N/A — 라우팅만 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — **update 권한이 없어도 렌더된다** | 다른 관리자가 지운 예약이면 이동 후 폼이 '찾을 수 없습니다'(FS-037-EL-032) | 예약마다 1개 |
| FS-040-EL-010 | N/A — 실패 상태 | 재시도 시 배너가 사라지고 격자가 돌아온다(이전 데이터가 있으면 그대로) | 이것이 실패 표현. '예약 일정을 불러오지 못했습니다.' 1문구 — 401/403/500 을 구분하지 않는다 | N/A — 입력 없음 | §4.1 공통 규칙 적용 — 권한 부족도 이 배너 | 재시도는 같은 조회 재발행 | N/A — 표시 전용 |

### 4.1 공통 예외 규칙

| 규칙 | 내용 |
|---|---|
| 이 화면은 **읽기 전용**이다 | 쓰기 요청이 하나도 없다. 예약을 만들거나 고치거나 지우지 않고, 수정 폼으로 **보내기만** 한다. 그래서 저장 실패·충돌·미저장 가드·중복 제출 같은 예외 축이 이 화면에 존재하지 않는다 |
| 데이터를 예약 관리와 공유한다 | 같은 쿼리 키(`['reservations','list']`)를 읽으므로 **예약 관리에서 저장·삭제하면 이 화면도 함께 무효화되어 갱신된다.** 반대로 이 화면을 열어 둔 채 30초(`staleTime`)가 지나기 전에는 재조회하지 않는다 |
| 네트워크 단절 | FS-040-EL-010 이 화면 전체를 대체한다. 오프라인 감지(`navigator.onLine`)는 없다 |
| 세션 만료 | 조회의 401 은 앱 공용 쿼리 계층이 가로채 재인증 경로로 보낸다. 이 화면에 보존할 입력이 없어 손실이 없다 |
| 요청 타임아웃 | 프론트 타임아웃 상한 없음. abort 는 화면 이탈(언마운트) 시 react-query 가 signal 로 처리 |
| 실패 통지의 자리 | 조회 실패는 **화면 전체를 대체하는** 인라인 배너 하나뿐이다. 이 화면은 토스트를 띄우지 않는다(쓰기가 없다) |
| 권한 없음 | 역할 기반 분기 없음. 화면 자체의 read 권한은 앱 공용 가드가 403 화면으로 막는다(리소스는 잎 `/reservations/schedule`). 서버 권한 응답은 FS-040-EL-010 으로 떨어진다 |
| 시간 판정의 기준 | '오늘'(`isToday`)·주 시작(`startOfWeek`)·날짜 산술(`addDays`)이 전부 **브라우저 로컬 타임존**이다. 표시 TZ 정책이 없다(§7 #3) |
| 상태 전이·더블부킹 | 이 화면은 판정하지 않는다 — 취소·노쇼를 세지 않는 것(`reservationsInSlot` → `isActive`)이 유일한 도메인 규칙 소비다. 더블부킹 배지는 예약 목록(FS-037-EL-007.12)에만 있고 **이 화면의 '마감'은 그것과 다른 개념이다**(§1.1) |

## 5. 서버 연동 지점

| 요소번호 | 이름 | 읽기/쓰기 | 필요 데이터 | 프론트 어댑터 | 비고 |
|---|---|---|---|---|---|
| FS-040-EL-007 / EL-008 / EL-008.4 / EL-008.5 / EL-008.6 / EL-009 / EL-009.3 / EL-010 | 예약 목록 조회 | R | 예약 전량 | `useCrudListQuery('reservations', reservationAdapter)` → `reservationAdapter.fetchAll(signal)` | **예약 관리(FS-037 §5)와 같은 어댑터·같은 쿼리 키다.** 이 화면 전용 조회가 아니다 |
| FS-040-EL-008.6 (수용량) | 자원 카탈로그 | R | 자원 개수 | `listResources()` (via `slotCapacity()`) | ⚠ **동기 함수** — 조회가 아니다(FS-037 §5 의 같은 제약) |
| FS-040-EL-009.3 (자원명) | 자원 카탈로그 | R | 자원 id → 이름 | `resourceName(id)` | ⚠ 동기 함수 |

> **현재 구현 상태 (A63 참고)**: **이 화면에는 `// TODO(backend)` 심이 하나도 없다.** `schedule-data.ts` 전체에 백엔드 주석이 없고, 데이터는 `_shared/reservation-store.ts` 의 어댑터에서 온다 — 그 파일의 심(`GET/POST /api/reservations · GET/PUT/DELETE /api/reservations/:id`)이 이 화면의 유일한 연동 지점이다. **즉 예약 일정은 예약 엔드포인트를 재사용하며 전용 엔드포인트를 갖지 않는다**(BE-040 §1).
>
> 슬롯 정의(`DAY_SLOTS` — 영업시간 09~20시)와 수용량(`slotCapacity()` — 자원 수)은 **클라이언트 상수에서 파생**된다. 서버에서 오지 않는다 — 심이 없으므로 BE-040 은 이들에 대한 엔드포인트를 정의하지 않고 '미정'으로 남긴다(§7 #11 · #13).
>
> **범위 조회(`?from=&to=`)가 없다.** 어댑터가 `fetchAll(signal)` 하나뿐이라 달력이 어느 주를 보든 **매번 예약 전량**을 읽고 클라이언트에서 날짜로 거른다(`reservationsInSlot`). 심이 없으므로 BE-040 이 지어내지 않는다 — §7 #13.

## 6. 자기 점검 (제출 전 확인)

- [x] 구현 소스를 전수 대조했다 — `ScheduleCalendarPage.tsx` · `components/CalendarGrid.tsx` · `schedule-data.ts` · `schedule.test.ts` · 소비하는 `_shared/**` 전부
- [x] **`schedule-data.ts` 에 `// TODO(backend)` 심이 없음을 F2 기준으로 재확인**했고, `ScheduleCalendarPage.tsx:116-121` 이 `useCrudListQuery('reservations', reservationAdapter)` 로 예약 어댑터를 읽는다는 사실을 코드로 확인해 §5 에 기록했다
- [x] 새 인터랙티브 표면(달력 격자·슬롯 셀·슬롯 패널)의 예외(로딩 표현 부재·거짓 빈 상태·선택 미announce)를 §4 에 채웠다
- [x] 보이지 않는 요소(가로 스크롤 컨테이너·패널 빈 상태·실패 대체 화면·슬롯 선택 해제 규칙)에 번호를 줬다
- [x] §4 예외 7축 빈칸 0건. 모든 `N/A` 에 사유
- [x] `[서버]` = O 요소가 §5 에 전부 요약됐다
- [x] 엔드포인트·HTTP·에러코드·DB 스키마를 쓰지 않았다 (BE-040 영역)
- [x] 화면에 없는 것을 지어내지 않았다 — 월 뷰·예약 등록·드래그 이동·범위 조회·자원별 열은 **부재**로 §7 에 기록했다

## 7. 미결 사항 (A11 / A01 / A63 / A40 이관)

| # | 내용 | 이관 대상 |
|---|---|---|
| 1 | **수용량 모델이 도메인과 어긋난다.** 이 화면의 수용량 = **자원 개수**(자리 4개, 배타 점유)인데 예약 폼의 `capacity` = **동시 수용 인원**(세미나룸 A 12명). 같은 방에 몰린 예약 4건이 '마감'이 되고 빈 자원 3개는 무시된다. `schedule.test.ts:51-63` 이 이 동작을 고정한다 — 즉 **의도인지 결함인지 코드만으로 판정할 수 없다.** 도메인이 배타 점유를 택하면 `capacity`(인원)는 정원 검증에만 쓰이고 슬롯 수용량은 자원 수가 맞다. 인원 수용을 택하면 이 화면의 '마감'은 자원별로 계산돼야 한다 | **A11 · A01 (도메인 결정)** · A63 (BE-037 §7.8 #4) |
| 2 | **선택·상태를 색상으로만 인코딩한 표면이 셋이다** — ① 일/주 토글이 `variant` 색상만 다르고 `aria-pressed` 가 없다(FS-040-EL-001·002) ② '오늘' 열 헤더가 primary 색만 다르다(FS-040-EL-008.2) ③ 격자의 예약 칩이 상태를 톤 색상으로만 구분한다(라벨은 '시각 + 고객명' — FS-040-EL-008.5). 슬롯 셀(`aria-pressed`)과 '마감'(색+문구)은 옳게 돼 있어 **한 화면 안에서 규칙이 갈린다** | A11 change_request |
| 3 | **'오늘'이 첫 렌더 시점에 고정되고 브라우저 로컬 TZ 기준이다.** `ScheduleCalendarPage.tsx:111` 이 `const today = toDateString(new Date())` 를 렌더 본문에서 계산하지만 `anchor` 초기값으로만 쓰이고 `'오늘' 버튼 비활성`(`anchor === today`)·`isToday()` 강조가 이에 의존한다 — 자정을 넘겨 화면을 열어 두면 '오늘'이 어제를 가리킨다. TZ 정책도 없다(`_shared/calendar.ts` 가 전부 browser-local getter) | A11 · A63 |
| 4 | **두 뷰의 날짜 표기 형식이 다르다** — 일 뷰 '7월 16일 (목)'(`formatDayLabel`) vs 주 뷰 '2026-07-13 ~ 2026-07-19'(ISO 원본 문자열 연결). 같은 자리에서 뷰만 바꿨을 뿐인데 표기 체계가 바뀐다 | A11 change_request |
| 5 | **격자에 로딩 표현이 없어 최초 로드 중이 '예약 0건'과 구별되지 않는다.** `all = data ?? []` 라 도착 전에도 격자가 '0/4' 로 **완성된 사실처럼** 렌더된다. 유일한 로딩 신호는 격자 위 힌트 한 줄('불러오는 중…')이고, 그 사이 슬롯을 열면 '이 시간대에 예약이 없습니다'라는 **거짓 빈 상태**까지 보인다. 운영자가 '이 시간 비었네' 하고 오판할 수 있는 화면이다 | A11 change_request (STATE-01 P0 — NFR-040 §5) |
| 6 | **격자의 ARIA 구조가 불완전하고 키보드 조작이 없다.** ① `role="grid"` 아래에 **`role="row"` 가 하나도 없다** — 각 슬롯 행을 감싸는 것은 `<div style={{display:'contents'}}>`(역할 없음)이고 `columnheader`/`rowheader` 가 행 안에 있지 않다 ② 셀이 `role="gridcell"` 이 아니라 그냥 `<button>` 이다 ③ **화살표 키 이동이 없다** — grid 패턴은 셀 하나만 Tab 정지점으로 두고 방향키로 이동하는데, 여기서는 주 뷰 **77개 버튼이 전부 Tab 정지점**이다(격자를 지나치려면 Tab 77번) ④ 슬롯을 선택해 패널이 열려도 포커스를 옮기지 않고 live region 으로 알리지도 않는다 — 스크린리더 사용자는 화면 어딘가에 카드가 생긴 것을 모른다 | A11 change_request (A11Y-16 P1 — NFR-040 §3) |
| 7 | **`CalendarGrid.tsx` 헤더 주석이 미구현 동작을 서술한다** — '칩 클릭 → 해당 예약 수정으로 이동'이라 적혀 있으나 칩(`StatusBadge`)에 클릭 핸들러가 없고, 칩이 셀 `<button>` 안에 있어 클릭은 슬롯 선택으로 흡수된다. 주석을 지우거나 동작을 구현해야 한다 | A11 change_request |
| 8 | **달력에서 예약을 만들 수 없다.** 빈 슬롯을 클릭하면 '이 시간대에 예약이 없습니다'로 끝난다 — 그 슬롯을 미리 채운 등록 폼(`/reservations/new?date=…&start=…`)으로 보내는 것이 달력의 기본 기대인데 경로가 없다. 드래그로 예약을 옮기는 경로도 없다 | A11 change_request |
| 9 | **'예약 상세' 버튼이 상세가 아니라 수정 폼으로 간다** — `/reservations/<id>/edit`. 예약 도메인에 상세 라우트(`/reservations/:id`)가 `APP_ROUTES` 에 존재하지 않는다. 라벨이 목적지를 잘못 알린다(FS-037 의 행 클릭도 같은 이유로 수정 폼으로 간다) | A11 change_request |
| 10 | **조회 실패가 화면 전체를 대체해 컨트롤까지 지운다** — `ScheduleCalendarPage.tsx:135-148` 이 이른 반환으로 뷰 토글·기간 이동을 함께 없앤다. 실패 상태에서 운영자가 할 수 있는 일은 '다시 시도' 하나뿐이고, 보던 기간이 어디였는지도 화면에서 사라진다 | A11 change_request |
| 11 | **영업시간(09~20시)이 클라이언트 상수라 그 밖의 예약이 격자에서 사라진다.** `DAY_SLOTS` 가 11개 슬롯을 고정하므로 08:00 이나 21:00 예약은 **어느 셀에도 나타나지 않는다** — 예약 폼은 그 시각을 아무 경고 없이 저장한다(`reservationSchema` 에 영업시간 검증이 없다). 즉 **저장은 되는데 달력에서는 보이지 않는 예약**이 만들어진다 | **A11 · A01 (도메인 결정)** · A63 |
| 12 | 한 셀의 칩 수에 상한이 없다 — 겹치는 예약이 많으면 칩이 줄바꿈되며 그 행 전체가 세로로 늘어나 격자 정렬이 무너진다. 'N건 더' 같은 접힘이 없다 | A11 change_request |
| 13 | **범위 조회가 없다.** 달력이 어느 주를 보든 예약 **전량**을 읽고 클라이언트가 날짜로 거른다 — `?from=&to=` 심이 없어 BE-040 이 계약을 정의하지 않았다. 예약이 쌓이면 달력 한 화면을 위해 전량을 내려받게 된다 | A63 (BE-040 §7.2) · A11 |
| 14 | **달력 상태가 URL 에 없다** — 뷰(일/주)·기간(`anchor`)·선택 슬롯이 전부 `useState` 다. 새로고침하면 오늘·주 뷰로 돌아가고, 예약을 열었다가 Back 하면 보던 주가 사라지며, '다음 주 화요일 10시 좀 봐주세요' 링크를 만들 수 없다 | A11 change_request (IA-13 P0 — NFR-040 §5) |
| 15 | 월 뷰가 없다(일·주 둘뿐) · 자원별/담당자별 열 분할이 없다 · 대응 SCR 문서 부재 | A11 / A01 |
