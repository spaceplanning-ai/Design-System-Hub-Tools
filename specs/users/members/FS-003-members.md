---
id: FS-003
title: "회원 관리 목록"
screen: SCR-003               # ⚠ 회원 관리 SCR(D2)이 아직 없다 — SCR-003 번호는 product-registration 이 점유 중. §7 미결 사항 참조
route: /users/members
owner: 기능 명세
reviewer: 명세 리뷰
gate: G9
status: draft
confirmedAt: 2026-07-15
version: 1.1
date: 2026-07-15
---

# FS-003. 회원 관리 목록

## 1. 화면 개요

| 항목 | 내용 |
|---|---|
| 목적 | 가입한 회원을 등급·그룹·키워드로 좁혀 찾고, 개별 회원의 상세로 이동하거나 삭제·알림 발송·CSV 내보내기를 수행한다 |
| 역할(주 사용자) | 관리자 (구현에 역할 분기 없음 — §4.1 참조) |
| 진입 경로 | 좌측 GNB > 회원 > 회원 관리 (`nav-config.ts`, `to: '/users/members'`) |
| 관련 화면 | FS-004 회원 상세 (행 클릭·닉네임 링크·메모 아이콘으로 이동) / `/marketing/templates` (배너 링크) |
| 대응 SCR | SCR-003 (미작성 — §7) |
| 구현 경로 | `apps/admin/src/pages/members/MembersPage.tsx` 및 `apps/admin/src/pages/members/components/**` |

### 1.1 이 화면에 없는 기능 (요구사항)

| 없는 것 | 이유 |
|---|---|
| '사용자 추가' / '대량 추가' 버튼 | 고객은 **회원가입으로만 유입된다**. 관리자가 회원을 직접 생성하는 경로를 제공하지 않는다. 이 사실은 화면 좌측 하단 안내문(FS-003-EL-006)이 명시한다 |
| 행 ⋯ 메뉴의 3번째 이후 항목 | 행 액션은 **'회원 삭제' / '알림 발송' 두 개뿐**이다 (요구사항) |

> `'새 그룹 만들기'`(FS-003-EL-005)는 회원이 아니라 **묶음(그룹)** 을 만드는 기능이므로 위 규칙과 충돌하지 않는다.

## 2. 영역 구성

| 영역번호 | 이름 | 설명 |
|---|---|---|
| FS-003-SEC-01 | 상단 안내 영역 | 페이지 최상단 전폭. 회원가입 안내 배너. 작업 결과(성공·실패)는 이 영역이 아니라 화면 고정 토스트 뷰포트(FS-003-EL-002)로 통지한다 |
| FS-003-SEC-02 | 좌측 필터 사이드바 | 등급 필터, 그룹 필터, '새 그룹 만들기' 버튼, 회원가입 유입 안내문 |
| FS-003-SEC-03 | 목록 툴바 | 우측 본문 최상단. 검색 입력과 내보내기 버튼 |
| FS-003-SEC-04 | 목록 표 | 조회 요약 텍스트와 회원 표. 로딩·빈 상태·조회 실패가 이 영역을 대체 렌더한다 |
| FS-003-SEC-05 | 페이지네이션 | 표 하단. 이전 / 번호 / 다음 |
| FS-003-SEC-06 | 새 그룹 만들기 모달 (비표시 기본) | FS-003-EL-005 를 눌렀을 때만 열리는 폼 모달 |
| FS-003-SEC-07 | 회원 삭제 확인 다이얼로그 (비표시 기본) | 행 ⋯ 메뉴의 '회원 삭제'를 골랐을 때만 열리는 확인 모달 |

## 3. 요소 명세

| 요소번호 | 영역 | 이름 | 유형 | 동작 | [서버] | 비고 |
|---|---|---|---|---|---|---|
| FS-003-EL-001 | FS-003-SEC-01 | 회원가입 안내 배너 | 배너 | 페이지 진입 시 항상 열린 상태로 렌더된다. 경고 톤(`tone="warning"`) 이며 문구는 '회원이 가입했어요! 가입 축하, 비밀번호 찾기 등 메일을 보내려면 메일 발송 설정을 해보세요.' 이다. `role="status"` | — | 조회 결과와 무관한 정적 안내 |
| FS-003-EL-001.1 | FS-003-SEC-01 | 배너 내 '메일 발송 설정' 링크 | 링크 | 클릭 시 `/marketing/templates` 로 이동한다 | — | 밑줄 + 굵은 글씨 |
| FS-003-EL-001.2 | FS-003-SEC-01 | 배너 닫기(×) 버튼 | 버튼 | 클릭 시 FS-003-EL-001 을 DOM 에서 제거한다. 같은 페이지 세션 동안 다시 표시되지 않는다. 브라우저 새로고침 또는 재진입 시에는 다시 표시된다(상태를 저장하지 않는다) | — | 접근 가능한 이름 '안내 닫기' |
| FS-003-EL-002 | FS-003-SEC-01 | 작업 결과 토스트 | 배너 | 쓰기 작업이 끝나면 결과를 토스트로 통지한다(`shared/ui/Toast`). 성공은 성공 톤 + `role="status"` 이며 4초 뒤 자동으로 사라진다. **실패는 위험 톤 + `role="alert"` 이며 자동으로 사라지지 않는다** — 사용자가 닫거나 '다시 시도'(FS-003-EL-002.2)를 누를 때까지 남는다. 문구는 작업별로 고정이다 — 내보내기 성공: '회원 N명을 CSV 로 내보냈습니다.' / 내보내기 실패: '내보내기에 실패했습니다. 잠시 후 다시 시도해 주세요.' / 그룹 생성: `'<그룹명>' 그룹을 만들었습니다.` / 알림 발송 성공: '<닉네임> 회원에게 알림을 발송했습니다.' / 알림 발송 실패: '<닉네임> 회원에게 알림을 발송하지 못했습니다. 잠시 후 다시 시도해 주세요.' / 회원 삭제 성공: '<닉네임> 회원을 삭제했습니다.' | — | 비표시 요소(기본). **회원 삭제 실패는 토스트가 아니라 확인 다이얼로그 **안** 배너(FS-003-EL-017.5)로 알린다** — 모달이 떠 있는 동안 토스트는 시선 밖이기 때문이다(`shared/ui/README.md`). 목록 **조회** 실패도 토스트가 아니라 인라인 배너(FS-003-EL-014)다 |
| FS-003-EL-002.1 | FS-003-SEC-01 | 결과 토스트 닫기(×) 버튼 | 버튼 | 클릭 시 해당 토스트를 제거한다. 다음 작업이 끝나면 새 토스트가 표시된다 | — | 비표시 요소. 접근 가능한 이름 '알림 닫기' |
| FS-003-EL-002.2 | FS-003-SEC-01 | 실패 토스트 '다시 시도' 버튼 | 버튼 | 실패 토스트에만 렌더된다. 클릭 시 토스트를 닫고 실패한 작업을 **같은 인자로** 다시 실행한다(내보내기 · 알림 발송 · 일괄 알림 발송) | O | 비표시 요소. 실패를 알리기만 하고 복구 경로를 주지 않으면 사용자는 같은 조작을 처음부터 되풀이해야 한다 |
| FS-003-EL-003 | FS-003-SEC-02 | 등급 필터 | 카드 | 제목 '등급' 아래 4개 버튼을 세로로 나열한다(`aria-label="회원 등급 필터"`). 선택된 항목은 `aria-pressed="true"` 이며 배경·색·굵기가 바뀐다 | O | 건수는 검색·그룹 필터와 무관한 **전체 회원 기준** 고정값이다 |
| FS-003-EL-003.1 | FS-003-SEC-02 | 등급 필터 '전체 사용자' | 버튼 | 클릭 시 등급 조건을 해제하고 페이지를 1로 되돌린 뒤 목록을 재조회한다. 우측에 전체 건수 배지를 표시한다 | O | 화면 진입 시 기본 선택 |
| FS-003-EL-003.2 | FS-003-SEC-02 | 등급 필터 '일반 사용자' | 버튼 | 클릭 시 등급을 `normal` 로 설정하고 페이지를 1로 되돌린 뒤 재조회한다. 우측에 일반 등급 건수 배지를 표시한다 | O | 그룹 필터와 AND 로 결합된다(FS-003-EL-007) |
| FS-003-EL-003.3 | FS-003-SEC-02 | 등급 필터 'VIP 사용자' | 버튼 | 클릭 시 등급을 `vip` 로 설정하고 페이지를 1로 되돌린 뒤 재조회한다. 우측에 VIP 건수 배지를 표시한다 | O | 그룹 필터와 AND 로 결합된다 |
| FS-003-EL-003.4 | FS-003-SEC-02 | 등급 필터 'VVIP 사용자' | 버튼 | 클릭 시 등급을 `vvip` 로 설정하고 페이지를 1로 되돌린 뒤 재조회한다. 우측에 VVIP 건수 배지를 표시한다 | O | 그룹 필터와 AND 로 결합된다 |
| FS-003-EL-004 | FS-003-SEC-02 | 그룹 필터 | 카드 | 제목 '그룹' 아래 '전체 그룹' + 서버가 내려준 그룹 목록을 세로로 나열한다(`aria-label="회원 그룹 필터"`). 그룹 목록은 화면 진입 시 1회 조회하며, 새 그룹을 만들면 다시 조회한다 | O | 등급 필터와 별개의 축이다 |
| FS-003-EL-004.1 | FS-003-SEC-02 | 그룹 필터 '전체 그룹' | 버튼 | 클릭 시 그룹 조건을 해제하고 페이지를 1로 되돌린 뒤 재조회한다 | O | 화면 진입 시 기본 선택. 건수 배지를 표시하지 않는다 |
| FS-003-EL-004.2 | FS-003-SEC-02 | 그룹 항목 버튼(그룹 수만큼 반복) | 버튼 | 클릭 시 해당 그룹 id 로 그룹 조건을 설정하고 페이지를 1로 되돌린 뒤 재조회한다. 우측에 해당 그룹의 회원 수 배지를 표시한다 | O | 건수는 검색·등급 필터와 무관한 전체 회원 기준 고정값이다 |
| FS-003-EL-005 | FS-003-SEC-02 | '+ 새 그룹 만들기' 버튼 | 버튼 | 클릭 시 FS-003-SEC-06(새 그룹 만들기 모달)을 연다. 이 버튼 자체는 서버를 호출하지 않는다 | — | 회원이 아니라 그룹을 만드는 기능이다 |
| FS-003-EL-006 | FS-003-SEC-02 | 회원가입 유입 안내문 | 텍스트 | '고객 목록은 회원가입으로만 유입됩니다. 관리자가 회원을 직접 추가하는 기능은 제공하지 않습니다.' 를 사이드바 하단에 상시 표시한다. 상호작용이 없다 | — | 상단 경계선으로 필터와 구분된다 |
| FS-003-EL-007 | FS-003-SEC-02 | 등급 × 그룹 AND 필터 조합 | 텍스트 | 등급 조건과 그룹 조건은 서로 다른 축이며, 둘 다 '전체'가 아니면 **AND** 로 결합해 조회한다. 예: 등급 `vip` + 그룹 `free-shipping` 을 고르면 두 조건을 모두 만족하는 회원만 표시된다. 어느 한쪽만 바꿔도 페이지는 1로 되돌아가고 행 선택은 전부 해제된다 | O | 비표시 요소(조합 규칙). 화면에 별도 UI 가 없고 FS-003-EL-003 · FS-003-EL-004 의 상호작용으로만 드러난다 |
| FS-003-EL-008 | FS-003-SEC-03 | 검색 입력 | 입력 | `type="search"`, placeholder '검색'. 입력이 멈춘 뒤 250ms 가 지나면 키워드를 확정하고 목록을 재조회한다. 키워드는 **닉네임 또는 계정**에 대해 대소문자를 구분하지 않는 부분 일치로 걸린다. 확정 시 페이지를 1로 되돌리고 행 선택을 전부 해제한다 | O | 접근 가능한 이름 '닉네임 또는 계정 검색'. 입력 좌측에 돋보기 아이콘(클릭 통과) |
| FS-003-EL-009 | FS-003-SEC-03 | 내보내기 버튼 | 버튼 | 클릭 시 **현재 페이지가 아니라 현재 등급·그룹·검색 조건에 걸린 전체 회원**을 조회해 CSV 파일을 브라우저 다운로드로 떨군다. 파일명은 `members-<YYYY-MM-DD>.csv`, 헤더는 닉네임·계정·회원 유형·그룹·가입일·적립금·누적 구매금액 7열이다. 파일 선두에 UTF-8 BOM 을 붙인다. 완료 시 FS-003-EL-002 로 결과를 알린다 | O | 진행 중 라벨이 '내보내는 중…' 으로 바뀌고 버튼이 비활성된다 |
| FS-003-EL-018 | FS-003-SEC-03 | 일괄 '알림 발송' 버튼 | 버튼 | 행 선택(FS-003-EL-011.1 · FS-003-EL-011.2)이 1건 이상일 때만 렌더된다. 라벨은 '선택 N명 알림 발송'. 클릭 시 확인 절차 없이 선택된 회원 전원에게 알림 발송을 요청한다. 전원 성공하면 FS-003-EL-002 로 '회원 N명에게 알림을 발송했습니다.', 1명이라도 실패하면 '회원 N명 중 M명에게 알림을 발송하지 못했습니다. 잠시 후 다시 시도해 주세요.' 를 '다시 시도'(FS-003-EL-002.2)와 함께 표시한다 | O | 액션 2종(알림 발송·회원 삭제)은 행 ⋯ 메뉴와 동일하다(요구사항). 진행 중 라벨이 '발송 중…' 으로 바뀌고 이 버튼과 FS-003-EL-019 가 함께 비활성된다 |
| FS-003-EL-019 | FS-003-SEC-03 | 일괄 '삭제' 버튼 | 버튼 | 행 선택이 1건 이상일 때만 렌더된다. 라벨은 '선택 N명 삭제'(위험 스타일). 클릭 시 FS-003-SEC-07(삭제 확인 다이얼로그)을 **선택된 회원 전원**을 대상으로 연다. 이 버튼 자체는 삭제하지 않는다 | O | 실제 삭제 호출은 FS-003-EL-017.4. 일괄 발송 진행 중에는 비활성된다 |
| FS-003-EL-010 | FS-003-SEC-04 | 조회 요약 텍스트 | 텍스트 | 조회 중에는 '불러오는 중…', 완료 후에는 '전체 N명' 을 표시한다. 선택된 행이 1건 이상이면 뒤에 ' · N명 선택됨' 을 덧붙인다 | — | N 은 천 단위 구분 기호를 붙인다 |
| FS-003-EL-011 | FS-003-SEC-04 | 회원 목록 표 | 표 | 페이지당 최대 10행. 컬럼은 좌→우로 선택 체크박스 · 닉네임 · 계정 · 회원 유형 · 그룹 · 가입일 · 적립금 · 글·댓글·구매평·문의 · 누적 구매금액 · 메모 · 행 액션 이다. 조회 중에는 `aria-busy="true"` 를 유지한다. 시각적으로 숨긴 caption 이 행 클릭 규칙을 설명한다 | O | 정렬 토글은 구현에 없다 |
| FS-003-EL-011.1 | FS-003-SEC-04 | 전체선택 체크박스 (헤더) | 입력 | 체크 시 **현재 페이지에 보이는 행 전부**를 선택하고, 해제 시 전부 해제한다. 현재 페이지 행이 일부만 선택된 상태에서는 `indeterminate` 로 표시된다. 현재 페이지 행이 0건이면 체크되지 않는다 | — | 접근 가능한 이름 '이 페이지의 회원 전체 선택'. 선택 상태는 서버로 전송되지 않는다 |
| FS-003-EL-011.2 | FS-003-SEC-04 | 행 체크박스 | 입력 | 체크/해제 시 해당 회원 id 를 선택 집합에 넣거나 뺀다. 이 클릭은 행 이동(FS-003-EL-011.13)을 발생시키지 않는다 | — | 접근 가능한 이름 '<닉네임> 선택' |
| FS-003-EL-011.3 | FS-003-SEC-04 | 닉네임 링크 | 링크 | 클릭 시 `/users/members/<회원 id>` 로 이동한다 | — | 키보드 사용자의 상세 진입 경로 |
| FS-003-EL-011.4 | FS-003-SEC-04 | 계정 셀 | 텍스트 | 회원의 계정(이메일)을 줄바꿈 없이 표시한다 | — | 상호작용 없음 |
| FS-003-EL-011.5 | FS-003-SEC-04 | 회원 유형 셀 | 텍스트 | 등급을 '일반회원' / 'VIP' / 'VVIP' 중 하나로 표시한다 | — | 상호작용 없음 |
| FS-003-EL-011.6 | FS-003-SEC-04 | 그룹 셀 | 텍스트 | 회원이 속한 그룹의 표시명을 표시한다 | — | 그룹이 없는 회원은 '그룹 없음' 으로 표시된다 |
| FS-003-EL-011.7 | FS-003-SEC-04 | 가입일 셀 | 텍스트 | 가입 시각이 현재로부터 7일 이내면 상대 시각('방금 전' / 'N분전' / 'N시간전' / 'N일전')으로, 7일을 넘거나 미래 시각이면 'YYYY-MM-DD' 로 표시한다 | — | 상호작용 없음 |
| FS-003-EL-011.8 | FS-003-SEC-04 | 적립금 셀 | 텍스트 | 보유 적립금을 천 단위 구분 기호를 붙여 우측 정렬로 표시한다 | — | 상호작용 없음 |
| FS-003-EL-011.9 | FS-003-SEC-04 | 활동 셀 (글·댓글·구매평·문의) | 텍스트 | 4개 카운트를 '글/ 댓글/ 구매평/ 문의' 순서로 `0/ 0/ 0/ 0` 형식으로 이어 표시한다 | — | 헤더 라벨은 '글·댓글·구매평·문의' |
| FS-003-EL-011.10 | FS-003-SEC-04 | 누적 구매금액 셀 | 텍스트 | 누적 구매금액을 천 단위 구분 기호를 붙여 우측 정렬로 표시한다 | — | 상호작용 없음 |
| FS-003-EL-011.11 | FS-003-SEC-04 | 메모 아이콘(연필) | 링크 | 클릭 시 `/users/members/<회원 id>` 로 이동한다. 메모 편집은 상세 화면의 '관리자 메모' 카드(FS-004-EL-009)가 담당하며, 목록에서는 편집하지 않는다 | — | 접근 가능한 이름 '<닉네임> 관리자 메모'. 마우스 hover 시 메모 본문을, 메모가 비어 있으면 '메모 없음' 을 title 로 보여준다 |
| FS-003-EL-011.12 | FS-003-SEC-04 | 행 ⋯ 액션 메뉴 | 드롭다운 | 트리거(⋯) 클릭 또는 ↓ 키로 열린다. 열리면 첫 항목으로 포커스가 이동한다. ↑↓·Home·End 로 항목을 이동하고, Esc·바깥 클릭·Tab 아웃으로 닫힌다. Esc 로 닫으면 포커스가 트리거로 복귀한다 | — | 접근 가능한 이름 '<닉네임> 회원 액션'. 항목은 **2개뿐**이다 (요구사항) |
| FS-003-EL-011.12.1 | FS-003-SEC-04 | ⋯ 메뉴 '회원 삭제' | 버튼 | 클릭 시 메뉴를 닫고 FS-003-SEC-07(삭제 확인 다이얼로그)을 해당 회원 대상으로 연다. 이 항목 자체는 삭제하지 않는다 | O | 파괴적 액션 — 붉은 텍스트. 실제 삭제 호출은 FS-003-EL-017.4 |
| FS-003-EL-011.12.2 | FS-003-SEC-04 | ⋯ 메뉴 '알림 발송' | 버튼 | 클릭 시 메뉴를 닫고 확인 절차 없이 해당 회원에게 알림 발송을 요청한다. **발송 중에는 그 행의 항목 라벨이 '발송 중…' 으로 바뀌고 비활성된다** — 같은 회원에게 두 번 보내지 않는다(진행 중인 회원 id 집합으로 행마다 독립 판정하므로, 다른 행은 계속 발송할 수 있다). 성공하면 FS-003-EL-002 로 '<닉네임> 회원에게 알림을 발송했습니다.', 실패하면 '<닉네임> 회원에게 알림을 발송하지 못했습니다. 잠시 후 다시 시도해 주세요.' 를 '다시 시도'(FS-003-EL-002.2)와 함께 표시한다 | O | 발송할 알림의 내용·채널을 고르는 UI 는 구현에 없다 |
| FS-003-EL-011.13 | FS-003-SEC-04 | 행 전체 클릭 이동 | 텍스트 | 행의 빈 영역을 클릭하면 `/users/members/<회원 id>` 로 이동한다. 단, 클릭 지점이 `a` · `button` · `input` · `select` · `textarea` · `label` · `role="menu"` · `role="menuitem"` 중 하나에 속하면(= 체크박스 · 메모 아이콘 · ⋯ 버튼 · 닉네임 링크) 이동하지 않고 그 요소의 동작만 수행한다. 셀 텍스트를 드래그로 선택 중이면 이동하지 않는다 | — | 비표시 요소(행 동작 규칙). 구현: `apps/admin/src/shared/useRowNavigation.ts`. `<tr>` 에 tabIndex·role 을 주지 않는다 — 키보드 경로는 FS-003-EL-011.3 이 담당한다 |
| FS-003-EL-012 | FS-003-SEC-04 | 목록 로딩 스켈레톤 | 스켈레톤 | 조회 중에는 표 본문을 10행 × 11열의 스켈레톤 막대로 대체한다. 헤더·툴바·필터·페이지네이션은 잠기지 않으며 계속 조작할 수 있다 | — | 비표시 요소. 조작하면 진행 중이던 요청이 취소되고 최신 조건으로 다시 조회한다 |
| FS-003-EL-013 | FS-003-SEC-04 | 검색 결과 없음 빈 상태 | 빈상태 | 조회가 끝났고 결과가 0건이면 표 본문 전체를 병합한 셀에 '검색 결과가 없습니다.' 를 가운데 정렬로 표시한다 | — | 비표시 요소. 필터 초기화 버튼은 구현에 없다 — 사용자는 FS-003-EL-003.1 / FS-003-EL-004.1 / FS-003-EL-008 로 조건을 되돌린다 |
| FS-003-EL-014 | FS-003-SEC-04 | 조회 실패 배너 | 배너 | 목록 조회가 실패하면 조회 요약·표·페이지네이션을 전부 감추고 그 자리에 위험 톤(`tone="danger"`, `role="alert"`) 배너를 표시한다. 문구는 '회원 목록을 불러오지 못했습니다.' 이다 | O | 비표시 요소. 좌측 필터·툴바는 계속 보이며 조작할 수 있다 |
| FS-003-EL-014.1 | FS-003-SEC-04 | 조회 실패 배너 '다시 시도' 버튼 | 버튼 | 클릭 시 **마지막 조회 조건(등급·그룹·키워드·페이지)을 그대로** 다시 조회한다. 재조회가 시작되면 배너가 사라지고 FS-003-EL-012(스켈레톤)가 표시된다 | O | 비표시 요소 |
| FS-003-EL-015 | FS-003-SEC-05 | 페이지네이션 | 페이지네이션 | 페이지당 10건 기준으로 총 페이지 수를 계산한다. 총 페이지가 1 이하면 이 영역 전체가 렌더되지 않는다. 번호는 현재 페이지가 가운데 오도록 최대 5개만 노출하며, 양 끝에서는 창이 끝에 붙는다 | O | `aria-label="회원 목록 페이지"`. 페이지를 바꾸면 행 선택이 전부 해제된다 |
| FS-003-EL-015.1 | FS-003-SEC-05 | '이전 페이지' 버튼 | 버튼 | 클릭 시 페이지를 1 줄이고 재조회한다. 1페이지에서는 비활성된다 | O | 접근 가능한 이름 '이전 페이지' |
| FS-003-EL-015.2 | FS-003-SEC-05 | 페이지 번호 버튼(최대 5개) | 버튼 | 클릭 시 해당 페이지로 이동해 재조회한다. 현재 페이지 버튼은 `aria-current="page"` 이며 채워진 배경으로 구분된다 | O | 스크린 리더에는 현재 페이지 앞에 '현재 페이지, ' 가 읽힌다 |
| FS-003-EL-015.3 | FS-003-SEC-05 | '다음 페이지' 버튼 | 버튼 | 클릭 시 페이지를 1 늘리고 재조회한다. 마지막 페이지에서는 비활성된다 | O | 접근 가능한 이름 '다음 페이지' |
| FS-003-EL-016 | FS-003-SEC-06 | 새 그룹 만들기 모달 | 모달 | FS-003-EL-005 를 눌렀을 때만 열린다. `role="dialog"` + `aria-modal="true"`. 열리면 포커스가 그룹명 입력으로 이동하고, Tab·Shift+Tab 이 모달 안에서 순환한다(포커스 트랩). Esc 키 · 딤 배경 클릭 · 닫기(×) · 취소 **4경로가 모두 미저장 가드(FS-003-EL-016.12)를 지난다** — 입력이 없으면 즉시 닫히고, 있으면 확인을 세운다. 닫히면 포커스가 FS-003-EL-005 로 복귀한다. 열려 있는 동안 배경 스크롤이 잠긴다. Enter 키는 제출(FS-003-EL-016.11)로 동작한다 | — | 비표시 요소. `document.body` 로 portal 렌더. 껍데기는 `@tds/ui` 의 `Modal`(`shared/ui` 가 재수출 — `shared/ui/index.ts:72`) |
| FS-003-EL-016.1 | FS-003-SEC-06 | 모달 제목 '새 그룹 만들기' | 텍스트 | 다이얼로그의 `aria-labelledby` 대상이다. 상호작용이 없다 | — | 비표시 요소 |
| FS-003-EL-016.2 | FS-003-SEC-06 | 모달 닫기(×) 버튼 | 버튼 | 클릭 시 미저장 가드(FS-003-EL-016.12)를 지난다 — 입력이 없으면 즉시 닫고, 있으면 확인을 세운다 | — | 비표시 요소. 접근 가능한 이름 '닫기' |
| FS-003-EL-016.3 | FS-003-SEC-06 | 그룹명 입력 | 입력 | `type="text"`, placeholder '예: 무료 배송 결제 그룹'. **필수**이며 앞뒤 공백을 제거한 뒤 검증한다. 모달이 열릴 때 이 입력이 첫 포커스를 받는다 | — | 비표시 요소. 최대 30자(제출 시 검증) |
| FS-003-EL-016.4 | FS-003-SEC-06 | 그룹 유형 선택 | 입력 | `select`. 선택지는 '일반 회원 그룹'(기본값) / '운영진 그룹' 2개다 | — | 비표시 요소. 운영진 그룹은 `/users/admins` 가 소비하며 회원 상세에는 노출되지 않는다 |
| FS-003-EL-016.5 | FS-003-SEC-06 | 그룹 유형 ⓘ 도움말 | 버튼 | 클릭 시 설명 문단을 펼치고, 다시 클릭하면 접는다(`aria-expanded` 토글). 문구: '일반 회원 그룹은 회원 목록에서 필터로 쓰입니다. 운영진 그룹은 관리자 화면(관리자 관리)에서만 쓰이며, 회원 상세에는 나타나지 않습니다.' | — | 비표시 요소. hover 툴팁이 아니라 버튼 토글 방식이다(키보드·터치에서 열린다). 접근 가능한 이름 '그룹 유형 설명' |
| FS-003-EL-016.6 | FS-003-SEC-06 | 배송비 혜택 선택 | 입력 | `select`. 선택지는 '없음'(기본값) / '무료 배송' / '조건부 무료 배송' 3개다 | — | 비표시 요소 |
| FS-003-EL-016.7 | FS-003-SEC-06 | 배송비 혜택 ⓘ 도움말 | 버튼 | 클릭 시 설명 문단을 펼치고, 다시 클릭하면 접는다. 문구: '이 그룹에 속한 회원의 주문에 적용할 배송비 정책입니다. 조건부 무료 배송은 상품 관리의 배송 정책에서 기준 금액을 따릅니다.' | — | 비표시 요소. 접근 가능한 이름 '배송비 혜택 설명' |
| FS-003-EL-016.8 | FS-003-SEC-06 | 모달 에러 문구 | 텍스트 | 유효성 위반 또는 생성 실패 시 폼 하단에 `role="alert"` 문단으로 1건만 표시된다. 표시되는 동안 그룹명 입력의 테두리가 위험색으로 바뀌고 `aria-invalid="true"` 가 된다 | — | 비표시 요소. 문구는 §4 FS-003-EL-016.3 / FS-003-EL-016.11 참조 |
| FS-003-EL-016.9 | FS-003-SEC-06 | 모달 안내문 | 텍스트 | '그룹을 만들어도 회원이 자동으로 들어가지는 않습니다. 회원은 회원가입으로만 유입되며, 그룹 배정은 별도 운영 정책을 따릅니다.' 를 상시 표시한다 | — | 비표시 요소 |
| FS-003-EL-016.10 | FS-003-SEC-06 | 모달 '취소' 버튼 | 버튼 | 클릭 시 미저장 가드(FS-003-EL-016.12)를 지난다 — 닫기(×)·Esc·딤과 **같은** `requestClose` 를 쓴다(`CreateGroupModal.tsx:165`). 생성 요청 중에는 비활성된다 | — | 비표시 요소 |
| FS-003-EL-016.11 | FS-003-SEC-06 | '그룹 만들기' 버튼 | 버튼 | 클릭(또는 폼 Enter) 시 그룹명·그룹 유형·배송비 혜택을 서버로 보내 그룹 생성을 요청한다. 성공하면 모달을 닫고 FS-003-EL-002 로 `'<그룹명>' 그룹을 만들었습니다.` 를 표시한 뒤 그룹 목록(FS-003-EL-004)을 다시 조회한다 | O | 비표시 요소. 요청 중 라벨이 '만드는 중…' 으로 바뀌고 취소·제출 버튼이 모두 비활성된다 |
| FS-003-EL-016.12 | FS-003-SEC-06 | 모달 미저장 이탈 가드 | 모달 | 닫기 4경로(Esc · 딤 클릭 · × · 취소)가 전부 이 가드를 지난다(`useModalDirtyGuard` — `CreateGroupModal.tsx:93`). **손대지 않은 모달은 프롬프트 없이 즉시 닫힌다**(확인 피로 방지). 입력이 있고 저장 중이 아니면(`isDirty && !saving`) 닫는 대신 확인 다이얼로그를 세운다 — 제목 '저장하지 않은 변경 사항이 있습니다', 본문 '입력한 내용이 사라집니다. 저장하지 않고 닫으시겠습니까?'. '확인'이면 닫고, '취소'면 모달에 머무르며 입력이 보존된다(이때 토스트를 띄우지 않는다 — 작업 취소가 아니라 '머무르기'다). 가드는 소모되지 않는다 — 취소 후 다시 닫으려 하면 확인이 다시 뜬다 | — | 비표시 요소. 확인 다이얼로그는 모달 **밖**에 렌더된다(`CreateGroupModal.tsx:284`) — 안에 두면 모달의 포커스 트랩이 그것을 가둔다. ⚠ **DS Modal 의 latch 결함으로 이 가드의 '취소' 경로가 모달을 영구히 닫히지 않게 만든다 — §7 미결 #7** |
| FS-003-EL-017 | FS-003-SEC-07 | 회원 삭제 확인 다이얼로그 | 모달 | FS-003-EL-011.12.1 을 골랐을 때만 열린다. `role="dialog"` + `aria-modal="true"`, 제목은 '회원 삭제'. 포커스 트랩·Esc·딤 클릭·포커스 복귀·배경 스크롤 잠금은 FS-003-EL-016 과 동일하다. **삭제 요청 중에도 취소·Esc·딤 클릭은 살아 있으며, 닫으면 진행 중이던 요청을 `AbortController` 로 취소한다** — 취소된 요청의 응답은 화면에 반영되지 않는다(실패 배너도 뜨지 않는다) | — | 비표시 요소. 구현: `shared/ui/ConfirmDialog.tsx` |
| FS-003-EL-017.1 | FS-003-SEC-07 | 확인 다이얼로그 본문 문구 | 텍스트 | '<닉네임>(<계정>) 회원을 삭제합니다. 이 작업은 되돌릴 수 없습니다.' 를 표시한다 | — | 비표시 요소 |
| FS-003-EL-017.2 | FS-003-SEC-07 | 확인 다이얼로그 닫기(×) 버튼 | 버튼 | 클릭 시 삭제하지 않고 다이얼로그를 닫는다 | — | 비표시 요소. 접근 가능한 이름 '닫기' |
| FS-003-EL-017.3 | FS-003-SEC-07 | 확인 다이얼로그 '취소' 버튼 | 버튼 | 클릭 시 삭제하지 않고 다이얼로그를 닫는다. **삭제 요청 중에도 비활성되지 않는다** — 이 버튼이 진행 중인 요청의 취소(abort) 경로이기 때문이다. 느린 요청에 사용자를 가두지 않는다. 누르면 '작업이 취소되었습니다' 토스트가 뜬다 | — | 비표시 요소 |
| FS-003-EL-017.4 | FS-003-SEC-07 | 확인 다이얼로그 '회원 삭제' 버튼 | 버튼 | 클릭 시 해당 회원의 삭제를 서버에 요청한다. 성공하면 다이얼로그를 닫고 FS-003-EL-002 로 '<닉네임> 회원을 삭제했습니다.' 를 표시한 뒤 목록을 현재 조건 그대로 재조회한다. **실패하면 다이얼로그를 닫지 않고 FS-003-EL-017.5 배너로 알리며, 이 버튼을 다시 활성화한다 — 재클릭이 곧 재시도다** | O | 비표시 요소. 위험 스타일. 요청 중 라벨이 '처리 중…' 으로 바뀌고 `aria-busy="true"` + 비활성된다(중복 클릭 차단). 취소 버튼은 잠기지 않는다(FS-003-EL-017.3) |
| FS-003-EL-017.5 | FS-003-SEC-07 | 확인 다이얼로그 실패 배너 | 배너 | 삭제 요청이 실패하면 다이얼로그 본문 문구 아래에 위험 톤 배너(`tone="danger"`, `role="alert"`)로 표시한다. 문구 — 1명: '회원을 삭제하지 못했습니다. 잠시 후 다시 시도해 주세요.' / N명(일괄): '회원 N명 중 M명을 삭제하지 못했습니다. 잠시 후 다시 시도해 주세요.' 복구 경로는 FS-003-EL-017.4 재클릭 또는 취소다 | — | 비표시 요소. **토스트가 아니라 다이얼로그 안 배너인 이유**: 모달이 떠 있는 동안 토스트는 시선 밖이고, 모달이 닫히면 함께 사라진다(`shared/ui/README.md`) |

## 4. 예외 명세

| 요소번호 | 빈 상태 | 로딩 | 실패 | 유효성 | 권한없음 | 경합 | 대량 |
|---|---|---|---|---|---|---|---|
| FS-003-EL-001 | N/A — 조회 데이터와 무관한 정적 배너. 회원 0건이어도 동일하게 표시된다 | N/A — 조회를 하지 않는다. 목록 조회 중에도 그대로 표시된다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 (요소 단위 권한 분기 없음) | N/A — 저장하는 상태가 없다 | N/A — 회원 수와 무관한 고정 배너 |
| FS-003-EL-001.1 | N/A — 링크 대상은 회원 데이터와 무관하다 | N/A — 조회하지 않는다 | N/A — 라우터 내부 이동이며 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용. `/marketing/templates` 는 현재 준비 중 화면으로 연결된다 | N/A — 저장하는 상태가 없다 | N/A — 데이터를 다루지 않는다 |
| FS-003-EL-001.2 | N/A — 데이터와 무관하다 | N/A — 즉시 동작한다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 상태가 이 브라우저 탭 안에서만 유지된다 | N/A — 데이터를 다루지 않는다 |
| FS-003-EL-002 | N/A — 표시할 메시지가 없으면 토스트가 렌더되지 않는다(이것이 기본 상태) | 진행 중에는 표시되지 않는다. 작업이 끝난 뒤에만 나타난다 | 내보내기·알림 발송 실패는 위험 톤 + `role="alert"` 토스트로 표시되며 **자동으로 사라지지 않는다** — 닫거나 '다시 시도'(FS-003-EL-002.2)를 눌러야 사라진다. 회원 삭제 실패만 예외로 다이얼로그 안 배너(FS-003-EL-017.5)가 맡는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 여러 작업이 연속되면 토스트가 뷰포트에 쌓인다. 성공 토스트는 4초 뒤 스스로 사라지고 실패 토스트는 남으므로, 실패가 성공에 밀려 사라지지 않는다 | N/A — 토스트 1건이 작업 1건에 대응한다 |
| FS-003-EL-002.1 | N/A — 토스트가 없으면 이 버튼도 없다 | N/A — 즉시 동작한다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 데이터를 다루지 않는다 |
| FS-003-EL-002.2 | N/A — 실패 토스트가 없으면 이 버튼도 없다 | 재시도가 시작되면 토스트가 닫히고, 재시도 대상 작업의 로딩 표시(내보내기 버튼 '내보내는 중…' · 행 메뉴 '발송 중…')가 다시 켜진다 | 재시도가 다시 실패하면 같은 실패 토스트가 다시 뜬다. 재시도 횟수 상한이 없다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 재시도는 실패한 작업을 **같은 인자로** 다시 실행한다 — 새 조건으로 실행하지 않는다 | 성공 토스트에는 이 버튼이 붙지 않는다 |
| FS-003-EL-003 | 회원이 0건이면 4개 항목이 모두 건수 0 으로 표시된다. 항목 자체는 사라지지 않는다 | 첫 조회가 끝나기 전에는 4개 항목의 건수 자리에 '—' 를 표시한다. 버튼은 잠기지 않는다 | 조회 실패 시 건수 자리가 '—' 로 유지된다. 필터 버튼은 계속 조작할 수 있고, 조작하면 재조회한다 | N/A — 자유 입력이 아니다(고정 4개 항목) | §4.1 공통 규칙 적용 | 다른 관리자가 회원을 추가·삭제해도 건수는 재조회 시점에만 갱신된다. 충돌 감지는 하지 않는다 | 항목이 4개로 고정이라 늘어나지 않는다 |
| FS-003-EL-003.1 | 회원 0건이면 배지에 '0' 을 표시한다 | 건수 미도착 시 배지에 '—'. 버튼은 조작 가능 | 조회 실패 시 배지 '—' 유지. 클릭하면 재조회를 시도한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-003 과 동일 | N/A — 단일 버튼이다 |
| FS-003-EL-003.2 | 해당 등급 0건이면 배지 '0' 을 표시하고, 선택 시 FS-003-EL-013(빈 상태)이 표시된다 | 건수 미도착 시 배지에 '—'. 버튼은 조작 가능 | 조회 실패 시 배지 '—' 유지. 클릭하면 재조회를 시도한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-003 과 동일 | N/A — 단일 버튼이다 |
| FS-003-EL-003.3 | 해당 등급 0건이면 배지 '0', 선택 시 FS-003-EL-013 이 표시된다 | 건수 미도착 시 배지에 '—'. 버튼은 조작 가능 | 조회 실패 시 배지 '—' 유지. 클릭하면 재조회를 시도한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-003 과 동일 | N/A — 단일 버튼이다 |
| FS-003-EL-003.4 | 해당 등급 0건이면 배지 '0', 선택 시 FS-003-EL-013 이 표시된다 | 건수 미도착 시 배지에 '—'. 버튼은 조작 가능 | 조회 실패 시 배지 '—' 유지. 클릭하면 재조회를 시도한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-003 과 동일 | N/A — 단일 버튼이다 |
| FS-003-EL-004 | 그룹이 0개면 '전체 그룹' 항목만 남는다. 목록은 사라지지 않는다 | 그룹 목록이 도착하기 전에는 '전체 그룹' 항목만 렌더되고, 건수 자리에는 '—' 를 표시한다 | 그룹 목록 조회가 실패하면 '전체 그룹' 항목만 남고, 그룹 필터에 **전용 재시도 경로**가 열린다(목록 조회와 별개의 요청이므로 목록은 계속 보인다). 새 그룹 생성 성공 시에도 다시 조회된다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 다른 관리자가 그룹을 만들어도 이 화면은 자동 갱신되지 않는다. 이 브라우저에서 그룹을 만들었을 때만 재조회한다 | 그룹 수 상한이 구현에 없다. 그룹이 많아지면 사이드바가 세로로 늘어난다 — §7 미결 사항 |
| FS-003-EL-004.1 | N/A — 항상 존재하는 고정 항목이다 | 그룹 목록 도착 전에도 즉시 조작할 수 있다 | 그룹 목록 조회가 실패해도 이 항목은 남고 동작한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-004 와 동일 | N/A — 단일 버튼이다. 건수 배지가 없다 |
| FS-003-EL-004.2 | 그룹이 0개면 이 항목이 하나도 렌더되지 않는다 | 그룹 목록 도착 전에는 렌더되지 않는다. 건수만 미도착이면 배지에 '—' | 목록 조회가 실패하면 배지가 '—' 로 유지된다. 클릭하면 재조회를 시도한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 다른 관리자가 삭제한 그룹을 고르면 조회 결과가 0건이 되어 FS-003-EL-013 이 표시된다. 충돌 감지는 하지 않는다 | 위 FS-003-EL-004 와 동일 |
| FS-003-EL-005 | N/A — 데이터와 무관하게 항상 표시된다 | N/A — 서버를 호출하지 않고 모달만 연다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 단일 버튼이다 |
| FS-003-EL-006 | N/A — 데이터와 무관한 정적 문구다 | N/A — 조회하지 않는다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 고정 문구다 |
| FS-003-EL-007 | 두 조건을 만족하는 회원이 0건이면 FS-003-EL-013(빈 상태)이 표시된다. 좌측 건수 배지는 전체 기준이라 0 이 아닐 수 있다 | 조건을 바꾸면 진행 중이던 조회가 취소되고 최신 조건으로 다시 조회한다. 필터는 잠기지 않는다 | 조회 실패 시 FS-003-EL-014 로 대체 렌더된다 | N/A — 자유 입력이 아니다(버튼 선택 조합) | §4.1 공통 규칙 적용 | 조건 조합은 조회 시점의 서버 상태만 반영한다. 충돌 감지는 하지 않는다 | 조합 결과가 10건을 넘으면 FS-003-EL-015(페이지네이션)로 나눈다 |
| FS-003-EL-008 | 키워드에 걸리는 회원이 0건이면 FS-003-EL-013(빈 상태)이 표시된다 | 250ms 디바운스로 확정된 키워드마다 1회 조회한다. 조회 중 키워드가 바뀌면 이전 요청을 abort 하고 최신 요청만 반영한다. 입력은 잠기지 않는다 | 조회 실패 시 FS-003-EL-014 로 대체 렌더된다. 입력값은 유지되며 FS-003-EL-014.1 로 같은 키워드를 재조회한다 | 자유 텍스트이며 길이 상한·문자 제약이 구현에 없다. 앞뒤 공백은 조회 시 제거된다. 빈 문자열이면 키워드 조건을 걸지 않는다. 검증 실패 문구가 없다 | §4.1 공통 규칙 적용 | 조회 시점의 서버 상태만 반영한다. 충돌 감지는 하지 않는다 | 매치 건수가 10건을 넘으면 FS-003-EL-015 로 나눈다. 클라이언트 상한은 없다 |
| FS-003-EL-009 | 조건에 걸린 회원이 0건이면 헤더 행만 든 CSV 를 내려받고 '회원 0명을 CSV 로 내보냈습니다.' 를 표시한다 | 요청 중 라벨이 '내보내는 중…' 으로 바뀌고 버튼이 비활성된다 — 중복 제출이 차단된다. 표·필터는 잠기지 않는다 | 실패 시 FS-003-EL-002 실패 토스트에 '내보내기에 실패했습니다. 잠시 후 다시 시도해 주세요.' 를 '다시 시도'(FS-003-EL-002.2)와 함께 표시하고 버튼을 다시 활성화한다. 파일은 내려받지 않는다. 복구 경로는 토스트의 '다시 시도' 또는 버튼 재클릭이다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 내보내는 순간의 서버 스냅샷을 받는다. 다른 관리자가 동시에 바꾼 내용은 다음 내보내기에 반영된다. 충돌 감지는 하지 않는다 | **현재 페이지가 아니라 조건에 걸린 전체 행**을 1회 요청으로 받아 브라우저 메모리에서 CSV 를 만든다. 행 수 상한·분할 다운로드가 구현에 없다 — §7 미결 사항 |
| FS-003-EL-010 | 회원 0건이면 '전체 0명' 을 표시한다 | 조회 중에는 '불러오는 중…' 을 표시한다. 상호작용이 없어 잠글 대상이 없다 | 조회 실패 시 FS-003-EL-014 가 이 텍스트를 포함한 본문 전체를 대체하므로 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다른 관리자가 회원을 추가·삭제해도 재조회 시점에만 갱신된다 | 천 단위 구분 기호를 붙여 표시한다. 자릿수 상한이 없다 |
| FS-003-EL-011 | 0건이면 FS-003-EL-013(빈 상태)으로 본문이 대체된다 | FS-003-EL-012(스켈레톤)로 본문이 대체되고 `aria-busy="true"` 가 된다 | FS-003-EL-014(실패 배너)로 표 전체가 대체된다 | N/A — 표 자체에 입력이 없다(하위 체크박스는 각 행 참조) | §4.1 공통 규칙 적용 | 다른 관리자가 삭제한 회원의 행을 클릭하면 상세에서 '회원을 찾을 수 없습니다.'(FS-004-EL-012)를 만난다. 목록은 자동 갱신되지 않는다 | 페이지당 10건 고정. 서버 페이징. 가상 스크롤은 없다 |
| FS-003-EL-011.1 | 현재 페이지 행이 0건이면 체크되지 않고 indeterminate 도 아니다. 클릭해도 선택되는 행이 없다 | 조회 중에는 헤더가 유지되며 이 체크박스도 조작할 수 있다. 다만 본문이 스켈레톤이라 선택 대상이 없다 | 조회 실패 시 표 전체가 대체되어 표시되지 않는다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 선택은 화면 로컬 상태다. 필터·검색·페이지가 바뀌면 전부 해제된다. 서버와 동기화하지 않는다 | **현재 페이지의 10건까지만** 선택한다. 조건에 걸린 전체를 한 번에 선택하는 기능은 구현에 없다 — §7 미결 사항 |
| FS-003-EL-011.2 | N/A — 행이 없으면 이 체크박스도 없다 | 조회 중에는 행이 스켈레톤이라 존재하지 않는다 | 조회 실패 시 표 전체가 대체되어 표시되지 않는다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 선택은 화면 로컬 상태이며 서버와 동기화하지 않는다 | 페이지당 최대 10개 존재한다 |
| FS-003-EL-011.3 | N/A — 행이 없으면 링크도 없다 | 조회 중에는 표시되지 않는다 | 이동 후 상세 조회가 실패하면 FS-004-EL-011(상세 조회 실패 배너)이 처리한다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다른 관리자가 삭제한 회원이면 상세에서 '회원을 찾을 수 없습니다.' 를 만난다 | N/A — 행마다 1개다 |
| FS-003-EL-011.4 | N/A — 행이 없으면 셀도 없다 | 조회 중에는 스켈레톤 막대로 대체된다 | 조회 실패 시 표 전체가 대체되어 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 조회 시점의 값을 그대로 보여준다. 충돌 감지는 하지 않는다 | 값이 길면 줄바꿈하지 않고 가로로 늘어난다(표가 가로 스크롤된다) |
| FS-003-EL-011.5 | N/A — 행이 없으면 셀도 없다 | 조회 중에는 스켈레톤 막대로 대체된다 | 조회 실패 시 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 조회 시점의 값을 보여준다 | 값이 3종(일반회원·VIP·VVIP)으로 고정이다 |
| FS-003-EL-011.6 | 그룹이 없는 회원은 '그룹 없음' 으로 표시된다 | 조회 중에는 스켈레톤 막대로 대체된다 | 조회 실패 시 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다른 관리자가 그룹명을 바꿔도 재조회 시점에만 갱신된다 | 그룹명이 길면 줄바꿈하지 않고 가로로 늘어난다 |
| FS-003-EL-011.7 | N/A — 행이 없으면 셀도 없다 | 조회 중에는 스켈레톤 막대로 대체된다 | 날짜 문자열을 해석할 수 없으면 원본 문자열을 그대로 표시한다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 조회 시점의 값을 보여준다 | 표기 규칙이 고정이라 데이터가 많아져도 셀 폭이 변하지 않는다 |
| FS-003-EL-011.8 | 적립금이 0 이면 '0' 을 표시한다 | 조회 중에는 스켈레톤 막대로 대체된다 | 조회 실패 시 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 상세에서 적립금을 지급·차감해도 목록은 재조회 전까지 갱신되지 않는다 | 천 단위 구분 기호를 붙인다. 자릿수 상한이 없다 |
| FS-003-EL-011.9 | 활동이 전부 0 이면 '0/ 0/ 0/ 0' 을 표시한다 | 조회 중에는 스켈레톤 막대로 대체된다 | 조회 실패 시 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 조회 시점의 값을 보여준다 | 4개 숫자로 고정이다. 천 단위 구분 기호를 붙이지 않는다 |
| FS-003-EL-011.10 | 누적 구매금액이 0 이면 '0' 을 표시한다 | 조회 중에는 스켈레톤 막대로 대체된다 | 조회 실패 시 표시되지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 조회 시점의 값을 보여준다 | 천 단위 구분 기호를 붙인다. 자릿수 상한이 없다 |
| FS-003-EL-011.11 | 메모가 비어 있으면 아이콘은 그대로 표시되고 hover title 이 '메모 없음' 이 된다 | 조회 중에는 표시되지 않는다 | 목록 조회 실패 시 표시되지 않는다. 이동 후 상세 조회가 실패하면 FS-004-EL-011 이 처리한다 | N/A — 목록에서 메모를 편집하지 않는다 | §4.1 공통 규칙 적용 | 다른 관리자가 메모를 바꿔도 목록은 재조회 시점에만 갱신된다 | 메모가 길어도 목록에는 아이콘만 표시된다(본문은 hover title) |
| FS-003-EL-011.12 | N/A — 행이 없으면 메뉴도 없다 | 조회 중에는 표시되지 않는다. 메뉴 자체는 서버를 호출하지 않으므로 로딩 상태가 없다 | N/A — 여는 동작은 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 — 두 항목 모두 항상 표시되며 비활성되지 않는다 | N/A — 저장하는 상태가 없다 | 항목이 2개로 고정이다 |
| FS-003-EL-011.12.1 | N/A — 행이 없으면 항목도 없다 | 이 항목 자체는 즉시 다이얼로그를 연다. 로딩은 FS-003-EL-017.4 가 담당한다 | N/A — 이 항목은 서버를 호출하지 않는다. 실패 처리는 FS-003-EL-017.4 참조 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 실제 삭제 시점의 경합은 FS-003-EL-017.4 참조 | N/A — 행마다 1개다 |
| FS-003-EL-011.12.2 | N/A — 행이 없으면 항목도 없다 | 발송 요청 중에는 메뉴를 닫고, 그 행의 항목 라벨을 '발송 중…' 으로 바꾼 뒤 비활성한다 — 같은 회원에 대한 중복 클릭이 차단된다. 진행 판정은 회원 id 단위라 다른 행의 발송은 잠기지 않는다 | 실패 시 위험 톤 토스트(FS-003-EL-002)로 '<닉네임> 회원에게 알림을 발송하지 못했습니다. 잠시 후 다시 시도해 주세요.' 를 표시하고 '다시 시도'(FS-003-EL-002.2)를 붙인다. 토스트는 자동으로 사라지지 않는다. 실패해도 행·목록은 바뀌지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다른 관리자가 이미 삭제한 회원에게 발송해도 화면은 성공 안내를 표시한다. 충돌 감지는 하지 않는다 | 한 번에 1명에게만 발송한다. 여러 행 일괄 발송은 선택 체크박스 + 툴바의 일괄 액션이 맡는다 |
| FS-003-EL-011.13 | N/A — 행이 없으면 클릭 대상도 없다 | 조회 중에는 스켈레톤 행이라 이동 규칙이 걸리지 않는다 | N/A — 라우터 내부 이동이며 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다른 관리자가 삭제한 회원의 행을 클릭하면 상세에서 '회원을 찾을 수 없습니다.' 를 만난다 | N/A — 행 단위 규칙이다 |
| FS-003-EL-012 | N/A — 스켈레톤은 데이터 도착 전 상태이며 0건 여부를 아직 모른다 | 이것이 로딩 표현 자체다. 10행 × 11열 막대를 표시한다 | 조회가 실패하면 스켈레톤이 사라지고 FS-003-EL-014 로 바뀐다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 표시 전용이다 | 행 수가 페이지 크기(10)로 고정이라 실제 결과 건수와 무관하다 |
| FS-003-EL-013 | 이것이 빈 상태 표현 자체다. 표 본문 전체를 병합한 셀에 '검색 결과가 없습니다.' 를 표시한다 | 재조회가 시작되면 FS-003-EL-012(스켈레톤)로 바뀐다 | 조회가 실패하면 FS-003-EL-014 로 바뀐다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 표시 전용이다 | N/A — 항상 1행이다 |
| FS-003-EL-014 | N/A — 실패 상태에서는 건수를 알 수 없다 | 재시도가 시작되면 배너가 사라지고 FS-003-EL-012(스켈레톤)로 바뀐다 | 이것이 실패 표현 자체다. 서버 오류·네트워크 단절·타임아웃을 구분하지 않고 '회원 목록을 불러오지 못했습니다.' 1개 문구로 표시한다. 복구 경로는 FS-003-EL-014.1 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 — 권한 부족도 이 배너로 표시된다(별도 403 화면이 없다) | N/A — 표시 전용이다 | N/A — 표시 전용이다 |
| FS-003-EL-014.1 | N/A — 배너가 없으면 이 버튼도 없다 | 클릭하면 즉시 재조회가 시작되고 배너가 사라진다. 버튼이 사라지므로 중복 클릭이 발생하지 않는다 | 재시도가 다시 실패하면 같은 배너가 다시 표시된다. 재시도 횟수 상한이 없다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 재시도는 마지막 조회 조건을 그대로 재전송한다 | N/A — 단일 버튼이다 |
| FS-003-EL-015 | 총 건수가 10건 이하면 총 페이지가 1이 되어 이 영역이 렌더되지 않는다 | 조회 중에도 그대로 표시되며 조작할 수 있다. 조작하면 진행 중이던 요청이 취소되고 새 페이지를 조회한다 | 조회 실패 시 표와 함께 감춰지고 FS-003-EL-014 가 그 자리를 차지한다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 다른 관리자가 회원을 삭제해 총 페이지가 줄면, 현재 페이지가 범위를 벗어나 FS-003-EL-013(빈 상태)이 표시될 수 있다. 페이지를 자동 보정하지 않는다 — §7 미결 사항 | 번호를 최대 5개만 노출해 페이지 수가 많아도 줄이 넘치지 않는다 |
| FS-003-EL-015.1 | N/A — 영역이 렌더되지 않으면 이 버튼도 없다 | 클릭 시 스켈레톤이 표시되지만 이 버튼은 잠기지 않는다 | 조회 실패 시 감춰진다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-015 와 동일 | 1페이지에서는 비활성되어 범위를 벗어나지 않는다 |
| FS-003-EL-015.2 | N/A — 영역이 렌더되지 않으면 번호도 없다 | 클릭 시 스켈레톤이 표시되지만 번호 버튼은 잠기지 않는다 | 조회 실패 시 감춰진다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-015 와 동일 | 총 페이지가 5를 넘으면 현재 페이지 주변 5개만 노출한다 |
| FS-003-EL-015.3 | N/A — 영역이 렌더되지 않으면 이 버튼도 없다 | 클릭 시 스켈레톤이 표시되지만 이 버튼은 잠기지 않는다 | 조회 실패 시 감춰진다 | N/A — 자유 입력이 아니다 | §4.1 공통 규칙 적용 | 위 FS-003-EL-015 와 동일 | 마지막 페이지에서는 비활성되어 범위를 벗어나지 않는다 |
| FS-003-EL-016 | N/A — 조회 데이터와 무관한 폼 모달이다 | 그룹 생성 요청 중에도 모달은 열려 있고 Esc·딤 클릭으로 닫을 수 있다. **닫으면 언마운트 시 진행 중이던 생성 요청이 `AbortController` 로 취소된다**(`CreateGroupModal.tsx:84`) — 화면을 떠난 요청의 결과가 뒤늦게 배너로 튀어나오지 않는다. 저장 중에는 가드가 비켜서므로(`isDirty && !saving`) 확인 없이 즉시 닫힌다 | 생성 실패 시 모달을 유지하고 FS-003-EL-016.8 에 문구를 표시한다. 입력값은 보존된다 | N/A — 검증은 하위 입력 요소가 담당한다(FS-003-EL-016.3) | §4.1 공통 규칙 적용 — 모달을 여는 버튼이 숨거나 비활성되지 않는다 | N/A — 열기 자체는 상태를 저장하지 않는다 | N/A — 폼 필드가 3개로 고정이다 |
| FS-003-EL-016.1 | N/A — 정적 제목이다 | N/A — 조회하지 않는다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 고정 문구다 |
| FS-003-EL-016.2 | N/A — 모달이 없으면 이 버튼도 없다 | 생성 요청 중에도 비활성되지 않는다 — 닫으면 결과 배너 없이 모달이 사라지고 진행 중이던 요청은 abort 된다(FS-003-EL-016) | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 단일 버튼이다 |
| FS-003-EL-016.3 | 초기값은 빈 문자열이며 placeholder '예: 무료 배송 결제 그룹' 이 보인다 | 생성 요청 중에는 입력이 비활성된다(`disabled={saving}` — `CreateGroupModal.tsx:194`) — 요청 중에 값을 바꾸면 전송된 값과 화면 값이 어긋나므로 응답까지 잠근다 | 생성 실패 시 입력값이 보존되고 테두리가 위험색으로 바뀐다 | **검증 시점은 제출(FS-003-EL-016.11) 뿐이다** (blur 검증 없음). 앞뒤 공백 제거 후 빈 문자열이면 '그룹명을 입력하세요.' / 30자를 넘으면 '그룹명은 30자를 넘을 수 없습니다.' 를 FS-003-EL-016.8 위치(폼 하단)에 표시하고 이 입력으로 포커스를 옮긴다. 입력 중 글자 수를 잘라내지 않는다 | §4.1 공통 규칙 적용 | 다른 관리자가 같은 이름의 그룹을 먼저 만들어도 중복 검사를 하지 않는다 — §7 미결 사항 | 30자를 넘겨 붙여넣어도 입력은 허용되고 제출 시 차단된다 |
| FS-003-EL-016.4 | N/A — 선택지가 2개로 고정이며 기본값이 항상 있다 | 요청 중에는 비활성된다(`CreateGroupModal.tsx:218`) | N/A — 이 요소는 서버를 호출하지 않는다 | N/A — 자유 입력이 아니다. 알 수 없는 값이 오면 '일반 회원 그룹'으로 되돌린다 | §4.1 공통 규칙 적용 | N/A — 제출 전까지 서버 상태를 건드리지 않는다 | 선택지가 2개로 고정이다 |
| FS-003-EL-016.5 | N/A — 정적 도움말 문구다 | N/A — 조회하지 않는다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 고정 문구다 |
| FS-003-EL-016.6 | N/A — 선택지가 3개로 고정이며 기본값이 항상 있다 | 요청 중에는 비활성된다(`CreateGroupModal.tsx:244`) | N/A — 이 요소는 서버를 호출하지 않는다 | N/A — 자유 입력이 아니다. 알 수 없는 값이 오면 '없음'으로 되돌린다 | §4.1 공통 규칙 적용 | N/A — 제출 전까지 서버 상태를 건드리지 않는다 | 선택지가 3개로 고정이다 |
| FS-003-EL-016.7 | N/A — 정적 도움말 문구다 | N/A — 조회하지 않는다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 고정 문구다 |
| FS-003-EL-016.8 | N/A — 표시할 오류가 없으면 렌더되지 않는다(기본 상태) | 제출을 다시 누르면 기존 문구를 먼저 지운 뒤 검증한다 | 생성 실패 시 '그룹을 만들지 못했습니다. 잠시 후 다시 시도해 주세요.' 를 표시한다 | 유효성 위반 문구를 표시하는 자리다 — 문구는 FS-003-EL-016.3 참조 | §4.1 공통 규칙 적용 — 권한 부족도 생성 실패 문구로 표시된다 | N/A — 표시 전용이다 | 항상 1건만 표시한다(마지막 오류가 이전 오류를 덮어쓴다) |
| FS-003-EL-016.9 | N/A — 정적 안내문이다 | N/A — 조회하지 않는다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 고정 문구다 |
| FS-003-EL-016.10 | N/A — 모달이 없으면 이 버튼도 없다 | 생성 요청 중에는 비활성된다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 단일 버튼이다 |
| FS-003-EL-016.11 | N/A — 폼 필드가 항상 렌더된다 | 요청 중 라벨이 '만드는 중…' 으로 바뀌고 이 버튼과 취소 버튼이 비활성된다 — 중복 제출이 차단된다 | 실패 시 FS-003-EL-016.8 에 '그룹을 만들지 못했습니다. 잠시 후 다시 시도해 주세요.' 를 표시하고 버튼을 다시 활성화한다. 모달과 입력값은 유지된다. 복구 경로는 버튼 재클릭이다 | 제출 시 FS-003-EL-016.3 을 검증한다. 위반이면 서버를 호출하지 않고 문구만 표시한다 | §4.1 공통 규칙 적용 | 같은 이름의 그룹이 이미 있어도 클라이언트는 중복을 검사하지 않는다 — §7 미결 사항 | 생성 성공 시 그룹 목록을 전부 다시 조회한다. 그룹 수 상한이 없다 |
| FS-003-EL-016.12 | N/A — 모달이 없으면 가드도 없다. 입력이 없으면(pristine) 확인을 세우지 않고 즉시 닫는다 | 저장 중(`saving`)에는 가드가 비켜선다 — 닫기 버튼이 이미 잠겨 있고 곧 성공해서 닫히기 때문이다 | N/A — 서버를 호출하지 않는 로컬 판정이다 | N/A — 입력 요소가 아니다. `isDirty` 는 RHF 가 판정한다(초기값과 같아지면 다시 pristine 이다) | §4.1 공통 규칙 적용 | ⚠ **'취소'(머무르기)를 고르면 DS Modal 이 latch 된 채 남아 모달이 영구히 닫히지 않는다 — §7 미결 #7.** 가드 단독 계약('가드는 소모되지 않는다')은 `useModalDirtyGuard.test.tsx:89` 가 고정하지만, 그 테스트는 실제 `Modal` 이 아니라 대역 버튼을 쓰므로 이 결함을 잡지 못한다 | N/A — 확인 다이얼로그가 1개만 뜬다 |
| FS-003-EL-017 | N/A — 대상 회원이 있어야만 열린다 | 삭제 요청 중에도 다이얼로그는 열려 있고 취소·Esc·딤 클릭으로 닫을 수 있다. **닫으면 진행 중이던 요청이 abort 된다** — 취소된 요청의 응답은 반영되지 않고 실패 배너도 뜨지 않는다 | 삭제 실패 시 다이얼로그가 닫히지 않고 열린 채 남으며, FS-003-EL-017.5 배너로 실패를 알린다. 복구 경로는 FS-003-EL-017.4 재클릭 또는 취소다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 열기 자체는 상태를 저장하지 않는다 | 행 ⋯ 메뉴는 1명, 툴바 일괄 삭제(FS-003-EL-019)는 선택된 N명을 대상으로 같은 다이얼로그를 연다 |
| FS-003-EL-017.1 | N/A — 대상 회원이 있어야만 열린다 | N/A — 정적 문구다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다이얼로그를 연 시점의 닉네임·계정을 그대로 표시한다 | N/A — 고정 문구다 |
| FS-003-EL-017.2 | N/A — 다이얼로그가 없으면 이 버튼도 없다 | 삭제 요청 중에도 비활성되지 않는다 — 이 버튼이 진행 중 요청의 abort 경로다(FS-003-EL-017.3 과 동일) | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | N/A — 저장하는 상태가 없다 | N/A — 단일 버튼이다 |
| FS-003-EL-017.3 | N/A — 다이얼로그가 없으면 이 버튼도 없다 | 삭제 요청 중에도 비활성되지 않는다 — 누르면 진행 중이던 요청을 abort 하고 다이얼로그를 닫는다. 느린 요청에 사용자를 가두지 않기 위한 설계다 | N/A — 서버를 호출하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 취소로 abort 된 요청의 응답은 화면에 반영되지 않는다 — 늦게 도착한 성공이 목록을 건드리지 않는다 | N/A — 단일 버튼이다 |
| FS-003-EL-017.4 | N/A — 대상 회원이 있어야만 열린다 | 요청 중 라벨이 '처리 중…' 으로 바뀌고 이 버튼이 `aria-busy="true"` + 비활성된다 — 중복 제출이 차단된다. 취소 버튼은 잠기지 않는다(abort 경로) | 실패 시 다이얼로그가 열린 채 이 버튼이 다시 활성화되고, FS-003-EL-017.5 배너에 '회원을 삭제하지 못했습니다. 잠시 후 다시 시도해 주세요.'(일괄이면 실패 건수를 밝힌 문구)를 표시한다. 목록·선택 상태는 그대로다. 복구 경로는 버튼 재클릭(= 재시도) 또는 취소다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 다른 관리자가 이미 삭제한 회원이면 화면은 성공으로 처리하고 목록을 재조회한다(해당 행이 사라진다). 충돌 감지는 하지 않는다 | 행 ⋯ 메뉴는 1명, 일괄 삭제(FS-003-EL-019)는 선택된 N명을 대상으로 한다. **부분 실패는 실패로 처리한다** — 1명이라도 실패하면 다이얼로그를 닫지 않고 실패 건수를 밝힌다 |
| FS-003-EL-017.5 | N/A — 실패가 없으면 렌더되지 않는다(기본 상태) | 재시도를 시작하면 이 배너가 사라지고 확인 버튼이 '처리 중…' 으로 바뀐다 | 이것이 삭제 실패 표시 자체다. 다이얼로그 안에 위험 톤 배너로 남아 모달이 닫힐 때까지 보인다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 — 권한 부족도 이 배너로 표시된다 | 취소(abort)는 실패가 아니다 — 다이얼로그를 닫아 취소한 요청은 이 배너를 띄우지 않는다 | 일괄 삭제의 부분 실패는 '회원 N명 중 M명을 삭제하지 못했습니다.' 로 건수를 밝힌다 |
| FS-003-EL-018 | 선택된 행이 0건이면 이 버튼이 렌더되지 않는다 | 요청 중 라벨이 '발송 중…' 으로 바뀌고 이 버튼과 FS-003-EL-019 가 비활성된다 — 중복 제출이 차단된다. 표·필터는 잠기지 않는다 | 1명이라도 실패하면 FS-003-EL-002 로 '회원 N명 중 M명에게 알림을 발송하지 못했습니다. 잠시 후 다시 시도해 주세요.' 를 '다시 시도'와 함께 표시한다. 부분 성공을 성공으로 보고하지 않는다 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 발송은 선택된 회원마다 독립 요청이며 한 건의 실패가 나머지를 중단시키지 않는다(전원 정산 후 실패 건수를 보고한다) | 선택 상한은 현재 페이지 10건이다(FS-003-EL-011.1) |
| FS-003-EL-019 | 선택된 행이 0건이면 이 버튼이 렌더되지 않는다 | N/A — 서버를 호출하지 않고 다이얼로그만 연다. 로딩은 FS-003-EL-017.4 가 담당한다 | N/A — 이 버튼은 서버를 호출하지 않는다. 실패 처리는 FS-003-EL-017.4 참조 | N/A — 입력이 없다 | §4.1 공통 규칙 적용 | 일괄 발송이 진행 중이면 비활성된다 — 발송 중인 회원을 동시에 지우지 않는다 | 선택 상한은 현재 페이지 10건이다. 다이얼로그 문구가 '선택한 회원 N명을 삭제합니다.' 로 바뀐다 |

### 4.1 공통 예외 규칙

| 규칙 | 내용 |
|---|---|
| 네트워크 단절 | 조회 계열(목록·그룹 목록)은 실패로 처리되어 FS-003-EL-014(조회 실패 배너)로 대체 렌더된다. 쓰기 계열(그룹 생성·회원 삭제·알림 발송·내보내기)은 각 요소의 실패 처리를 따른다. 서버 오류·네트워크 단절·타임아웃을 구분하는 문구가 구현에 없다 |
| 세션 만료 | 세션 만료를 감지해 로그인으로 보내는 처리가 이 화면의 구현에 없다. 조회는 FS-003-EL-014 로, 쓰기는 각 요소의 실패 처리로 떨어진다 — §7 미결 사항 |
| 요청 타임아웃 | 프론트 타임아웃 상한이 구현에 없다. 요청은 응답 또는 abort 까지 대기한다. abort 는 조회 조건이 바뀌거나, 화면을 벗어나거나, **삭제 확인 다이얼로그를 닫을 때** 발생한다 — §7 미결 사항(상한 자체는 미정) |
| 중복 제출 | 그룹 생성(FS-003-EL-016.11) · 회원 삭제(FS-003-EL-017.4) · 내보내기(FS-003-EL-009) · 일괄 알림 발송(FS-003-EL-018)은 요청 중 해당 버튼이 비활성되어 차단된다. 행 단위 알림 발송(FS-003-EL-011.12.2)은 **진행 중인 회원 id 단위로** 그 행의 메뉴 항목이 '발송 중…' 으로 잠겨 차단된다 — 같은 회원에게 두 번 보내지 않되, 다른 행의 발송은 막지 않는다 |
| 실패 통지의 자리 | 실패를 어디에 띄우는지는 **사용자가 지금 무엇을 보고 있는가**로 정한다(`shared/ui/README.md`) — ① 쓰기 실패(내보내기·알림 발송)는 **토스트**(FS-003-EL-002): 자동으로 사라지지 않고 '다시 시도'가 붙는다 ② **조회** 실패는 **인라인 배너**(FS-003-EL-014): 화면이 비어 있고 할 일이 '다시 시도' 하나뿐이라 사라지면 안 된다 ③ 모달이 떠 있는 동안의 실패(회원 삭제)는 **다이얼로그 안 배너**(FS-003-EL-017.5): 토스트는 모달 뒤로 밀려 시선 밖이다 |
| 동시 조회 | 목록 조회는 동시에 1건만 유지된다. 조건이 바뀌면 진행 중이던 요청을 abort 하므로, 늦게 도착한 이전 응답이 최신 결과를 덮어쓰지 않는다 |
| 권한 없음 | **이 화면의 구현에는 역할 기반 분기가 없다.** 요소가 역할에 따라 숨거나 비활성되지 않으며, 전용 403 화면도 없다. 서버가 권한 부족을 응답하면 조회는 FS-003-EL-014 로, 쓰기는 각 요소의 실패 처리로 떨어진다. 은닉 정책(403 vs 404)은 백엔드 명세가 확정한다 — §7 미결 사항 |
| 행 선택의 수명 | 선택(FS-003-EL-011.1 · FS-003-EL-011.2)은 화면 로컬 상태이며 서버로 전송되지 않는다. 등급·그룹·키워드·페이지 중 하나라도 바뀌면 전부 해제된다 |

## 5. 서버 연동 지점

| 요소번호 | 이름 | 읽기/쓰기 | 필요 데이터 | 프론트 어댑터 (data-source.ts) | 비고 |
|---|---|---|---|---|---|
| FS-003-EL-003 / FS-003-EL-003.1 / FS-003-EL-003.2 / FS-003-EL-003.3 / FS-003-EL-003.4 | 등급 필터 | R | 등급 조건이 반영된 회원 목록 + 등급별 건수(전체·일반·VIP·VVIP) | `fetchMembers(query, signal)` | 등급별 건수는 **검색·그룹 조건과 무관한 전체 회원 기준**이다 |
| FS-003-EL-004 | 그룹 필터 (그룹 목록) | R | 그룹 id · 표시명 목록 | `fetchGroups(signal)` | 화면 진입 시 1회 + 그룹 생성 성공 시 재조회 |
| FS-003-EL-004.1 / FS-003-EL-004.2 | 그룹 필터 항목 (그룹별 건수 + 필터 조회) | R | 그룹 조건이 반영된 회원 목록 + 그룹별 건수 | `fetchMembers(query, signal)` | 그룹별 건수는 전체 회원 기준이다 |
| FS-003-EL-007 | 등급 × 그룹 AND 조합 | R | 두 조건을 AND 로 만족하는 회원 목록 + 총 건수 | `fetchMembers(query, signal)` | 조합 규칙은 서버 쿼리로 처리된다 |
| FS-003-EL-008 | 검색 입력 | R | 닉네임 또는 계정에 키워드가 부분 일치하는 회원 목록 + 총 건수 | `fetchMembers(query, signal)` | 대소문자를 구분하지 않는다. 250ms 디바운스 후 1회 조회 |
| FS-003-EL-009 | 내보내기 | R | 현재 등급·그룹·키워드 조건에 걸린 **전체** 회원 (페이지 미적용) | `fetchMembersForExport(query, signal)` + `toCsv(members)` | CSV 직렬화는 현재 프론트에서 수행한다. 필요 필드: 닉네임·계정·회원 유형·그룹·가입일·적립금·누적 구매금액 |
| FS-003-EL-010 | 조회 요약 텍스트 | R | 조건에 걸린 총 건수 | `fetchMembers(query, signal)` | 표와 같은 응답의 `total` 을 쓴다 |
| FS-003-EL-011 | 회원 목록 표 | R | 회원 배열(id·닉네임·계정·등급·그룹 id·그룹명·가입일시·적립금·활동 4종·누적 구매금액·메모) + 등급별 건수 + 그룹별 건수 + 총 건수 | `fetchMembers(query, signal)` | 페이지당 10건. 가입일 상대 시각 계산에 시·분이 필요하다 |
| FS-003-EL-011.12.1 | 행 ⋯ '회원 삭제' | W | 회원 id | `deleteMember(id)` | 이 요소는 다이얼로그만 연다. 실제 호출은 FS-003-EL-017.4 |
| FS-003-EL-011.12.2 | 행 ⋯ '알림 발송' | W | 회원 id | `sendNotification(id)` | 확인 절차 없이 즉시 요청한다. 알림 내용·채널을 고르는 입력이 없다. 진행 중인 회원 id 를 화면이 집합으로 들고 있어 그 행만 잠근다 |
| FS-003-EL-018 | 일괄 '알림 발송' | W | 선택된 회원 id 배열 | `sendNotification(id)` × N (회원마다 1건) | 회원마다 독립 요청이며 전원 정산 후 실패 건수를 보고한다 — 한 건의 실패가 나머지를 중단시키지 않는다 |
| FS-003-EL-019 | 일괄 '삭제' | W | 선택된 회원 id 배열 | `deleteMember(id)` × N | 이 요소는 다이얼로그만 연다. 실제 호출은 FS-003-EL-017.4. 1명이라도 실패하면 다이얼로그를 닫지 않는다 |
| FS-003-EL-014 / FS-003-EL-014.1 | 조회 실패 배너와 재시도 | R | 마지막 조회 조건(등급·그룹·키워드·페이지)을 그대로 재전송 | `fetchMembers(query, signal)` | 재시도는 새 요청을 만든다 |
| FS-003-EL-015 / FS-003-EL-015.1 / FS-003-EL-015.2 / FS-003-EL-015.3 | 페이지네이션 | R | 요청 페이지의 회원 배열 + 총 건수 | `fetchMembers(query, signal)` | 페이지 크기 10 고정(`PAGE_SIZE`) |
| FS-003-EL-016.11 | '그룹 만들기' 버튼 | W | 그룹명(문자열, 최대 30자) · 그룹 유형(`member` \| `staff`) · 배송비 혜택(`none` \| `free` \| `conditional`) | `createGroup(input, signal)` | 성공 후 `fetchGroups(signal)` 를 다시 호출한다 |
| FS-003-EL-017.4 | 삭제 확인 '회원 삭제' 버튼 | W | 회원 id | `deleteMember(id)` | 성공 후 목록을 현재 조건 그대로 재조회한다 |

> **현재 구현 상태 (백엔드 명세 참고)**: `data-source.ts` 의 쓰기 함수(`createGroup` · `deleteMember` · `sendNotification`)는 지연 후 resolve 만 하고 **아무것도 저장하지 않는다**(백엔드 미구현). 화면은 이 resolve 를 성공으로 간주해 안내 배너를 띄운다. 위 표는 백엔드 연결 후의 **의도된 동작**을 기술한 것이다.

## 6. 자기 점검 (제출 전 확인)

- [x] 구현 소스를 열어 화면 요소를 전수 대조했다 — 구현에 있는데 §3에 없는 요소 0건
- [x] **v1.1 재대조 (2026-07-15)** — 구현이 앞서 나간 요소를 §3/§4 에 반영했다: 일괄 액션 2종(EL-018 · EL-019) · 삭제 실패 배너(EL-017.5) · 실패 토스트의 '다시 시도'(EL-002.2) · 작업 결과가 배너가 아니라 **토스트**라는 사실(EL-002) · 행 단위 발송 잠금 · 다이얼로그 닫기의 요청 abort · 그룹 목록 전용 재시도. 결정이 끝난 항목은 §7 에서 §7.1(종결)로 옮겼다
- [x] 보이지 않는 요소(빈 상태·로딩·에러·모달·드롭다운·확인 다이얼로그·행 이동 규칙·필터 조합 규칙)에도 번호를 줬다
- [x] §3의 모든 요소가 §2의 SEC 중 하나에 소속된다 (고아 요소 0건)
- [x] 요소번호에 중복·재사용이 없다. 삭제된 요소가 없다 (v1.0 최초 발행)
- [x] §4 예외 7축에 빈칸 0건. 모든 `N/A` 에 사유가 붙어 있다
- [x] 모호어 0건 — G9 #10 금지어 목록(판정 불가 부사·조건어 6종)을 본문에서 1건도 쓰지 않았다
- [x] §3의 `[서버]` = O 요소가 §5에 전부 요약됐다
- [x] 엔드포인트·HTTP 메서드·에러코드·DB 스키마를 쓰지 않았다 (백엔드 명세의 영역)
- [ ] `confirmedAt` — 구현이 리포지토리에 커밋된 일자(2026-07-14)를 컨펌일로 기록했다. 오케스트레이터 의 Task Graph 기록과 대조가 필요하다

## 7. 미결 사항 (UI 기획 / 아키텍처 / 백엔드 명세 이관)

| # | 내용 | 이관 대상 |
|---|---|---|
| 1 | **대응 SCR 문서 부재** — `docs/plan/ui/` 에 회원 관리 SCR 이 없다. SCR-003 번호는 `SCR-003-product-registration.md` 가 점유 중이라 `FS 번호 = SCR 번호` 규칙(G9 #12)을 현재 만족할 수 없다 | UI 기획 (SCR 작성) / 아키텍처 (번호 판정) |
| 2 | 내보내기(FS-003-EL-009)에 행 수 상한이 없다 | UI 기획 / 백엔드 명세 |
| 3 | 그룹명 중복 검사 정책이 정해지지 않았다 | UI 기획 / 백엔드 명세 |
| 4 | 세션 만료 처리·요청 타임아웃 상한·권한 은닉 정책(403 vs 404)이 정해지지 않았다 | 백엔드 명세 (BE 명세) |
| 5 | 그룹 수 상한이 없어 사이드바 길이가 그룹 수에 비례해 늘어난다 | UI 기획 쪽 변경 요청 |
| 6 | 전체 선택(FS-003-EL-011.1)은 **현재 페이지 10건까지만** 선택한다. 조건에 걸린 전체를 한 번에 선택하는 경로가 없다 | UI 기획 쪽 변경 요청 |
| 7 | ★ **DS Modal 일방향 latch — 미저장 가드의 '취소' 경로가 모달을 영구히 닫히지 않게 만든다 (FEEDBACK-06 재판정 · 신규 발견 2026-07-17)**. 재현: FS-003-EL-016 에 값을 입력(dirty) → Esc → ① `Modal` 이 `closingRef=true, closing=true` 로 **latch**(`Modal.tsx:122-126`) → ② `--closing` 이 오버레이를 `pointer-events:none` 으로 만들고 dialog exit 애니메이션이 `forwards` 로 `opacity:0` 을 고정 → ③ `onAnimationEnd` 가 `onClose()` 발사(`Modal.tsx:216-218`) → ④ 그 `onClose` 는 가드의 `requestClose` 라 dirty 이므로 `setAsking(true)` 만 하고 **부모가 언마운트하지 않는다** → ⑤ 사용자가 '취소'(머무르기) 선택 → `setAsking(false)` → **종착: 모달은 마운트된 채 `closing` 이 true 로 남고, dialog 는 `opacity:0` 이라 보이지 않으며, 오버레이는 `pointer-events:none` 이고, 이후 모든 Esc/딤/× 는 `Modal.tsx:123` 에서 즉시 return 한다 — 영구히 닫히지 않고 보이지도 않으며 입력한 데이터가 갇힌다.** `closingRef` 를 되돌리는 코드가 어디에도 없다(`setClosing(false)`·리셋 effect 전무). reduced-motion 환경은 경로만 다르고(`Modal.tsx:129-132` 가 `onClose()` 를 동기 발사) 같은 latch 에 빠지며, 이때 dialog 는 *보이지만* 오버레이가 `pointer-events:none` 이고 latch 라 **보이는데 완전 무반응**이다. 근인: `Modal.tsx:19-25` 가 'onClose() → 부모가 언마운트' 를 설계 전제로 문서화하는데 dirty 가드가 그 전제를 깨고, 깨졌을 때 latch 를 되돌릴 신호가 없다(`onClose` 가 `void` 반환). **기존 테스트가 못 잡는 이유**: `useModalDirtyGuard.test.tsx` 의 `Harness`(`:17-31`)가 실제 `Modal` 이 아니라 평범한 `<button>` 으로 4경로를 *흉내* 내므로 `closingRef` 가 관여하지 않는다. 폭발 반경은 **거부(veto) 가능한 requestClose 를 Modal.onClose 로 넘기는 9곳** — `useModalDirtyGuard` 소비자 7곳(`CreateGroupModal:154` · `PasswordChangeModal:103` · `RoleFormModal:68` · `LogoFormModal:126` · `PortfolioCategoryFormModal:104` · `ProductCategoryFormModal:104` · `CategoryFormModal:106`) + 훅을 쓰지 않고 **같은 거부 모양을 손으로 구현한 2곳**(`CreateApiKeyModal:161-168` `setConfirmingDiscard(true)` · `RevealKeyModal:113-119` `setConfirmingClose(true)`) — 이며 이 화면은 그중 `CreateGroupModal.tsx:154` 다 | 프론트 구현 (DS Modal 소유) / 아키텍처 |

### 7.1 종결된 미결 사항 (구현 확인 후 닫음 — 2026-07-15)

**이 절이 존재하는 이유**: 이미 결정·구현된 항목이 '미결'로 남아 있으면 다음 사람이 같은 결정을 다시 내리려 든다.
아래 항목은 구현 소스를 열어 확인한 뒤 닫았다 — 되살리려면 근거가 새로 필요하다.

| 이전 # | 내용 | 종결 근거 (구현) |
|---|---|---|
| 2 | 알림 발송에 진행 표시·중복 클릭 차단·실패 안내가 없다 | **해결됨.** 발송 중 행 메뉴 항목이 '발송 중…' 으로 잠기고(회원 id 단위), 실패는 '다시 시도'가 붙은 위험 톤 토스트로 뜬다 — `MembersPage.tsx` `onNotify` · `notifyingIds` |
| 3 | 회원 삭제 실패 시 사용자에게 보이는 안내가 없다 | **해결됨.** 실패 시 다이얼로그를 닫지 않고 그 안에 위험 톤 배너를 띄운다(FS-003-EL-017.5) — `MembersPage.tsx` `deleteError` → `ConfirmDialog error` |
| 4 | 선택 체크박스가 어떤 동작에도 연결되지 않는다 | **해결됨.** 툴바에 일괄 액션 2종이 붙었다(FS-003-EL-018 · FS-003-EL-019) — `MembersToolbar.tsx` |
| 6 | 페이지가 총 페이지 수를 넘겼을 때 자동 보정이 없다 | **해결됨.** 총 페이지가 줄면 마지막 페이지로 보정하고 재조회한다 — `MembersPage.tsx` (`page > pages` 보정 effect) |
| 9 | 그룹 목록 조회 실패 시 전용 재시도 경로가 없다 | **해결됨.** 그룹 목록은 별도 쿼리이며 실패 시 좌측 필터에 재시도 경로가 열린다 — `MembersPage.tsx` `groupsError` / `refetchGroups` → `GroupFilter failed·onRetry` |
| — | 삭제 다이얼로그를 닫아도 진행 중인 요청이 취소되지 않는다 | **해결됨.** 취소·Esc·딤 클릭이 `AbortController` 로 요청을 중단한다 — `MembersPage.tsx` `closeDelete` |
