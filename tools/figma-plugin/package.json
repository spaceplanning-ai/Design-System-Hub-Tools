{
  "name": "@tds/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TDS Sync — contracts/tokens 산출물을 Figma Variables/Component Set으로 동기화하는 플러그인. 담당: Figma 플러그인, 검수: Figma 리뷰(G7)",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.6.3"
  }
}
