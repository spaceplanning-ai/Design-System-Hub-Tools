{
  "name": "@tds/nav-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "nav-config 파생물 동기화 게이트 — 정본(apps/admin/src/shared/layout/nav-config.ts)에서 파생됐으나 어떤 게이트도 최신 여부를 보지 않던 사본/생성물을 값으로 대조한다 (불일치 → exit 1)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
