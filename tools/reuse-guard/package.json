{
  "name": "@tds/reuse-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "재사용 가드 — check: 신규 컴포넌트 요청을 기존 계약과 비교해 CREATE_BLOCKED/EXTEND_RECOMMENDED/CREATE_OK 판정 (유사도 85% 이상 신규 생성 차단) · scan: 어드민 페이지 반복 UI 패턴을 스캔해 공통 모듈 후보 추출 (page-module-pipeline ②)",
  "scripts": {
    "check": "tsx src/index.ts",
    "scan": "tsx src/scan.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
